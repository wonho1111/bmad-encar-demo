# Team override for bmad-quick-dev.
# 레포에 커밋됨. 병합 규칙·위치 선정 이유는 `bmad-agent-dev.toml` 헤더 참조.
#
# ✎ 2026-07-29 장부 통합 — 이 파일이 담던 persistent_facts 2건을 **삭제했다.**
#
#   이 파일은 원래 "bmad-quick-dev의 step-01/02/04/oneshot 네 파일이 `deferred-work.md`를
#   가리키는데 그 파일이 동결됐으니 따르지 마라"를 주입하고 있었다. 2026-07-29에 장부를
#   `deferred-work.md`로 되돌렸으므로 **스킬의 원래 지시가 이제 맞는 지시다.**
#   막을 것이 없으니 주입할 사실도 없다 — 파일을 지우지 않고 이 경위만 남긴다
#   (되살리려는 다음 사람이 왜 비었는지 알아야 한다).
#
#   경위: `docs/decisions-archive.md` · `docs/tech-debt.md` 서문

[workflow]
persistent_facts = []
