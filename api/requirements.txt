# api/requirements.txt — 컨테이너(런타임)용 의존성 목록.
# Docker/Cloud Run은 이 파일을 보고 라이브러리를 설치한다(pyproject의 core + ai 그룹).
# 개발 전용(langgraph-cli·pytest·httpx)은 런타임에 불필요하므로 제외한다.
fastapi==0.137.1
uvicorn[standard]
supabase
psycopg[binary,pool]
pydantic-settings
# app/config.py가 dotenv_values를 직접 import한다(LangSmith env 승격, DW-658).
# pydantic-settings의 전이 의존성으로 딸려오지만, 앱 코드가 직접 쓰면 직접 선언해야
# 상위 패키지가 의존성을 바꿔도 안 깨진다.
python-dotenv
# app/rerank_client.py가 리랭커 사이드카를 직접 호출(4단계 부품 A). supabase의 전이
# 의존성으로 이미 딸려오지만 앱 코드가 직접 import하므로 명시 선언.
httpx
langgraph==1.2.4
langchain-google-genai
pgvector
sqlparse
# langchain-google-genai·langgraph의 전이 의존성으로 이미 딸려오지만, app/graph/agent.py·
# agent_tools.py(4단계 부품 B)가 @tool·ToolMessage 등을 직접 import하므로 명시 선언한다.
langchain-core

# 시세 진단 적정가 예측(3단계) — v2 고정(3.x는 PriorLabs 로그인·비상업 라이선스라 배제 확정)
tabpfn>=2,<3
