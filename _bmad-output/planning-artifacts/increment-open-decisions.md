# 브라운필드 증분 — 열린 결정 · 후속 리서치 (PRD/UX 입력)

- **작성일**: 2026-07-11 · **출처**: 랜딩 목업 v1(`차장님`) 사용자 피드백
- **성격**: 이번 증분(이미지·UI개편·반응형·랜딩) PRD/UX/스키마에서 **반드시 결정해야 할 열린 항목**. `reference-synthesis.md`와 함께 `bmad-prd` 입력.
- **상태**: ✅ **종결 (2026-07-15) — 3건 전부 결정됨.** 이 파일은 **경위 보관용**이며 열린 항목을 갖지 않는다.

> ## ✅ 결정 결과 요약 (2026-07-15 실측 대조로 확정)
>
> 헤더가 *"미결(PRD에서 확정)"* 인 채 2026-07-11 이후 동결돼 있었으나, **PRD·UX가 3건 모두 확정했고 이미 스토리로 분해까지 끝났다.** 문서만 "미결"로 남아 있었다.
>
> | 항목 | 결정 | 소비 스토리 |
> |---|---|---|
> | **① 상단 내비·IA** | **확정: `내 차 사기 · AI로 찾기 · 내 차 팔기`** — "매물 탐색"→"내 차 사기" 명칭 변경 채택(판매와 대구), **"문의"는 최상위 내비에서 제외**(매물 상세의 판매자 채팅이 주 경로). 로그인 시 우측 찜♡·채팅🔔·프로필▾. *(근거: `ux-designs/.../EXPERIENCE.md:41` D8)* | `11-2-상단-내비-재구성` |
> | **② 계정 역할 모델** | **확정: 역할 통합** — 가입 시 역할선택 제거, 소유권 기반 접근제어. *(근거: PRD `prd.md:130` **F14** — 조사한 국내 6개 서비스(엔카·헤이딜러·K Car·KB차차차·당근·번개장터)가 **전부 구매자/판매자를 계정으로 나누지 않음** = 업계 표준. "판매/구매"는 계정 타입이 아니라 지금 누른 버튼.)* ⚠️ 인증·RLS를 건드려 위험도가 높아 **별도 워크스트림(Epic 14)으로 분리** — 이 문서가 권고한 "분리 판단"이 그대로 채택됨 | Epic 14 (`14-1-role-check-완화-마이그레이션` · `14-2-가입-역할선택-제거-트리거-기본-role` · `14-3-소유권-기반-판매-게이트`) |
> | **③ 카드 뱃지·옵션 데이터 구조** | **확정** — 후속 리서치 완료(아래 §3의 "리서치 결론" 절이 그 산출물). 신뢰속성 `options`와 분리 · `accident_status` 3값 + 무사고만 초록 뱃지 · 셋 다 "판매자 신고" + 면책 라벨(가짜 검증 UI 안 만듦) · 옵션은 통제어휘 + priority 상위 3~4개 카드 노출 | Epic 10 (`10-1-신뢰속성-스키마-락스텝-반영` · `10-2-신뢰-뱃지-표시-면책-라벨` · `10-3-옵션-통제어휘-우선순위-상수-저장구조-정비` · `10-4-옵션-하이브리드-피커-등록`) |
>
> **확정 값의 정본은 여기가 아니다** — 계약은 `docs/conventions.md`, 스펙은 증분 PRD/UX/아키텍처(`*-increment-2026-07-12.md`)가 갖는다. 이 문서는 **"왜 그 질문을 했나"** 만 갖는다.
>
> 📌 **왜 이게 문제였나**: 결정이 끝났는데 문서가 "미결"로 남으면, 다음 사람이 **이미 끝난 논의를 다시 연다.** 열린 일 장부는 `docs/tech-debt.md` 하나다(CLAUDE.md B8).

---

## 1. 상단 내비게이션 · IA 재검토 (UX)
- **배경**: 목업 상단 메뉴 = `매물 탐색 · 내 차 팔기 · 문의` + (우측) `로그인 · 내 차 등록`.
- **결정 필요**:
  - `문의` 버튼이 최상위 내비에 필요한가? (문의는 매물 상세의 판매자 채팅이 주 경로 — 전역 내비 자리값이 맞는지)
  - `매물 탐색` → **`내 차 사기`**로 명칭 변경 검토(판매="내 차 팔기"와 대구, 사용자 의도 언어).
- **참고**: 기존 역할별 내비 규칙은 `nav-ia-rules.md`. 이 항목은 그 연장선 재검토.

## 2. 계정 역할 모델 재검토 (구매자/판매자)
- **배경**: 목업의 `내 차 등록` 버튼 노출은 현재 **계정이 판매자/구매자 역할로 구분**돼 있음을 전제(회원가입 시 역할 선택 — Story 1.2).
- **결정 필요**: 역할 구분을 **유지 / 계정 내 역할 전환 허용 / 구매자·판매자 통합**(누구나 사고팔기) 중 어디로 갈지.
  - 통합 시: `내 차 등록`을 모두에게 노출, RLS·프로필 스키마·가입 흐름 영향(Epic 1 전반). 영향 범위 큼 → PRD에서 신중히.
- **주의**: 역할 통합은 **인증/권한 구조(profiles.role, RLS)** 를 건드리므로 이미지/랜딩 작업과 별개의 큰 결정. 분리 판단 권장.

## 3. ★ 카드 뱃지·옵션 데이터 구조 (가장 큰 항목 — 후속 리서치 필요)
- **배경**: 목업 카드가 옵션(후방카메라·스마트키 등)을 하단 태그로, 무사고를 사진 위 오버레이로 표시. 사용자 지적:
  - **(a) 옵션 희소도**: 스마트키·블루투스·후방카메라는 이제 **보편화**돼 변별력이 없음 → 태그로 나열하기보다 **오토파일럿·파노라마·V2L 등 희소성 있는 옵션을 우선 노출**.
  - **(b) 옵션 아닌 신뢰속성 분리**: `무사고·1인소유·비흡연`은 "장비 옵션"이 아니라 **차량 신뢰속성**이라 별도 데이터로 구분·관리해야 함.
  - **(c) 검증 가능성 차등**(중요):
    - `무사고` — 보험이력·성능기록부에서 도출(객관 근거). 현재 seed는 `accident_free`(boolean) 보유.
    - `1인소유` — 보험이력 데이터가 있어야 도출 가능 → **추가 데이터 필요**.
    - `비흡연` — **객관 검증 수단 없음**(판매자 자기신고). 데모엔 실제 보험이력 연동이 없으므로 이 셋의 근거 수준을 어떻게 다룰지 정의 필요.
  - **(d) 배치 방향(사용자 선호)**: 무사고·1인소유·비흡연 등 **신뢰속성 = 카드 상단(무사고 옆)**, **옵션 = 하단 + 희소 옵션 우선**.
- **결정 필요(스키마·표시)**:
  1. 신뢰속성을 `options(text[])`와 분리된 **별도 구조**(예: 신뢰속성 boolean 컬럼들 또는 태그 테이블)로 둘지.
  2. 옵션 **희소도** 개념을 어떻게 표현할지(보편 옵션 목록 정의 후 숨김 / 희소도 순 정렬 / 큐레이션).
  3. 데모 제약(보험이력 API 없음) 하에서 각 신뢰속성의 **근거 라벨**(검증됨 vs 판매자 신고)을 UI에 어떻게 표기할지.
- **🔎 후속 리서치 필요**(agent-reach, sonnet — PRD 前 권장):
  - 중고차 플랫폼이 **무사고/1인소유/비흡연/옵션**을 각각 어떻게 **저장·검증·표시**하는가.
  - **보험이력·성능기록부**에서 나오는 데이터 항목(무사고·소유자변경·용도이력 등)과, 자기신고 항목의 구분 관행.
  - 옵션 **표준화/희소도** 처리(보편 옵션 vs 셀링포인트 옵션) 사례.
  - (참고) 조사 종합 `reference-synthesis.md`에서 기아 "보험이력없음", K Car 특징 태그, 엔카 성능기록부 노출 등 단서 이미 있음 → 그 위에 심화.

### 리서치 결론 (2026-07-11 완료) — `research-data-trust-attributes.md` · `research-data-options.md`

**A. 신뢰속성 (무사고·1인소유·비흡연)**
- **업계도 옵션(장비)과 신뢰속성("핫마크")을 데이터·UI 양쪽에서 분리** → 우리도 분리 확정. 스키마: `options text[]`(장비) ↔ 별도 신뢰속성 필드(예 `accident_status`, `is_single_owner`, `is_non_smoker`). `accident_status`는 이상적으로 enum(무사고/외판교환/사고, 엔카 4단계), 데모는 **무사고/사고 2단 단순화** 가능(기존 `accident_free` boolean 승격).
- **검증 3티어 실존**: ① 법정 성능·상태점검기록부 + 보험개발원 카히스토리(객관) · ② 플랫폼 진단(오진단 보상) · ③ 판매자 자기신고(면책). **데모는 ①② 인프라 없음 → 셋 다 ③ "판매자 신고"로 통일** 처리(엔카·KB "핫마크" 선례와 동일, 억지 아님).
- **정직성 규칙(채택)**: 신뢰속성 뱃지 옆/상세에 **"판매자 제공 정보" 면책 라벨** 부착(엔카 "사실여부 직접 확인" 선례 재현). 과장 없는 문구 선호 — 밋밋한 "무사고"보다 출처 명시형(기아 "보험이력없음" 스타일). **가짜 검증 API/UI는 만들지 않는다**(정직).
- 각 속성 근거·한계: **무사고**=성능기록부(주요골격 판금·용접·교환만; 외판교환은 무사고로 표기) · **1인소유**=카히스토리 소유자변경 0회(객관 검증 가능하나 카드 아이콘은 대개 자기신고) · **비흡연**=검증수단 원천 부재(순수 자기신고).
- **배치(사용자 선호 확증)**: 신뢰속성 = 카드 상단(무사고 옆), 옵션 = 하단.

**B. 옵션 (희소도)**
- 업계는 **통제된 옵션 사전**(카테고리+표준명) 사용(엔카: 5카테고리 외관·내장/안전/편의·멀티미디어/시트/기타). 우리 seed는 자유 `text[]` → 데모는 정규화 테이블까진 과설계, **"표준 옵션명 상수 목록"**만 두고 그 밖 값 배제.
- 업계 카드/상세는 **옵션 전량 나열**이 기본(희소도 큐레이션 UI는 드묾). 단 **가치·희소도 데이터는 실존**(엔카 감가율 방어 랭킹: 선루프 1.5~2% > 내비≈HUD 1% > 차로이탈방지 0.5~1%; 현대 CPO는 옵션 원가 명시).
- **우리 구현(채택)**:
  1. **보편 옵션 하드코딩 목록**(스마트키·블루투스·후방카메라·열선시트·자동에어컨·크루즈컨트롤 등) = 저순위.
  2. **희소도 priority 매핑**(옵션명→점수): 엔카 랭킹 + 희소 옵션(오토파일럿·파노라마선루프·V2L 등) 수동 추가.
  3. **카드 = priority 상위 3~4개만**; 희소 옵션 없으면 보편 옵션으로 fallback.
  4. **상세 = 카테고리 섹션 전량 노출**(희소 필터 없음, 업계 관행 일치).
  5. 옵션 **금전가치 표기 = 데모 과설계 → 제외**(seed에 옵션가 없음).

---

## 우선순위·연계
- **#3 = 이미지 증분과 직접 연동**(카드가 사진+뱃지+옵션으로 재구성되므로 스키마·계약 함께 확정). → 후속 리서치 1회 돌리고 PRD에 반영 권장.
- **#1 = 경량 UX 결정**(PRD/UX에서 바로).
- **#2 = 인증 구조 큰 결정** → 이미지/랜딩과 분리해 별도 판단(범위 팽창 주의).

> 확정된 것(목업 v1 기준): 전체 방향(AI검색 히어로+마켓 그리드+이미지 카드) · 색(페트롤+앰버) · 이미지 비율 5:3 · 서비스명 **차장님**.
