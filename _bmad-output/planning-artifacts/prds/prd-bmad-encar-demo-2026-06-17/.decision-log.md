# 결정 로그 (Decision Log) — bmad-encar-demo PRD

이 파일은 PRD 작성 과정의 모든 결정·변경·가정·오버라이드를 시간순으로 기록하는 정식 메모리이자 감사 추적입니다.

## 2026-06-17

- **의도(Intent):** Create — 기존 PRD 없음, 신규 작성.
- **입력 자료:** `docs/idea.md`, `_bmad-output/planning-artifacts/research/technical-ai-search-architecture-research-2026-06-16.md`.
- **성격(Stakes):** 컨설팅 과정 과제 제출 + 간단한 시연용 → 중간 분량(5~8쪽), 출시용 운영/규정 섹션은 축소.
- **작업 방식:** 코칭 경로(Coaching path) 선택.
- **RAG 대상 문서 자료:** PRD 단계에서는 요구사항만 정의, 실제 문서 내용은 구현 단계에서 작성하기로 결정.
- **진입점:** 여정 중심(Journey-led) 선택.
- **[철회됨] AI 검색 범위 축소 (오독):** 한때 "문서 기반 RAG 경로 제거, Text-to-SQL만"으로 기록했으나, 사용자 의도 검증 결과 오독으로 판명되어 아래 항목으로 **정정**함.
- **[정정·확정] AI 검색 아키텍처 (idea.md 2경로 원안 유지):**
  - **구분:** ① *순수 상식 Q&A*(매물 없이 안내문만) = **제외**. ② *질적/의미형 매물 검색*("잔고장 없는", "유지비 저렴한", "패밀리카로 좋은", "사회초년생용") = **포함**.
  - **라우터 노드** → **경로 A(SQL 기반 RAG): Text-to-SQL** 구조 조건 필터(가격·연식·차종·색상·주행거리) / **경로 B(문서 기반 RAG): 차량 상식·가이드 문서 임베딩 유사도 검색(pgvector)** 으로 적합 차종 근거 확보 → 매물 추천.
  - **출력 원칙:** 두 경로 모두 "매물 카드 + 근거 설명"을 반환. 매물 없는 순수 상식 답변은 안 함.
  - **근거:** 과제 목적이 "문서 기반 RAG + SQL 기반 RAG 둘 다 경험". 두 경로 모두 학습 산출물로 필수.
  - **영향:** 차량 상식/구매 가이드 문서 코퍼스가 **필수 산출물**로 확정(구현 단계에서 작성). pgvector·Gemini 임베딩 유지.
- **판매자 계정/역할:** 가입 시 역할 분리(구매자 계정 / 판매자 계정 구분).
- **차량 등록 노출:** 등록 즉시 노출(관리자 사전 승인 없음).
- **"구매 완료" 처리:** 판매자만 누르며 상대 구매자 지정 불필요(판매 완료 플래그 처리).
- **역할 분리 구조:** 가입 시 구매자/판매자 역할 선택, 계정은 해당 역할로 고정(둘 다 하려면 계정 2개). 시연 단순화 목적.
- **관리자 범위:** 회원 조회+정지/삭제 · 전체 매물 조회+삭제 · 거래내역(판매완료 매물 목록) 조회 · 채팅방 열람+삭제.
- **[확정] AI 라우터 가드:** 라우터가 의도를 3분류 — (A) 구조형 매물 검색→Text-to-SQL, (B) 질적/의미형 매물 검색→문서 기반 RAG, (C) 매물과 무관한 질문→정중히 거절+검색 유도. 가드(C)를 두는 쪽이 라우터 분기 1개 추가로 끝나 구현이 더 쉽고, 안전장치(SELECT 전용·범위 제한)와도 부합. 막지 않으면 무의미 결과 처리 로직이 추가로 필요.
- **로그인 방식:** 이메일/비밀번호만(Supabase Auth). 소셜 로그인은 시연 범위 밖.
- **신고 기능 없음:** 사용자 신고 흐름 미구현. 관리자가 직접 조회·조치(매물·채팅 삭제)하는 수준.
- **[확정·그라운딩] 매물 등록 필드(FR5) = 확장 셋 ≈16개:** 제조사 · 모델 · 차종 · 연식 · 가격 · 주행거리 · 색상 · 연료 · 변속기 · 배기량 · 인승 · 지역 · 사고이력(무사고 여부) · 옵션 · 설명 · 사진.
  - 근거: 엔카·KCar·헤이딜러 그라운딩(Explore 서브에이전트 웹리서치). 연료·변속기·지역·사고이력은 AI 자연어 검색(FR14·15) 임팩트를 좌우하는 핵심 필터.
  - 제외(데모 복잡도): 차대번호·69항목 검수기록·압류/저당·명의이전·전주이력.
- **[확정] FR 목록:** 25개 / 6그룹. 중복 정리 완료(판매완료 미노출→FR11 단일 규칙, FR1/FR3 역할 중복 제거). FAST 작성 아님(코칭).
- **성공 지표:** SM2로 기본 검색(FR9) 지표 추가(사용자 요청).

## Finalize (2026-06-17)

- **결정 로그 감사:** 모든 결정 PRD/addendum 반영 확인.
- **입력 재조정(서브에이전트 2):** idea.md → 문서 RAG 코퍼스에 "매물 설명 텍스트" 누락 발견 → FR15·addendum에 ①매물 텍스트+②문서 둘 다로 복원. research → 임베딩 차원 일치 제약·SQL 결정론적 안전장치·구현 전 확정항목 누락 → NFR2 보강 + addendum 차원 제약 + §8 오픈이슈(OI1~6) 신설. (reconcile-idea.md, reconcile-research.md)
- **Reviewer Gate(루브릭 워커):** 판정 PASS-WITH-NITS. 라우터 가드 경계(→FR16 보완·OI5), 매물 카드 필드·0건 처리(→FR17), 용어 드리프트(→§1 용어 정의) 반영. (review-rubric.md)
- **오픈이슈 triage:** Phase-blocker 없음. 구현 전 확정 항목은 §8 OI1~6으로 이관(아키텍처 단계 처리).
- **폴리시:** 초안 안내문 제거, UJ-2 등록필드·§4 코퍼스 표현을 FR과 일치, 용어 통일.
- **종료:** `prd.md` status: final. 외부 핸드오프 없음(external_handoffs 미구성).
- **다음 단계 후보:** bmad-ux / bmad-create-architecture / bmad-create-epics-and-stories.

## 외부 설정 진행 (2026-06-17, PRD 이후)

- **git:** 저장소 init 완료(main+develop), 초기 커밋 `b54364f`, 현재 develop 작업.
- **Gemini(OI1) 확정:** 무료 등급 / 생성 `gemini-flash-latest` / 임베딩 `gemini-embedding-001` 차원 **768**(실호출 검증). `GEMINI_EMBEDDING_DIM=768`, pgvector `vector(768)`.
- 보안: 사용자가 초기 노출 키 폐기 후 신규 키 발급, `.env`에만 보관 권고.
- 점검 스크립트: `scripts/check-embedding-dim.ps1`(키 미포함).
- **구매자 여정 끝점:** 직거래 연결(문의 채팅)까지가 서비스 범위. 결제·계약은 서비스 밖(오프라인). 앱 내 결제 없음.
- **구매자 페르소나:** 지훈(32, 중고차 초보, 예산 3천만원, 가족용 SUV 희망).

## 스키마 정밀화 (2026-06-18, 아키텍처 이후)

- **매물 단위 고정:** `mileage`=정수 km, `price`=원(KRW), `displacement`=cc. 저장·입력·검색·Text-to-SQL·표시 전 구간 통일(mile 금지). 아키텍처 "단위·측정 규칙"에 명문화.
- **listings 컬럼 제약 확정:** 고정 짧은 목록(color·fuel·transmission·region·manufacturer·body_type)은 `text + CHECK`, 숫자 범위(year·seats)는 `CHECK BETWEEN`, displacement는 `CHECK >= 0`(전기차 0 허용). model은 자유 text. 조합 제약은 미적용(시드·UI에서 통제).
- **[검토 후 철회] 차급·차형 분리안:** 차종을 차급(크기 등급)+차형(차체 형태) 2필드로 나누는 안을 검토했으나 **철회**. Encar·K-Car 등 국내 중고차 앱이 차종을 **단일 분류**로 쓰는 관행을 따라 `body_type` 단일 컬럼 유지. 허용값(12): 경차·소형차·준중형차·중형차·대형차·스포츠카·SUV·RV·경승합차·승합차·화물차·기타. FR5는 16필드 그대로(PRD FR5에 분류값 주석 추가).
- **차형 용어 매핑(OI5 추가):** Encar식 차종 분류엔 "세단·해치백·쿠페"가 없어, 그런 용어 질의 시 Text-to-SQL 매핑 규칙(예: "세단"→준중형차·중형차·대형차)을 질의셋(OI5)에서 정의하도록 명시.
- **컬럼명 매핑 확정:** 주행거리→`mileage`, 제조사→`manufacturer`, 모델→`model`, 차종→`body_type`, 연식→`year`, 가격→`price`, 색상→`color`, 연료→`fuel`, 변속기→`transmission`, 배기량→`displacement`, 인승→`seats`, 지역→`region`, 사고이력→`accident_free`(boolean), 옵션→`options`(text[]), 설명→`description`, 사진→`photos`(text[]). 상세 DDL은 `architecture.md` 참조.
