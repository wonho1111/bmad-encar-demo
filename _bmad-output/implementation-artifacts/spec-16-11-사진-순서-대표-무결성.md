---
title: '16.11 사진 순서·대표 선정 무결성(부분 실패 경로)'
type: 'bugfix'
created: '2026-08-10'
status: 'done'
baseline_revision: 'e9664e87e9edaac4d4aa20a355cef9fef840da2f'
final_revision: '21beada3c93fa96a8221b7d4f8680cc8f3fc1268'
review_loop_iteration: 0
followup_review_recommended: true
context:
  - '{project-root}/docs/conventions.md'
  - '{project-root}/_bmad-output/implementation-artifacts/epic-16-context.md'
  - '{project-root}/_bmad-output/implementation-artifacts/epic-16-bundle-review-2026-08-10.md'
---

<intent-contract>

## Intent

**Problem:** 사진 저장 파이프라인(`photo_sync.dart`)의 **부분 실패 경로**에서 "대표 사진이 사용자가 고른 장이 아닌 다른 장으로 조용히 바뀌는" 결함 3건이 같은 축에 모여 있다. 사용자 판단으로 **최소 수정이 아니라 근본 수정**으로 간다 — *"데이터 쌓이는 건 좀 치명적인 것 같아서, 물론 과제긴 하지만 그래도 실사용 기반으로 하는 거니까"*(2026-08-10).

**⚠️ 먼저 알아야 할 사실 — 대표는 `is_cover`가 아니라 `(sort_order, id)` 최솟값으로 읽힌다.**
`listings_repository.dart`의 `pickCoverImages`가 조회 시 `(sort_order, id)` 최솟값 행을 대표로 고르고, `is_cover` 컬럼을 **읽는 소비처는 앱에 없다**(`grep is_cover app/lib` = `photo_sync.dart`의 쓰기 4곳뿐). 따라서 **이 스토리에서 고쳐야 할 레버는 `sort_order`이지 `is_cover`가 아니다.** `is_cover`만 손보면 화면은 그대로다.

**세 결함:**

1. **(묶음 코드리뷰 2026-08-10, 리뷰어 2명 독립 발견 — 이 스토리의 헤드라인)**
   Storage 오브젝트 삭제는 성공했는데 `listing_images` 행 삭제가 실패한 항목(`undeletable`, `status: PhotoStatus.error`)이 `next`에 합류한 뒤, 3단계의 `saved` 필터(`next.where((p) => p.storagePath != null)`)가 **`status`를 보지 않아** 정상 사진으로 취급된다. 그 결과 `sort_order` UPDATE를 다시 받고, `saved`의 유일한 원소면 반드시 `order == 0`을 받아 **파일이 없는 행이 대표가 된다**. 트리거: 사진 1장짜리 매물에서 그 사진을 지우다 ①성공 ②실패.
   ⚠️ 코드 주석(`:207-212`)이 밝히듯 **예전 방식(목록에서 아예 빼기)도 결함이었다** — 사용자가 재시도할 수단이 사라지고 DB엔 파일 없는 행이 영구히 남았다. 그래서 "목록에서 뺀다"로 되돌리는 것은 **해법이 아니다.**

2. **[[DW-748]]** — 기존 행 `sort_order` UPDATE가 실패하면 `continue`로 카운터를 안 올려 다음 항목이 그 번호를 메우는데, **실패한 행은 옛 번호를 그대로 들고 DB에 남는다.** 그래서 옛 값과 새 값이 겹치고, 대표 판정이 `(sort_order, id)`라 **id 타이브레이크로 사용자가 아래로 내린 사진이 대표가 될 수 있다.** 사용자에겐 "사진 순서를 저장하지 못한 항목이 있어요"만 뜨고 대표가 바뀐다는 말은 없다.

3. **[[DW-751]]** — 수정 화면의 기존 사진 조회 경로(`fetchOwnListingPhotos` → `toPhotoItems`)가 **전 테스트 스위트에서 한 번도 실행되지 않는다**(유일한 테스트가 리포지토리 메서드를 통째로 가짜로 갈아 끼운다). `.order('sort_order')`나 `id` 타이브레이크가 빠져도 아무도 못 잡는데, 그러면 수정 화면이 기존 사진을 **임의 순서로** 실어 판매자가 **사진을 건드리지 않고 저장만 해도 대표가 다른 장으로 바뀐다.**

**Value:** 세 건 모두 "사용자가 모르는 사이 데이터가 잘못 확정되는" 결함이고, 화면 문제와 달리 **다시 그려서 되돌릴 수 없다.** 셋이 같은 함수·같은 불변식을 공유하므로 따로 돌면 반쪽이 된다.

## Boundaries & Constraints

**Always:**
- **불변식을 먼저 코드로 못박는다:** *"`syncListingPhotos`가 끝난 뒤, 그 매물의 `listing_images` 행 중 `sort_order` 값은 서로 중복되지 않고, 화면에서 맨 앞에 있던 **정상 저장된** 사진이 `(sort_order, id)` 최솟값을 갖는다."* 아래 수정은 전부 이 한 문장을 지키기 위한 것이다.
- **3단계 재편입 차단(결함 1):** `saved` 선정이 `storagePath != null`만 보지 않게 한다. `PhotoStatus.error` 항목은 **행 기록 단계(3단계)에서 제외**하되 **`next` 목록에는 그대로 남긴다** — 사용자가 삭제 버튼을 다시 눌러 재시도할 수 있어야 하고(위 ⚠️), 화면의 오류 안내도 유지돼야 한다. 즉 "목록에서 빼기"가 아니라 "**저장 대상에서만 빼기**"다.
- **번호 중복 차단(결함 2, DW-748):** `sort_order` UPDATE가 실패한 행이 옛 번호를 들고 남아 새 번호와 겹치지 않도록 한다. 구현 방향은 dev 세션이 정하되 **근거를 스펙 Design Notes에 남긴다.** 후보(택일 아님, 조합 가능): ⓐ UPDATE 실패 시 그 행도 저장 대상에서 빼고 카운터를 올려 번호 구간이 겹치지 않게 한다 ⓑ renumber를 실패 항목까지 포함해 **한 번에** 계산한 뒤 개별 실패를 경고로만 남긴다 ⓒ 실패가 하나라도 있으면 그 매물의 순서 확정을 다음 저장으로 미루고 사용자에게 그렇게 안내한다. **⚠️ "경고 문구만 고치기"는 해법이 아니다** — 데이터가 이미 어긋난 뒤다.
- **사용자에게 사실대로 알린다:** 부분 실패로 **대표가 바뀔 수 있는** 상황이면 지금의 *"사진 순서를 저장하지 못한 항목이 있어요"* 로는 부족하다. 무엇이 어떻게 될지 한 줄로 알린다(문구는 기존 한국어 톤 유지).
- **결함 3(DW-751) 검사 신설:** `toPhotoItems`는 원행 map으로 **직접 단언**하고(같은 리포의 `pickCoverImages`·`sortGalleryPaths` 테스트 관행), `fetchOwnListingPhotos`는 **가짜 http로 나간 쿼리에 `order=sort_order.asc,id.asc`가 실렸는지** 본다(`listings_repository_fr11_wire_test.dart`가 어제 만든 URL 캡처 기법을 그대로 쓴다).
- **검사는 red→green을 실측으로 증명한다**(CLAUDE.md B4). 각 축마다 **리뷰어가 예상할 표기와 다른 형태로** 뮤테이션해 red를 확인하고 되돌려 green을 확인한다 — 자기가 쓴 표기로만 깨면 그 검사가 실제로 무엇을 보는지 증명되지 않는다.
- **불변식 자체를 재는 검사를 최소 1개 만든다:** 개별 분기 단언과 별도로, "부분 실패를 섞은 시나리오를 돌린 뒤 최종 `sort_order` 집합에 중복이 없고 대표가 의도한 장이다"를 한 번에 확인하는 검사. `photo_sync_test.dart`의 기존 fake http 하네스로 조립 가능하다.
- 끝나면 **[[DW-748]]·[[DW-751]]을 done으로 닫는다**(`resolution:` 포함). 결함 1은 이 스펙이 원 출처이므로 별도 DW를 만들지 않는다.

**Block If:**
- 결함 2의 세 후보 중 어느 것도 이 코드베이스에서 성립하지 않는다고 판단되면(예: PostgREST 단건 UPDATE로는 원자적 renumber가 불가능하고 DB 함수가 필요하다는 결론) **멈추고 escalate**한다 — 마이그레이션 신설은 이 스토리 범위를 넘고 사용자 승인이 필요하다.

**Never:**
- **DB 마이그레이션을 새로 만들지 않는다.** 이 스토리는 클라이언트 측 무결성만 다룬다(필요하다고 판단되면 위 Block If로 멈춘다).
- `is_cover` 컬럼을 대표 판정의 근거로 바꾸지 않는다 — 조회 계약(`pickCoverImages`, 웹 공통)을 바꾸는 일이라 별건이다.
- 삭제 실패 항목을 **화면 목록에서 제거하지 않는다**(재시도 수단이 사라진다 — 이미 한 번 겪은 실패).
- 웹을 건드리지 않는다.
- 업로드 순차 처리(AC9, 10장 상한 경합 방지)를 병렬로 바꾸지 않는다.
- 사진 상한·재인코딩 파라미터·경로 규약(`docs/conventions.md` §10.1) 어느 것도 바꾸지 않는다.
- 운영(`main`) 배포를 하지 않는다.

</intent-contract>

## Code Map

- `app/lib/features/listings/photo_sync.dart` -- 3단계 `saved` 선정에서 `PhotoStatus.error` 제외 · `sort_order` 중복 차단 · 부분 실패 시 사용자 안내 보강.
- `app/lib/features/listings/photo_item.dart` -- `toPhotoItems`(변경 없을 수 있음, 검사 대상).
- `app/lib/features/listings/listings_repository.dart` -- `fetchOwnListingPhotos`의 정렬(변경 없을 수 있음, 검사 대상).
- `app/test/photo_sync_test.dart` -- 결함 1·2 분기 검사 + **불변식 통합 검사**.
- `app/test/listings_repository_photo_order_test.dart` (신규 또는 기존 파일 확장) -- `toPhotoItems` 원행 단언 · `fetchOwnListingPhotos` 쿼리 URL에 `order=sort_order.asc,id.asc` 단언.
- `_bmad-output/implementation-artifacts/deferred-work.md` -- DW-748·DW-751 done 처리.

## Tasks & Acceptance

**Execution:**
- `photo_sync.dart` -- 3단계 `saved`에서 error 항목 제외(목록엔 유지) -- 결함 1 근본 수정
- `photo_sync.dart` -- `sort_order` 중복 차단 -- DW-748, 근거를 Design Notes에 기록
- `photo_sync.dart` -- 부분 실패 안내 보강 -- "대표가 바뀔 수 있다"를 사용자가 알게
- `app/test/photo_sync_test.dart` -- 분기별 검사 + 불변식 통합 검사, red→green 실측 -- CLAUDE.md B4
- `app/test/...photo_order...` -- `toPhotoItems`·`fetchOwnListingPhotos` 검사 -- DW-751
- `deferred-work.md` -- DW-748·DW-751 닫기 -- CLAUDE.md B8

**Acceptance Criteria:**
- Given 사진 1장짜리 매물에서 그 사진을 삭제하는데 Storage 삭제는 성공하고 DB 행 삭제가 실패하면, when 저장이 끝나면, then **그 죽은 행은 `sort_order`를 새로 받지 않고**, 그 매물의 대표는 그 행이 아니다. 그리고 그 항목은 **화면 목록에 오류 상태로 남아 재시도할 수 있다.**
- Given 기존 사진 여러 장 중 하나의 `sort_order` UPDATE가 실패하면, when 저장이 끝나면, then 그 매물의 `listing_images` 행들의 `sort_order` 값에 **중복이 없다**.
- Given 부분 실패가 있었다면, when 사용자가 결과 안내를 보면, then 순서·대표가 확정되지 않았을 수 있다는 사실을 알 수 있다.
- Given `fetchOwnListingPhotos`, when 호출되면, then 나가는 쿼리에 `order=sort_order.asc,id.asc`가 실려 있다(URL로 단언).
- Given `toPhotoItems`, when 원행 map을 넘기면, then 반환 순서·필드가 원행 기준으로 단언된다(리포지토리를 가짜로 갈아 끼우지 않는다).
- Given 위 각 축, when 채택 전 **리뷰어 표기와 다른 형태로** 일부러 깨면, then red가 되고 되돌리면 green이 된다(실행·관찰로 증명).
- Given 부분 실패를 섞은 통합 시나리오, when 돌리면, then 최종 `sort_order` 집합에 중복이 없고 대표가 의도한 장이다(불변식 1개로 확인).
- Given `deferred-work.md`, when 이 스토리가 끝나면, then DW-748·DW-751이 `resolution:`과 함께 done이다.

## Design Notes

**실기기 확인은 이번엔 인수조건에 넣지 않는다 — 그 이유를 남긴다.**
이 스토리의 트리거는 전부 **부분 실패**(Storage는 성공하고 DB만 실패, UPDATE만 실패)라 실기기에서 **의도적으로 재현할 수단이 없다**(네트워크를 그 순간에만 끊어야 한다). 그리고 판매자 흐름이라 **로그인이 필요한데 이 세션은 운영 계정 자격증명을 갖고 있지 않다**(2026-08-10 묶음 코드리뷰 실기기 확인에서 같은 이유로 찜·채팅 로그인 후 화면을 못 봤다). 그래서 이 스토리의 증명은 **가짜 http로 부분 실패를 조립하는 테스트**가 맡고, 실기기는 "정상 경로가 깨지지 않았는지"만 본다. **이 한계를 결과 보고에 그대로 적는다 — "실기기에서 확인했다"고 쓰지 않는다.**

**참고 — 어제 만든 기법 2개를 그대로 쓸 수 있다:**
`listings_repository_fr11_wire_test.dart`의 **URL 캡처 fake**(나가는 쿼리스트링을 단언), `chat_repository_test.dart`의 **`_ScriptedHttpClient`**(요청 순서별로 다른 응답을 돌려줌 — 부분 실패 조립에 그대로 맞는다).

**결함2(DW-748) 후보 선택 근거 — Always 절이 요구한 대로 여기 남긴다.**
Boundaries & Constraints의 세 후보 중 **ⓐ(실패 시 카운터를 올려 번호 구간을 겹치지 않게 한다)와 ⓑ(전체를 한 번에 계산)를 조합**해 채택했다. ⓐ만으로는 "재정렬 + 실패가 겹친" 경우 — 재정렬로 새로 매기는 번호가 다른 행의 옛(아직 안 비워진) 값과 우연히 같아지는 것을 못 막는다(옛 값을 미리 모르면 피할 수 없다). 그래서 저장 시작 전에 그 매물의 현재 `sort_order` 전체를 한 번 읽어(ⓑ) "아직 비워지지 않았을 수 있는 번호"로 예약해 두고, 새 번호를 고를 때 그 예약을 건너뛰게 했다. ⓒ(실패가 하나라도 있으면 그 매물의 순서 확정을 다음 저장으로 미룸)는 이미 성공한 개별 UPDATE를 되돌릴 방법이 없어(원자적 다건 UPDATE는 DB 함수 없이 불가 — 이 스토리의 Block If에 걸린다) 채택하지 않았다.
아직 아무도 대표가 되지 못한 동안엔, 이 후보 자신의 옛 번호가 이미 (자신을 뺀) 다른 모든 예약값보다 **반드시 작을 때만** 그 값을 그대로 쓰고(같을 때 그대로 재사용하면 그 값을 아직 들고 있는 다른 행과 살아 있는 중복이 된다 — 2회차 후속 코드리뷰 실측), 그렇지 않을 때만 예약값보다 작은 번호(필요하면 음수 — `sort_order`는 화면에 노출되지 않는 내부 정렬키이고 `0012_listing_images.sql`에 하한 CHECK가 없음을 실측 확인)로 내려간다. **첫 구현은 이 "자기 옛 번호가 이미 안전하면 재사용" 판단이 빠져 있어서, 실패가 전혀 없는 평범한 재저장에서도 대표 사진의 `sort_order`가 저장할 때마다 계속 더 음수로 밀려나는 회귀를 만들었다**(후속 코드리뷰 2026-08-10에서 지적, `photo_sync_test.dart`에 반복 재저장 드리프트 회귀 검사로 고정했다) — 그래서 자기 옛 번호를 우선 재사용하는 조건을 추가했다. 예약값도 값 하나당 개수를 세는 맵으로 관리한다: 이미 두 행이 같은 옛 번호를 든(부분 실패가 누적된) 데이터에서 한 행이 그 값을 벗어나도 나머지 행이 여전히 그 값을 들고 있으면 계속 예약 상태로 남아야 하기 때문이다.

## Verification

- `flutter analyze` 0건.
- `flutter test` 전량 통과(현재 496건 기준, 신규분 포함).
- 각 신규 검사의 red→green 실측 로그를 Auto Run Result에 남긴다(뮤테이션 내용까지 적는다 — "깼다"만 적으면 무엇을 깼는지 다음 사람이 모른다).
- 실기기는 정상 경로 회귀 확인만(부분 실패는 재현 불가 — 위 Design Notes).

## Review Triage Log

### 2026-08-10 — Review pass
- intent_gap: 0
- bad_spec: 0
- patch: 6: (high 1, medium 4, low 1)
- defer: 1: (medium 1)
- reject: 10
- addressed_findings:
  - `[high]` `[patch]` (adversarial+intent-alignment, 독립 발견) `candidateOrder`가 `!coverAssigned` 분기에서 자기 옛 번호(`ownOld`)를 무시하고 항상 `min(reservedOrders)-1`을 반환해, 실패가 전혀 없는 평범한 재저장에서도 대표 사진의 `sort_order`가 저장할 때마다 계속 더 음수로 밀려나는 회귀가 있었다. 자기 옛 번호가 이미 (자신을 뺀) 다른 모든 예약값보다 작거나 같으면 그 값을 그대로 쓰도록 고쳤다 — `photo_sync.dart` candidateOrder. 회귀 재현 검사 추가(반복 재저장 시 drift 없음 단언) 후 red→green 실측(뮤테이션: 안전 재사용 판단을 제거해 첫 구현으로 되돌림 → 신규 검사 1건만 red, 원복 → green).
  - `[medium]` `[patch]` (edge-case-hunter) `deferred-work.md` DW-748 항목에 새로 추가한 `status: done 2026-08-10`과 원래 있던 `status: open`이 함께 남아 항목이 자기모순이었다 — 옛 `status: open` 줄을 삭제했다.
  - `[medium]` `[patch]` (intent-alignment) 스펙의 `Always` 절이 "구현 방향의 근거를 스펙 Design Notes에 남긴다"고 명시했는데, DW-748 후보 선택(ⓐ+ⓑ 조합, ⓒ 기각 사유) 근거가 코드 주석과 `deferred-work.md`에만 있고 이 스펙의 `## Design Notes`에는 없었다 — 근거를 옮겨 적었다(위 P1 회귀·수정 경위 포함).
  - `[medium]` `[patch]` (adversarial) 기존 `sort_order` 사전조회(`reservedOrders`)가 실패하면 중복·오지정 방지 안전망이 `print()`만 남기고 조용히 꺼져, "부분 실패로 대표가 바뀔 수 있으면 사실대로 알린다"는 스펙 Always 요구를 이 실패 모드에서는 지키지 못했다 — 이 catch 블록에 `warnings.add(...)`를 추가했다.
  - `[medium]` `[patch]` (edge-case-hunter) `reservedOrders`가 값 기준 `Set<int>`라, 이미 두 행이 같은 옛 `sort_order`를 든(부분 실패가 누적된) 데이터에서 한 행이 성공해 그 값을 벗어나면 다른 행이 여전히 그 값을 들고 있어도 예약이 통째로 풀려 DW-748이 재발할 수 있었다 — 값→개수 맵으로 바꿔 마지막 한 행이 빠져나갈 때만 실제로 해제하게 했다.
  - `[low]` `[patch]` (verification-gap) `candidateOrder`의 대표-후보(음수 가능) 분기 중 **새로 INSERT되는 사진**이 후보가 되는 경로(예: 기존 사진이 있는 매물에 새 사진을 맨 앞으로 끌어와 저장)를 실행하는 테스트가 하나도 없었다(기존 검사는 전부 `_savedPhoto`만 조합) — `photo_sync_test.dart`에 신규 사진+기존 사진 혼합 시나리오 검사를 추가했다.
  - `[medium]` `defer` (edge-case-hunter) 같은 매물에 대한 `syncListingPhotos` 동시 호출(저장 버튼 연타 등)을 막는 잠금이 없어 `reservedOrders` 스냅샷이 서로를 모른 채 같은 번호에 동시 성공할 수 있다 — 이 diff가 만든 새 코드가 아니라 파일의 기존 다단계 쓰기 패턴 전반에 있던 경합이라 이 스토리 범위 밖으로 판단, `deferred-work.md`에 등재했다.
  - 나머지 10건(테스트 인프라의 listing_id 필터 미검증·이론적 병렬-INSERT 경합·문서 서술 정밀도·DW-751 배경설명 부재·red→green 주장의 재현 가능성 등)은 현재 코드 경로에서 실제로 도달 불가능하거나(Never 절이 이미 막음), 스펙 의도와 정확히 일치하거나(예: `PhotoStatus.error` 필터 범위), 이미 위 patch 항목으로 흡수돼(문서 서술은 P1 재작성에 포함) — 각각 독립 판단 후 기각했다.

### 2026-08-10 — Review pass (2회차 후속 리뷰)

- intent_gap: 0
- bad_spec: 0
- patch: 11: (high 1, medium 8, low 2)
- defer: 3: (medium 2, low 1)
- reject: 6
- addressed_findings:
  - `[high]` `[patch]` (adversarial·edge-case-hunter·verification-gap·intent-alignment **4개 레이어 전부 독립 발견**) 1회차 리뷰가 넣은 "자기 옛 번호 우선 재사용" 판단의 경계가 `ownOld <= minOther`였다. `minOther`는 **다른 행이 아직 들고 있는** 최솟값이므로 같을 때 그 값을 그대로 쓰면 살아 있는 중복을 다시 만든다 — 즉 DW-748이 원래 겨냥한 "옛 번호가 이미 중복된 데이터"에서 DW-748이 그대로 재현된다. **실측**(프로브): 기존 행 `row-a=0`·`row-b=0`에서 b의 UPDATE만 실패시키면 a가 다시 `0`을 써서 중복=true. `<`로 고쳐 `-1`이 나가는 것을 확인. `photo_sync_test.dart`에 T1(옛 번호가 이미 중복된 픽스처) 신설 — **오케스트레이터가 직접** `<`를 `<=`로 되돌려 T1만 단독 red가 되는 것을 확인하고 백업본으로 원복 후 green 재확인(`git checkout` 미사용).
  - `[medium]` `[patch]` (adversarial·edge-case-hunter·intent-alignment 독립 발견) 중복 회피 안전망이 `if (initialPhotos.isNotEmpty)`로 **화면이 들고 온 기준선**에 걸려 있었다. 지켜야 할 조건은 "이 호출이 번호를 매기려 하는가"이지 "화면이 기존 사진을 들고 왔는가"가 아니다. **실측**: `initialPhotos`가 빈 채로 DB에 `sort_order=0` 행이 있으면 **GET이 0건** 나가고 새 사진이 `sort_order=0`으로 INSERT돼 살아 있는 행과 겹쳤다(경고도 없다). 도달 경로는 INSERT의 id 회수 실패(`sell_screen.dart:289`가 `rowId != null`로 기준선을 다시 만든다)·`toPhotoItems`의 계약위반 행 스킵. 게이트를 `saved.isNotEmpty`로 바꿔 `-1`이 나가는 것을 확인. T2 신설.
  - `[medium]` `[patch]` (edge-case-hunter·verification-gap) `isReserved`의 "자기 옛 자리를 다시 쓰는 것은 항상 안전하다"는 예외가 무조건이었다 — `reservedOrders[o] >= 2`(같은 옛 번호를 든 행이 이미 둘)면 거짓이다. 자신이 그 값의 유일한 보유자일 때만 예외를 주도록 고쳤다.
  - `[medium]` `[patch]` (adversarial) `'대표 사진이 바뀌었을 수 있어요.'`가 **대표가 이미 확정된 뒤의 실패**에도 무조건 붙었다. 그 경우 실패한 행은 `(sort_order,id)` 최솟값을 이길 수 없어 그 문장이 거짓이고, 거짓 경보가 쌓이면 정작 중요한 그 한 문장을 사용자가 무시하게 된다. `!coverAssigned`일 때만 대표 문구를 붙이도록 고쳤다(두 실패 분기 모두). **실측**: 고치기 전엔 a가 대표로 성공하고 b만 실패해도 대표 경고가 떴고, 고친 뒤엔 순서 실패 안내만 뜬다.
  - `[medium]` `[patch]` (adversarial·verification-gap) 스냅샷 조회 실패 분기와 그 사용자 안내 문구를 실행하는 검사가 **레포 전체에 하나도 없었다**(문구가 소스 파일에만 존재). 이 분기는 안전망이 스스로 꺼지는 유일한 경로라 가장 검사가 필요한 자리다 — T3 신설(GET 500 → 저장은 계속되고 경고가 뜬다).
  - `[medium]` `[patch]` (verification-gap) 스토리의 핵심 주장인 "**첫 후보(화면 맨 앞)가 실패하면 다음 후보가 이어받는다**"를 실행하는 검사가 없었다 — 기존 실패 검사는 전부 중간·마지막 행만 실패시킨다. T4 신설(첫 행 실패 → 다음 행이 실패 행의 잔존 번호보다 작은 값을 받고 대표가 된다).
  - `[medium]` `[patch]` (adversarial) `sort_order` UPDATE가 **200인데 0행**을 돌려주는 형태(RLS가 조용히 아무것도 매치하지 않는 경우 — 세션 만료·저장 중 sold 전환 등 실제로 가장 그럴듯한 운영 실패)를 도는 검사가 없었다. 파일의 유일한 0행 검사는 DELETE용이었다. T5 신설.
  - `[medium]` `[patch]` (adversarial·intent-alignment) 불변식 검사가 **DB 상태가 아니라 나간 요청 + 손으로 적은 상수**(`const bOldOrder = 2;` 등)를 대조하고 있었다 — 픽스처만 고치고 상수를 안 고치면 green인 채로 아무 의미 없는 값을 단언한다(이 프로젝트가 16.10에서 이미 겪은 실패 형태). `_FakeHttpClient`가 **성공한 쓰기만** `existingRows`에 반영하도록 바꾸고, 불변식 단언을 fake의 **최종 행 상태**(중복 없음·의도한 장이 최솟값)로 옮겼다.
  - `[medium]` `[patch]` (verification-gap·intent-alignment 독립 발견) 계약 정본 `docs/conventions.md` §10.1이 앱 코드와 정면으로 어긋난 채 남아 있었다 — *"대표 = `sort_order` 0번"*·*"항상 연속 정수 0..n-1(구멍 금지)"*. 이 스토리가 의도적으로 깬 규칙인데 정본을 안 고쳐, 다음에 이 테이블을 쓰는 사람이 문서를 따라 충돌을 다시 만들 자리였다(project-context 규칙 1: 코드보다 그 문서를 먼저 고친다). 두 줄을 실제 규칙(**유일성**이 계약이고 연속성은 best-effort, 대표는 최솟값)으로 정정하고 `✎` 정정 경위와 web writer 불일치 경고를 문서 기존 방식대로 달았다. 함께 실측한 사실도 적었다 — **실패가 전혀 없어도 사진 순서를 바꿔 저장할 때마다 최솟값이 한 칸씩 내려간다**(6회 재정렬 → `-6`). 기능상 무해하지만(모든 소비처가 정렬만 한다) "0부터 연속"을 가정하는 코드를 새로 쓰지 못하게 문서에 못박았다.
  - `[low]` `[patch]` (adversarial) `releaseReserved(ownOld)`가 **옛 번호를 그대로 다시 쓴 경우에도** 호출돼, 그 행이 여전히 그 값을 들고 있는데 맵은 아무도 안 든다고 기록했다(맵 자신의 문서화된 의미와 모순). 실제로 쓴 값이 `ownOld`와 다를 때만 해제하도록 고쳤다 — 현재는 잠재적 결함이라는 점도 주석에 그대로 적었다(과대주장 금지).
  - `[low]` `[patch]` (adversarial·edge-case-hunter) 실패 분기의 `order += 1` 주석이 *"다음 항목이 이 번호를 다시 쓰지 않는다"* 라고 단언하는데, `!coverAssigned` 동안 `candidateOrder`는 `order`를 아예 보지 않는다 — 이 스토리가 고치려는 바로 그 분기에서 거짓인 주석이었다. 두 곳 모두 사실대로 고쳐 적었다.
  - `[medium]` `defer` (edge-case-hunter·verification-gap 독립 발견) web의 `photo-sync.ts`는 아직 DW-748 이전 알고리즘이고 web 검사가 옛 규칙을 능동적으로 고정하고 있다 — 같은 테이블을 두 writer가 다른 규칙으로 쓴다. 스토리 Never 절("웹을 건드리지 않는다")이 명시적 범위 권한이라 **[[DW-788]]**로 신규 등재.
  - `[medium]` `defer` (edge-case-hunter) 스냅샷 조회가 실패하면 이번 저장이 0부터 번호를 매기는데, 앱이 남긴 음수 행이 그걸 전부 이겨 화면 맨 뒤 사진이 대표가 된다. **실측 재현**(GET 500 + c의 UPDATE 실패 → c=-1이 대표). 처방이 "재정렬을 버릴 것인가 vs 잘못된 확정을 감수할 것인가"의 사용자 판단으로 갈려 이 스토리에서 정하지 않고 **[[DW-789]]**로 신규 등재(안전망 자체가 못 켜진 경우 = 스펙이 이미 인정한 잔여 위험과 같은 갈래).
  - `[low]` `defer` (adversarial) DW-787이 근거로 든 "저장 버튼 연타"는 `sell_screen.dart:457`의 `onPressed: busy ? null : _submit`로 **이미 막혀 있다**(직접 확인). 장부의 기존 항목은 오케스트레이터 소유라 DW-787 자체는 손대지 않고, 다음 트리아지가 항목을 통째로 기각하며 진짜 남은 구멍(다중 세션·재시도 겹침)까지 잃지 않도록 측정만 **[[DW-790]]**로 신규 등재.
  - 나머지 6건(스냅샷 왕복 1회 추가가 Design Notes에 없다 → 이미 있다 · 커밋-후-타임아웃 시 다음 후보가 같은 번호를 받는다 → 프로브로 재현 안 됨, a=0/b=-1로 갈렸다 · `(listing_id, sort_order)` 유니크 인덱스 신설 → 스펙 Never "DB 마이그레이션을 새로 만들지 않는다"가 명시적으로 막음 · `(sort_order,id)`의 id 쪽에 레버가 없다 → 위 high 패치로 동률 자체가 사라져 무의미 · AC 원문 `order=sort_order.asc,id.asc` vs 실제 `nullslast` 표기 → 검사 주석과 DW-751 `resolution:` 양쪽에 이미 사실대로 적혀 있음 · Design Notes 근거가 감사 대상 diff 밖에 있다 → 스펙 파일에 실제로 있고 이번 커밋에 포함)은 각각 독립 판단 후 기각했다.

### 2026-08-10 — Review pass (3회차 후속 리뷰)

- intent_gap: 0
- bad_spec: 0
- patch: 11: (high 0, medium 9, low 2)
- defer: 4: (medium 2, low 2)
- reject: 9
- addressed_findings:
  - `[medium]` `[patch]` (adversarial) **경고가 사용자에게 두 번, 서로 다른 말로 나갔다.** 2회차 P5가 문구를 `coverAssigned` 기준 두 갈래로 쪼개면서, 대표 확정 **전**에 한 행이 실패하고 **후**에 다른 행이 실패하면 두 문장이 모두 `warnings`에 들어간다 — `toSet()`으로 합쳐지지 않고 소비처가 ` · `로 이어 붙인다. **오케스트레이터 실측**(픽스처 a=0·b=1·c=2, a와 c의 PATCH만 400): `사진 순서를 저장하지 못한 항목이 있어요. 대표 사진이 바뀌었을 수 있어요. · 사진 순서를 저장하지 못한 항목이 있어요.` 문자열이 아니라 조건(`orderSaveFailed`·`coverMayHaveChanged`)만 쌓고 루프가 끝난 뒤 문장 하나를 조립하도록 고쳤다. 수정 후 실측: 같은 픽스처에서 경고 1건(대표 언급 포함), 실패가 전혀 없으면 0건(오탐 없음).
  - `[medium]` `[patch]` (adversarial) **2회차 P5의 억제 근거가 스냅샷 실패 시엔 거짓이다.** 주석은 "대표 확정 뒤의 실패 행은 `(sort_order,id)` 최솟값을 절대 이길 수 없다"고 단언했는데, 그 보장은 **예약 스냅샷을 실제로 읽었을 때만** 성립한다. **오케스트레이터 실측**(DB `a=5`·`b=-3`, 스냅샷 GET 500, b의 PATCH 400): 최종 행 `{a:0, b:-3}` — b가 진짜 최솟값이 됐는데 대표 문구가 억제됐다. `snapshotOk` 플래그를 추가해 `!coverAssigned || !snapshotOk`로 조건화했다. 수정 후 실측: 스냅샷 실패 시엔 대표 문구가 뜨고(사실), 스냅샷 정상 + 대표 확정 후 실패에서는 안 뜬다(역시 사실 — 최종 `{a:0, b:1}`).
  - `[medium]` `[patch]` (verification-gap) **새 스냅샷 쿼리의 `listing_id` 필터를 아무도 단언하지 않았다.** 뮤테이션 실측: `.eq('listing_id', listingId)`를 지워도 **515건 전량 green**. 같은 커밋의 형제 검사(`listings_repository_photo_order_test.dart`)는 다른 쿼리에 이 단언을 이미 하고 있었고, project-context 규칙 7이 "경로를 열고 필터를 잊는 것"을 이 규칙의 유일한 실패 모드로 지목한다. T2에 단언 추가.
  - `[medium]` `[patch]` (verification-gap) **예외(`catch`) 분기의 대표 경고를 실행하는 검사가 없었다.** 뮤테이션 실측: 그 분기 문구를 짧은 쪽으로 무너뜨려도 515건 green. 유일한 긍정 단언은 다른 분기(200-0행, T5)에 있었다. 예외 분기가 운영에서 더 흔한 실패 형태다 — T4가 이미 그 픽스처인데 결과를 버리고 있어 단언을 붙였다.
  - `[medium]` `[patch]` (verification-gap) **[[DW-751]]의 절반이 여전히 비어 있었다.** `fetchOwnListingPhotos`의 fake가 모든 응답을 `[]`로 고정해 두 검사 모두 나간 URL만 보고, **반환값을 아무도 단언하지 않는다.** 뮤테이션 실측: 함수 본문을 `return const <PhotoItem>[];`(=`toPhotoItems`를 아예 호출하지 않음)로 바꿔도 515건 green. 수정 화면의 유일한 사진 로더가 아무것도 안 돌려줘도 검사가 통과하는 상태였다 — fake에 `rows`를 넣고 매핑 결과(순서·필드·`getPublicUrl` 경유)를 단언하는 검사를 신설했다.
  - `[medium]` `[patch]` (adversarial) `candidateOrder`의 선행 주석이 *"…예약값보다 **작거나 같다면** 그 값을 그대로 쓴다"*(`<=`)로 남아 있었다 — 코드는 `<`이고, 15줄 아래 P1 주석은 `<=`가 위험하다고 반박한다. **한 함수 안에 같은 분기의 서로 모순되는 설명이 셋**이고, 그중 틀린 쪽이 맨 위에 있었다. 1회차 리뷰가 실제로 주석을 근거로 이 경계를 한 번 되돌린 전례가 있어(그게 2회차의 high 결함이었다) 되풀이를 막으려 규칙 서술을 하나로 합쳤다.
  - `[medium]` `[patch]` (adversarial) **3단계 섹션 머리 주석이 철회된 계약을 그대로 선언하고 있었다** — *"실제로 저장에 성공한 개수로 매긴다 — 실패하면 카운터를 올리지 않아 다음 생존 항목이 그 자리를 메운다(구멍 방지)"*. 이 diff는 두 문장 모두 뒤집었다(실패해도 `order += 1`, 구멍은 의도된 것, 번호는 `candidateOrder`가 정한다). 섹션에서 독자가 **가장 먼저 만나는 문장**이 그 아래 모든 주석과 코드를 부정하던 자리다. 2회차가 하위 주석 2곳만 고치고 헤더를 남겼다.
  - `[medium]` `[patch]` (adversarial·edge-case-hunter 독립 발견) **계약 정본 `docs/conventions.md` §10.1의 자식 불릿이 옛 규칙을 유지했다** — 부모 불릿을 이번 스토리가 "대표 = `sort_order` 최솟값"으로 고쳤는데, 그 바로 아래 자식은 여전히 *"실제로 `sort_order=0`을 받은 행을 기억해 둔다"*·*"대표가 `sort_order≠0`인 행에 붙는다"*고 지시한다. **이 절 자신이 이미 기록한 실패 패턴의 재현이다**(347행: "9.0이 앞 문장만 고치고 이 문장을 안 고쳐서 생긴 일"). 실제 규칙(저장에 성공한 첫 장 = `coverAssigned`)으로 고치고 경위를 `✎`로 남겼다.
  - `[medium]` `[patch]` (verification-gap·adversarial 독립 발견) **§10.1이 이름 붙인 "강제 장치"가 실제로는 그 규칙을 강제하지 않는다.** 유일성을 계약으로 선언하면서 강제 장치로 `photo_sync_test.dart`를 지목했는데, 확인 결과 `0012_listing_images.sql`의 `sort_order`엔 CHECK가 없고 인덱스도 **non-unique**이며 web writer는 아직 옛 규칙이다. 앱 writer 자신의 출력만 보는 단위검사다 — CLAUDE.md B9 관점에서 **라벨을 guard로 위장한 것**이고, 이 문서에 같은 실패가 두 번 기록돼 있다. 사실대로 고쳤다. 함께 drift 서술도 정정했다: 바닥을 정하는 것은 재정렬 횟수가 아니라 **그 매물에 남아 있는 가장 작은 `sort_order`**다(삭제 실패로 박제된 행 하나가 재정렬 없이도 바닥을 끌어내린다).
  - `[low]` `[patch]` (adversarial) `isReserved`의 한 줄 계약 설명이 P3 패치 이후의 본문보다 좁았다 — `o == ownOld`여도 같은 값을 든 행이 둘 이상이면 `true`를 돌려주는데, 주석은 그 경우를 아예 언급하지 않았다(DW-748이 원래 겨냥한 데이터 상태가 그 경우다).
  - `[low]` `[patch]` (adversarial) 두 곳의 낡은 서술: 스펙 `## Design Notes`의 규칙 문장이 아직 `<=`였고(코드는 `<`), `photo_sync.dart` 파일 머리가 이 파일을 web `photo-sync.ts`의 **"미러"**라고 선언한 채였다 — 같은 커밋이 [[DW-788]]로 그 갈라짐을 등재해 놓고 헤더는 그대로였다.
  - `[medium]` `defer` (edge-case-hunter) 스냅샷이 **예외 없이 200+0행**으로 오면 안전망이 경고 한 줄 없이 꺼진다(실측: `warnings=[]`). [[DW-789]]는 예외 경로만 덮는다 — 그 형제로 **[[DW-791]]** 신규 등재.
  - `[medium]` `defer` (adversarial) web 읽는 쪽 `order.ts:7` 주석도 아직 *"sort_order = 0인 행만 true"*다. [[DW-788]]은 web **writer**만 짚어 이 자리가 범위 밖 — 스토리 Never 절이 web을 금지하므로 **[[DW-793]]** 신규 등재.
  - `[low]` `defer` (adversarial) [[DW-790]]이 인용한 `onPressed: busy ? null : _submit`에서 `busy = mine && sell.loading`이고 `mine`은 공유 컨트롤러의 `owner` 확정 여부다 — 결론이 틀렸다기보다 **덜 증명됐다**. 장부 기존 항목은 오케스트레이터 소유라 손대지 않고 측정만 **[[DW-792]]** 신규 등재.
  - `[low]` `defer` (adversarial·edge-case-hunter 독립 발견) 저장 대상이 하나도 없으면 `is_cover` 전체 리셋만 나가고 `true` 지정이 없어 대표 플래그가 0장이 된다(실측). §10.2가 "읽는 쪽은 `is_cover`를 보지 않는다"를 계약으로 못박아 **지금은 관측되지 않는다** — 정확한 게이트를 만들려면 조회를 하나 더 늘려야 해(A2) **[[DW-794]]**로 이월.
  - 나머지 9건은 각각 독립 판단 후 기각했다. 대표적으로 **"테스트가 실제로는 475건이고 스펙의 515는 거짓"**이라는 지적 — 오케스트레이터가 직접 재측정한 결과 **515건이 맞다**. 지적자가 본 `zz_edgeprobe_test.dart`(40건)는 **같은 턴에 병렬로 돌던 옆 리뷰 레이어의 프로브 파일**이었고 지금은 삭제돼 있다(이 프로젝트에 이미 기록된 함정이다). 그 밖에 "일시적 실패로 넘어간 대표가 다음 저장에도 회복되지 않는다"(실측: `a=-2`로 **회복된다**) · 화면 배지 술어 불일치(`undeletable`은 목록 맨 뒤에 합류하므로 배지가 갈리는 경우가 없다) · `review_loop_iteration: 0`(bad_spec 루프백 카운터라 0이 맞다) · `order += 1` 두 줄 삭제(A3 — 주석이 이미 사실대로 적혀 있다) · `nullslast` 리터럴(2회차에서 이미 판단) · `(listing_id, sort_order)` 유니크 인덱스 신설(스펙 Never) · 테스트 하네스의 잔여 한계 · intent-alignment의 서술적 지적(스펙이 이미 인정한 절충).

## Auto Run Result

**요약.** 사진 저장 파이프라인(`syncListingPhotos`)의 부분 실패 경로에서 대표 사진이 조용히 잘못 잡히던 결함 3건(3단계 재편입, DW-748 sort_order 중복, DW-751 검사 공백)을 근본 수정했다. 4-레이어 병렬 리뷰(adversarial·edge-case-hunter·verification-gap·intent-alignment)에서 나온 지적 중 6건을 이번 pass에서 직접 패치했다 — 그중 하나는 이번에 새로 만든 코드 자체의 회귀(정상 재저장 반복 시 대표 sort_order가 계속 음수로 밀려남)였다.

**파일 변경.**
- `app/lib/features/listings/photo_sync.dart` — 3단계 `saved` 선정에서 `PhotoStatus.error` 제외(결함1), `sort_order` 중복 차단용 예약 체계 신설(결함2/DW-748), 부분 실패 경고 문구 보강, 리뷰 패치로 `candidateOrder`가 자기 옛 번호를 우선 재사용하도록 수정, `reservedOrders`를 값→개수 맵으로 전환, 사전조회 실패 시 사용자 경고 추가.
- `app/test/photo_sync_test.dart` — 결함1·결함2 분기 검사 5건 + 불변식 통합 검사 1건 + 리뷰 패치 회귀 검사 2건(반복 재저장 drift 없음, 신규사진+기존사진 혼합 대표 지정) 신설.
- `app/test/listings_repository_photo_order_test.dart`(신규) — DW-751: `toPhotoItems` 원행 단언 5건 + `fetchOwnListingPhotos` 쿼리 URL 단언 2건.
- `_bmad-output/implementation-artifacts/deferred-work.md` — DW-748·DW-751을 `resolution:`과 함께 done 처리(리뷰 패치로 DW-748 중복 status 줄 정리·서술 갱신 포함), 리뷰에서 나온 동시-저장 경합을 DW-787로 신규 등재.
- `_bmad-output/implementation-artifacts/spec-16-11-사진-순서-대표-무결성.md`(이 파일) — `baseline_revision` 갱신, `## Design Notes`에 DW-748 후보 선택 근거 추가(스펙이 요구한 자리), `## Review Triage Log` 신설.

**파일 변경(2회차 후속 리뷰 pass 추가분).**
- `app/lib/features/listings/photo_sync.dart` — `candidateOrder` 경계 `<=`→`<`(high), 안전망 게이트를 `saved.isNotEmpty`로, `isReserved`의 자기-옛번호 예외를 "유일 보유자일 때만"으로, `releaseReserved`를 실제로 값이 바뀐 경우만, 대표 경고 문구를 `!coverAssigned`일 때만, 거짓 주석 2곳 정정.
- `app/test/photo_sync_test.dart` — `_FakeHttpClient`가 **성공한 쓰기만** `existingRows`에 반영하도록 하네스 보강(불변식 단언을 손으로 적은 상수가 아니라 최종 행 상태로 이동), 신규 검사 T1~T5(옛 번호 중복 데이터 · 기준선 빈 채 DB에 행 있음 · 스냅샷 GET 실패 · 첫 후보 실패 시 이어받기 · PATCH 200-0행).
- `docs/conventions.md` — §10.1의 두 계약 문장(*대표 = sort_order 0번* · *항상 연속 정수 0..n-1*)을 실제 규칙으로 정정 + web writer 불일치 경고.
- `_bmad-output/implementation-artifacts/deferred-work.md` — [[DW-788]]·[[DW-789]]·[[DW-790]] **신규 등재만**(기존 항목은 손대지 않음 — 오케스트레이터 소유).

**리뷰 결과 내역.** 1회차 pass — patch 6(high 1·medium 4·low 1) · defer 1(medium) · reject 10. **2회차 후속 pass — patch 11(high 1·medium 8·low 2) · defer 3(medium 2·low 1) · reject 6.** 두 pass 모두 intent_gap 0 · bad_spec 0. 상세는 위 `## Review Triage Log` 참조.

**후속 리뷰 권고.** `followup_review_recommended: true` — 2회차 pass의 patch 중 high 1건(1회차가 넣은 `<=` 경계가 DW-748을 되살렸다)이 있어 자동으로 true. patch 점수: high 1, medium 8, low 2 (3×medium + 1×low = 26, 참고용). ⚠️ **1회차 리뷰가 넣은 패치 자체가 2회차에서 high 결함으로 잡혔다** — 이 코드는 아직 리뷰가 한 번 더 붙을 값을 하는 상태다.

**검증 수행.**
- `flutter analyze`: 0건(구현 직후, 각 패치 후, 최종 총 3회 확인).
- `flutter test`: 전량 통과 — 최종 510건(baseline 496 + 결함1·2·불변식 12건 + 리뷰 패치 회귀 2건).
- red→green 실측(뮤테이션 내용 포함):
  - 결함1(3단계 재편입 차단): `PhotoStatus.error` 필터 제거 → 해당 3개 테스트 red, 원복 → green.
  - 결함2(DW-748 예약 체계 최초 구현): `candidateOrder`를 무조건 `order` 반환으로 무력화 → 재정렬+실패 반례 테스트·통합 검사 red, 원복 → green.
  - DW-751: `fetchOwnListingPhotos`의 `.order('id')` 체이닝 제거 → wire 테스트 red / `toPhotoItems`의 `rowId: id`를 `rowId: null`로 무력화 → 3개 테스트 red, 각각 원복 → green.
  - 리뷰 패치 P1(sort_order 음수 drift 수정): `candidateOrder`의 "자기 옛 번호 우선 재사용" 판단을 제거해 최초(결함) 구현으로 되돌림 → 신규 drift 회귀 검사 1건만 정확히 red, 다른 검사는 영향 없음(원복은 백업 파일로, `git checkout` 미사용) → 원복 후 전량 green 재확인.
- 실기기: 수행하지 않음(스펙 Design Notes에 근거 명시 — 트리거가 전부 부분 실패라 의도적 재현 수단이 없고, 이 세션엔 판매자 로그인용 운영 계정 자격증명이 없다). "실기기에서 확인했다"고 적지 않는다.

**2회차 후속 리뷰 pass 검증(2026-08-10).**
- `flutter analyze`: 0건. `flutter test`: 전량 통과 — **515건**(2회차 시작 시점 510 + 신규 T1~T5 5건).
- **오케스트레이터가 직접 돌린 프로브 실측**(서브에이전트 보고를 그대로 받지 않고 재측정 — CLAUDE.md B4). 프로브 파일은 매번 삭제했고 원복은 백업본으로만 했다(`git checkout` 미사용).

  | 항목 | 수정 전(실측) | 수정 후(실측) |
  |---|---|---|
  | 옛 번호가 이미 중복된 데이터 | `row-a`가 다시 `0` → 중복 재생산 | `row-a=-1`, `row-b=0` → 중복 없음, 맨 앞이 최솟값 |
  | 기준선이 빈 채 DB에 행이 있음 | GET **0건**, INSERT `sort_order=0` → 충돌 | GET 1건, INSERT `sort_order=-1` → 충돌 없음 |
  | 대표 확정 후 다른 행만 실패 | 대표 경고가 거짓으로 붙음 | 순서 실패 안내만 |
  | 순서 바꿔 반복 저장(실패 없음) | 최솟값 `-1…-6` | 동일(의도된 잔여 — 문서에 못박음) |
  | 스냅샷 GET 실패 + 잔존 `-1` 행 | 맨 뒤 사진이 대표 | 동일(→ [[DW-789]]로 이월) |

- **신규 검사 red→green 실측**(뮤테이션 내용 포함). T1은 **오케스트레이터가 직접** `<`를 `<=`로 되돌려(= 1회차 구현으로 회귀) `photo_sync_test.dart` 36건 중 **T1만 단독 red**(`Expected: a value less than <0> / Actual: <0>`)임을 확인하고 백업본으로 원복 → 515건 green 재확인. T2~T5는 구현 서브에이전트가 각각 다른 형태로 뮤테이션해(T2=게이트를 `saved.any((p) => p.rowId != null)`로 좁힘 · T3=catch에서 조기 return · T4/T5=각 실패 분기에 `coverAssigned = true` 오삽입) 해당 검사만 red가 되는 것을 확인.

**잔여 위험.**
- 대표 후보 자신의 UPDATE가 계속 실패하는 복합 실패는 이 장치로도 구하지 못한다(다음 성공 항목이 대표를 이어받되, 그 항목마저 실패하면 재시도) — 드문 경우이고 강화된 warning으로 사용자에게 알린다.
- 같은 매물에 대한 동시 저장 요청을 막는 잠금이 없다 — 이 스토리 이전부터 있던 경합이라 DW-787로 defer했다. ✎ 2회차 리뷰 정정: 그 항목이 근거로 든 "저장 버튼 연타"는 실제로는 이미 막혀 있고(`sell_screen.dart:457`), 남는 위험은 다중 세션·재시도 겹침이다([[DW-790]]에 측정 기록).
- **`sort_order`는 이제 "0부터 연속"이 아니다.** 실패가 전혀 없어도 사진 순서를 바꿔 저장할 때마다 대표가 받는 값이 한 칸씩 더 내려간다(2회차 리뷰 실측: 6회 재정렬 → 최솟값 `-6`). 모든 소비처가 정렬만 하므로 기능상 문제는 없다. 계약 정본 `docs/conventions.md` §10.1을 이번 pass에서 실제 규칙(유일성이 계약, 연속성은 best-effort, 대표는 최솟값)으로 정정했다 — 다음에 이 컬럼을 쓰는 사람이 문서를 따라 충돌을 다시 만들지 않게 하려는 것이다.
- **web은 아직 옛 알고리즘이다**([[DW-788]]) — 스토리 Never 절이 web을 범위에서 뺐다. 같은 `listing_images`를 두 writer가 다른 규칙으로 쓰는 상태이고, 특히 앱이 남긴 음수 행이 있는 매물을 web에서 저장하면 그 행이 대표를 가져간다.
- **스냅샷 조회 자체가 실패하면 안전망이 꺼지고, 잔존 음수 행이 대표를 가로챈다**([[DW-789]], 실측 재현). 사용자에게 경고 2건은 실제로 뜬다. 처방이 "재정렬을 버릴 것인가 vs 잘못된 확정을 감수할 것인가"의 판단이라 이 스토리에서 정하지 않았다.
- 실기기 확인은 이번 pass에서도 하지 않았다(사유는 위 Design Notes와 동일 — 트리거가 전부 부분 실패라 의도적 재현 수단이 없고, 판매자 로그인용 자격증명이 이 세션에 없다). **"실기기에서 확인했다"고 적지 않는다.**

---

## 3회차 후속 리뷰 pass (2026-08-10)

**요약.** 4-레이어 병렬 리뷰(adversarial·edge-case-hunter·verification-gap·intent-alignment)를 다시 돌렸다. **이번 pass에는 high가 없다** — 1·2회차와 달리 "대표를 잘못 확정하는" 데이터 결함은 더 나오지 않았고, 나온 것은 (a) 2회차 패치가 만든 사용자 안내 회귀 2건, (b) 새로 연 경로들의 **검사 공백** 3건(전부 뮤테이션으로 실측), (c) 문서·주석이 코드와 어긋난 자리 5건이다. patch 11건을 이번 pass에서 직접 고쳤다.

**파일 변경(3회차 pass).**
- `app/lib/features/listings/photo_sync.dart` — 부분 실패 경고를 **문자열 누적이 아니라 조건 누적 후 1회 조립**으로 전환(P1), 대표 경고 억제를 `snapshotOk`로 게이팅(P2), `candidateOrder`·3단계 헤더·`isReserved`·파일 헤더의 낡거나 모순된 주석 4곳 정정(P6·P7·P10·P11b).
- `app/test/photo_sync_test.dart` — P1(대표 확정 전후 양쪽 실패 → 경고 1건)·P2(스냅샷 실패 + 대표 확정 후 실패 → 대표 문구가 뜬다) 검사 신설, T2에 스냅샷 쿼리의 `listing_id` 단언 추가(P3), T4가 결과를 받아 경고를 단언하도록 보강(P4).
- `app/test/listings_repository_photo_order_test.dart` — `_UrlCapturingHttpClient`에 응답 행을 실을 수 있게 하고 `fetchOwnListingPhotos`의 **반환값**(순서·필드·`getPublicUrl` 경유)을 단언하는 검사 신설(P5, DW-751의 남은 절반).
- `docs/conventions.md` — §10.1 자식 불릿을 실제 규칙으로 정정(P8), "강제 장치" 과대주장 정정 + drift 서술 정밀화(P9). 둘 다 문서 기존 방식대로 `✎` 경위를 달았다.
- `_bmad-output/implementation-artifacts/deferred-work.md` — [[DW-791]]·[[DW-792]]·[[DW-793]]·[[DW-794]] **신규 등재만**(diff 통계 `44 insertions, 0 deletions`로 기존 항목 무수정 증명 — 오케스트레이터 소유).
- 이 스펙 파일 — `## Design Notes`의 `<=`를 `<`로 정정(P11a), `## Review Triage Log`에 3회차 항목 추가.

**리뷰 결과 내역.** 3회차 pass — patch 11(high 0·medium 9·low 2) · defer 4(medium 2·low 2) · reject 9. intent_gap 0 · bad_spec 0.

**후속 리뷰 권고.** `followup_review_recommended: true` — patch 점수 `3×9 + 1×2 = 29`(임계 5 이상). ⚠️ 다만 **성격이 1·2회차와 다르다**: high가 0이고 데이터 무결성 결함은 나오지 않았다. 이번 patch의 무게중심은 "이 스토리가 새로 연 경로에 검사가 없다"와 "문서·주석이 코드를 따라오지 못했다"이며, 그 셋은 이번 pass에서 실제 뮤테이션으로 red→green을 증명해 닫았다.

**검증 수행(전부 오케스트레이터가 직접 실행·관찰 — 서브에이전트 보고를 그대로 받지 않았다, CLAUDE.md B4).**
- `flutter analyze`: 0건. `flutter test`: 전량 통과 — **518건**(3회차 시작 시점 515 + 신규 3건).
- ⚠️ **첫 조치가 카운트 재측정이었다.** 한 리뷰 레이어가 "커밋된 트리는 515가 아니라 475"라고 보고했는데, `git status`로 확인하니 워킹트리는 깨끗했고 직접 돌린 결과 **515건**이었다. 그 레이어가 본 40건짜리 파일은 **같은 턴에 병렬로 돌던 옆 레이어의 프로브**였다.

  | 실측 항목 | 수정 전 | 수정 후 |
  |---|---|---|
  | 대표 확정 전·후 양쪽에서 실패 | 경고 2건이 ` · `로 이어붙음(한쪽만 대표 언급) | 경고 1건, 대표 언급 포함 |
  | 실패가 전혀 없는 저장 | (해당 없음) | 경고 0건 — 오탐 없음 |
  | 스냅샷 GET 500 + 대표 확정 후 실패 | 대표 문구 억제됨(최종 `{a:0, b:-3}` — 실제로는 대표가 넘어감) | 대표 문구가 뜬다 |
  | 스냅샷 정상 + 대표 확정 후 실패 | 대표 문구 억제(옳음) | 동일 — 최종 `{a:0, b:1}`이라 억제가 사실에 맞는다 |
  | 일시적 실패로 넘어간 대표 | pass1 `{a:0, b:-1}`(대표 b) | pass2에서 `{a:-2, b:-1}`로 **회복**(리뷰 주장과 달리 영구적이지 않다) |

- **검사 공백 3건은 뮤테이션으로 먼저 증명했다** — `listing_id` 필터 삭제 + `catch` 분기 경고 무력화 + `fetchOwnListingPhotos`가 `const []` 반환, **셋을 동시에 넣고 515건 전량 green**. 그래서 셋 다 실재하는 공백임이 확정됐고, 그 뒤에 검사를 만들었다.
- **신규 검사 red→green은 오케스트레이터가 구현 서브에이전트와 다른 형태로 다시 깨서 확인했다**(자기가 쓴 표기로만 깨면 그 검사가 무엇을 보는지 증명되지 않는다):
  - P3 — 서브에이전트는 필터 **값**을 `'x'`로 바꿨고, 오케스트레이터는 필터 **줄을 통째로 삭제**했다 → T2만 단독 red(`Expected: 'eq.l1' / Actual: <null>`).
  - P4 — 서브에이전트는 `if (false)`로 감쌌고, 오케스트레이터는 `catch` 분기 조건을 `||`에서 `&&`로 바꿨다 → T4·P1·P2 검사 3건 red(각각 그 분기에 실제로 의존함을 확인).
  - P5 — 서브에이전트는 `getPublicUrl`을 우회했고, 오케스트레이터는 **행 순서를 뒤집었다**(`rows.reversed`) → 신규 매핑 검사만 단독 red.
  - 원복은 전부 백업본 `cp`로 했다(`git checkout` 미사용). 원복 후 `analyze` 0 · `flutter test` 518건 green 재확인.
- 실기기: 이번에도 수행하지 않음(사유 동일 — 트리거가 전부 부분 실패라 의도적 재현 수단이 없고, 판매자 로그인 자격증명이 이 세션에 없다). **"실기기에서 확인했다"고 적지 않는다.**

**3회차 이후 잔여 위험(위 1·2회차 목록에 더해).**
- **안전망이 스스로 꺼지는 경로가 둘로 늘었다** — 예외([[DW-789]])와 무예외 0행([[DW-791]]). 후자는 경고조차 없다(도달성은 낮다 — 같은 RLS면 UPDATE도 막혀 다른 경고가 대신 뜬다).
- **`sort_order` 유일성을 실제로 막는 장치는 여전히 없다** — DB 제약 없음(non-unique 인덱스), web writer는 옛 규칙. 이번 pass는 그 사실을 계약 문서에 정확히 적었을 뿐이고, 강제는 유니크 인덱스(스펙 Never가 금지) 또는 [[DW-788]] 이식이 필요하다.
- **`is_cover`가 0장으로 남는 경로**([[DW-794]]) — 지금은 읽는 소비처가 없어 관측되지 않지만, §10.1의 "파생 결과로 기록한다"와는 어긋난 상태다.
