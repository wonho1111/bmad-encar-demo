---
baseline_commit: dba989fce6c8e4aa1be220f91bbaf48dae0df37e
---

# Story 9.3: 사진 업로더 (등록/수정)

Status: review

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a 판매자,
I want 매물 등록·수정 시 사진을 여러 장 올리고 대표를 지정하길,
so that 구매자가 내 차를 사진으로 먼저 본다.

**범위: web(`web/src/app/(user)/sell/`)만.** Flutter 앱 업로더는 이 스토리 밖이다 — 근거는 [Dev Notes §0 범위 경계](#0-범위-경계-먼저-읽을-것).

## Acceptance Criteria

원문 출처: `_bmad-output/planning-artifacts/epics-increment-2026-07-12.md:489-506` (Story 9.3). AC1~AC7이 원문, AC8~AC10은 9.1·9.2 코드리뷰 이월 부채가 이 스토리를 **트리거로 지목**해서 추가된 것(`docs/tech-debt.md` #43·#47·#49·#51).

1. **업로드 기본** — 매물 등록/수정 폼에서 드롭존 + "N/10" 카운터로 **최대 10장 · 장당 원본 5MB**까지 업로드된다. 순서 변경이 되고, **대표 = 항상 순서상 첫 번째 사진**이다. (FR26, UX-DR13)
   - **대표는 별도 상태가 아니라 "순서 1번"의 다른 이름이다** (사용자 확정 2026-07-18). 대표 전용 토글 버튼을 만들지 않는다 — 순서와 대표가 각각 움직이면 진실이 두 군데 생기고 어긋난다.
   - 대표를 바꾸는 방법은 **하나의 메커니즘(순서 이동)에 진입점 2개**: ① 썸네일을 첫 칸으로 드래그 ② 썸네일의 [대표로] 버튼 — **②는 그 사진을 0번 위치로 옮기는 것과 정확히 동일한 동작**이다(별도 필드를 건드리지 않는다).
   - 첫 칸 썸네일에만 "대표" 배지를 표시한다. DB의 `is_cover`는 이 규칙의 **파생 결과**로 기록한다(`sort_order = 0`인 행만 `true`).
2. **사진은 선택** — 사진 0장이어도 등록·수정이 정상 완료된다. 폼에 안내 문구를 그대로 노출한다: **"사진은 선택이에요. 없어도 등록되지만, 있으면 문의가 훨씬 잘 와요."**
3. **실패는 폼을 막지 않는다** — 업로드 실패·용량초과·미지원 포맷은 **해당 사진 항목의 인라인 오류 + 재시도 버튼**으로 처리하고, **폼 제출 자체는 차단하지 않는다**. 대표사진을 삭제하면 **다음 순서 사진이 자동으로 대표 승격**한다.
4. **업로드 전 클라이언트 리사이즈/압축** — 원본 5MB 상한과 별개로, 저장본을 카드/갤러리에 적합한 크기·품질로 **다운스케일해서 올린다**. (앱은 서명 **원본**을 받으므로(ADR-IMG-01) 목록에서 수 MB 다중 다운로드가 나면 NFR7 위반) 저장본은 **긴 변 ≤1600px · WebP · quality 0.82**로 고정한다 — 근거·구현법은 Dev Notes §4.
5. **경로 규약** — 저장 경로는 9.1 규칙 **`{user_id}/{listing_id}/{filename}`** 을 따른다. **신규 등록은 `listings` insert로 `listing_id`를 받은 뒤** 업로드한다(스테이징 경로 없음).
6. **처리중·실패는 로컬 상태** — 업로드 진행/실패는 업로더 본인 세션의 화면 상태일 뿐이고, **wire(DB·JSON)에는 `null` 또는 확정된 값만** 나간다(I8). `listing_images` 행은 **업로드가 실제 성공한 사진에 대해서만** 만든다.
7. **이탈 가드** — 변경(특히 업로드한 사진)이 있는 채 이탈하려 하면 확인 다이얼로그가 뜬다: **"저장하지 않고 나가시겠어요? 작성한 내용이 사라져요."** + [계속 작성]/[나가기]. **변경이 없으면 경고 없이 이탈**한다. (UX-DR23)
8. **대표 기록은 2문장으로** — 0번 사진이 바뀌어 `is_cover`를 옮길 때 `is_cover`를 **단일 UPDATE로 뒤집지 않는다**(부분 유니크 인덱스에 걸려 `duplicate key`로 죽는다 — 도커 실측, #47-1). **① 해당 매물 전체 `is_cover=false` → ② 대상 1장만 `true`** 순서로 실행하고, 이 순서가 필요한 이유를 코드 주석에 남긴다.
9. **10장 상한을 클라이언트가 먼저 막는다** — DB 트리거는 **INSERT만** 보고 동시 삽입 경합에서 새는 것이 실측돼 있다(#43·#49). 따라서 (a) UI가 10장 초과 선택을 애초에 막고, (b) **행 INSERT는 순차(직렬)로** 보내 병렬 경합을 만들지 않는다. ⚠️ 이건 **UX 층의 1차 차단**이지 강제가 아니다 — 강제는 계약대로 서버(트리거·버킷 설정)에 남는다(`docs/conventions.md:193` "클라 검증은 우회 가능하다"). 클라가 막는다고 서버 검증을 빼지 말 것.
10. **삭제가 실제로 지워지는지 실측** — 사진 삭제는 **`listing_images` 행 + Storage 오브젝트 둘 다** 제거해야 한다. 그런데 **인증 사용자의 Storage DELETE/LIST가 RLS·GRANT가 맞는데도 실패하는 현상(#51)이 열려 있다.** 착수 시 **원격에서 먼저 재현 여부를 측정**하고(Task 0), 결과에 따라 경로를 정한 뒤 구현한다. **측정 없이 "된다/안 된다"를 코드나 문서에 적지 않는다.**

## Tasks / Subtasks

- [x] **Task 0 — 착수 전 실측: Storage 삭제가 되는가 (#51) (AC: 10)** ⚠️ **다른 작업보다 먼저**
  - [x] 원격 Supabase에 테스트 계정으로 로그인해 본인 소유 오브젝트 1개 업로드 → **authenticated JWT로 단건 DELETE**·**LIST** 호출 → 응답 코드와 실제 삭제 여부를 **관찰해서 기록**
  - [x] ~~재현되면: `security definer` RPC로 감싸 삭제~~ → **RPC 불필요.** 재현은 됐으나 **원인을 규명했고 그 원인이 순서 문제**였다(Debug Log 1). `0014` 마이그레이션·`service_role` 둘 다 필요 없음
  - [x] 관찰한 응답 원문을 `docs/tech-debt.md` #51에 기록하고 **원인 규명 + 해소 규칙(순서)** 로 항목을 닫음
  - [x] 결과를 Debug Log에 "관찰"로 남김

- [x] **Task 1 — 순수 유틸 3종 + Vitest (AC: 1, 3, 4, 8)**
  - [x] `web/src/lib/images/validate.ts` — `validateImageFile(file)`: MIME 3종(`image/jpeg`·`image/png`·`image/webp`) 화이트리스트 + 원본 5MB 상한. 반환은 throw가 아니라 `{ ok: true } | { ok: false, reason: '...' }`(한국어 사유)
  - [x] `web/src/lib/images/resize.ts` — `resizeImage(file)`: 긴 변 ≤1600px, WebP, quality 0.82. **canvas 기반 브라우저 네이티브 구현**(새 의존성 추가 금지 — Dev Notes §4)
  - [x] `web/src/lib/images/order.ts` — `reorder(items, from, to)` / `moveToFront(items, index)` / `remove(items, index)`: 사진 순서 배열을 **DOM·Supabase 없이** 계산하는 순수 함수. **대표를 따로 계산하는 함수는 만들지 않는다** — 대표 = 배열 0번이라 순서 계산이 곧 대표 계산이다(AC1)
  - [x] Vitest: `validate.test.ts`(경계 — 5MB 정확히, 5MB+1byte, `image/gif` 거부) · `order.test.ts`(0번 삭제 시 다음 장이 0번이 됨 = 자동 대표 승격, 마지막 1장 삭제 시 빈 배열, `moveToFront`가 나머지 순서를 보존)
  - [x] 검증: `npm test` 통과. **각 테스트를 한 번씩 일부러 깨서 red 확인 후 되돌린다**(통과가 의미를 갖게)

- [x] **Task 2 — 업로더 컴포넌트 (AC: 1, 2, 3, 6)**
  - [x] `web/src/app/(user)/sell/PhotoUploader.tsx` (클라이언트 컴포넌트) 신규
  - [x] 드롭존(드래그&드롭 + 클릭 파일선택) · 썸네일 그리드 · "N/10" 카운터 · **첫 칸에만 "대표" 배지** · 개별 삭제(✕) · 드래그 순서 변경 · 각 썸네일 [대표로] 버튼(= 0번으로 이동, 첫 칸에는 표시 안 함)
  - [x] 안내 문구 2종을 그대로: 드롭존 보조문 **"JPG · PNG · WebP, 장당 최대 5MB"**, 하단 힌트 **"사진은 선택이에요. 없어도 등록되지만, 있으면 문의가 훨씬 잘 와요."**
  - [x] 상태: 대기/업로드중(스피너)/성공/실패(인라인 오류 + [재시도]). 실패한 항목은 목록에 남되 **제출을 막지 않는다**
  - [x] 순서 변경은 HTML5 native drag&drop으로 — **`dnd-kit` 등 새 라이브러리 추가 금지**(Dev Notes §4)
  - [x] 반응형: 썸네일 그리드는 **열 수로만 흡수**(데스크톱 4열 → 모바일 3열). 썸네일 내부 가로 배치를 세로로 접지 않는다(D5)
  - [x] 접근성: 드롭존은 키보드로 도달·활성화 가능, 각 썸네일 삭제 버튼에 `aria-label`("N번째 사진 삭제")

- [x] **Task 3 — 등록 흐름 배선 (AC: 1, 5, 6, 9)**
  - [x] `SellForm.tsx` 등록 insert를 **`.insert({...}).select('id').single()`** 로 바꿔 `listing_id`를 받는다(지금은 `.select()`가 없어 id를 못 받는다 — `SellForm.tsx:270-272`)
  - [x] 매물 insert 성공 → 각 사진을 `{user.id}/{listingId}/{uuid}.webp` 로 업로드 → 성공한 것만 `listing_images` 행 INSERT(**순차**, AC9)
  - [x] 첫 사진(sort_order=0)에 `is_cover=true`
  - [x] 사진 업로드가 전부 실패해도 **매물 등록 자체는 성공 처리**하고, "매물은 등록됐고 사진만 실패했다"는 것이 사용자에게 분명히 보이게 한다(AC3)

- [x] **Task 4 — 수정 흐름 배선 (AC: 1, 3, 5, 8, 10)**
  - [x] `/sell/[id]/edit` 진입 시 기존 `listing_images`를 `sort_order` 순으로 읽어 썸네일로 표시한다. **표시용 URL은 9.2 헬퍼 `getSignedUrl('listing-images', storage_path)` 로 서버측에서 발급해 내려준다** — 브라우저에서 서명하지 않는다(`web/src/lib/storage/index.ts`는 서버 전용)
  - [x] 추가 업로드 · 삭제 · 순서 변경 반영. 순서가 바뀌어 **0번 사진이 달라지면** `is_cover`를 **AC8의 2문장 순서**로 다시 기록
  - [x] 삭제는 **행 + 오브젝트 둘 다** (Task 0에서 정한 경로로)
  - [x] `sort_order`는 화면 순서와 **연속 정수(0..n-1)**로 다시 매긴다(구멍 남기지 않기 — #47-2 tie-break 미정의 회피)

- [x] **Task 5 — 폼 이탈 가드 (AC: 7)**
  - [x] dirty 감지: 초기값 대비 폼 필드 변경 **또는** 사진 추가/삭제/순서변경이 있으면 dirty
  - [x] `beforeunload`(새로고침·탭 닫기) + 폼 자체의 [취소]·[목록으로] 조작에 확인 다이얼로그
  - [x] 다이얼로그는 기존 `FocusTrap`(`web/src/components/ui/FocusTrap.tsx`)을 **재사용**한다 — 새 모달 프리미티브를 만들지 않는다. 이 컴포넌트의 첫 실사용처다
  - [x] ⚠️ Next.js App Router는 **`<Link>` 내부 이동을 가로채는 공식 API가 없다.** 그 경로는 이번에 못 막는다 — **막았다고 적지 말고**, 무엇이 안 막히는지 `docs/tech-debt.md`에 새 항목으로 등재한다

- [x] **Task 6 — 실제로 돌려서 확인 (B4)**
  - [x] `npm run lint` · `npm test` · `npm run build` 전부 초록
  - [x] dev 서버 + 실제 브라우저(Playwright MCP)로 **판매자 계정 E2E 관찰**: 등록(사진 3장 → 대표 확인) · 사진 0장 등록 · 5MB 초과 파일 거부 인라인 오류 · 대표 삭제 시 승격 · 수정 화면 재진입 시 사진 유지 · dirty 상태 이탈 경고
  - [x] **저장본이 실제로 작아졌는지 측정**: 업로드한 원본 바이트 vs Storage 오브젝트 바이트를 비교해 기록(AC4가 "했다"가 아니라 "줄었다"로 증명되게)
  - [x] Storage와 DB를 **직접 조회**해 경로·행 개수·`is_cover` 1건을 확인한다(화면만 보고 판단하지 않는다)

- [x] **Task 7 — 계약·대장 갱신 (Epic 8 회고 A1·A5 이행)**
  - [x] `docs/conventions.md` §10에 이번에 확정된 사실 추가: 저장본 규격(1600px·WebP·0.82) · 대표 교체 2문장 규칙 · 클라 순차 INSERT
  - [x] `docs/tech-debt.md`: #51 판정(닫힘/이월+근거) · #43·#47·#49 중 이번에 **다룬 것과 안 다룬 것을 명시** · Link 이동 미차단 신규 등재
  - [x] **`deferred-work.md`에 쓰지 않는다** — 그 파일은 동결됐고 열린 일 장부는 `docs/tech-debt.md` 하나다
  - [x] 이 스토리 산출물에 **실측 없이 적힌 주장이 0건**인지 스스로 훑는다(회고 A5)

### Review Findings

코드리뷰 2026-07-19 (새 세션, opus). 4개 레이어 병렬: Blind Hunter(diff만) · Edge Case Hunter(diff+코드) · Acceptance Auditor(diff+스펙+계약문서) · Claim Verifier(개발 에이전트가 Discord에 보고한 주장 검증 — 사용자 요청분).
**상위 결함 6건은 3~4개 레이어가 서로 모른 채 동일 지목** — 오탐 가능성 낮음.

#### 결정 필요 (decision-needed) — **2026-07-19 사용자 결정 완료, 둘 다 (a) 채택 → patch로 전환**

- [x] **[Review][Decision→Patch] 등록 부분 실패 후 재제출 → 매물이 하나 더 생긴다** — **결정: (a) 등록 성공 직후 edit 모드로 전환해 create 재실행을 봉쇄.** 근거: 매물 INSERT는 이미 성공했으므로 그 시점부터는 "수정"이 사실에 맞고, 사진 재시도도 `syncListingPhotos`의 기존 edit 경로를 그대로 타 분기가 늘지 않는다. (b)는 사용자가 실패한 사진 목록을 잃고, (c)는 재시도 자체를 못 하게 만든다.
- [x] **[Review][Decision→Patch] 검증 실패 항목을 목록에 남기는 설계의 파생 결함 3종** — **결정: (a) error 항목을 정원·대표·`failedCount` 계산에서 모두 제외.** 근거: 목록에 남겨 "어느 파일이 왜 안 됐는지 보여준다"는 원래 의도는 유지하면서, **저장될 사진에만 의미가 있는 세 계산**에서만 빼면 된다. (b)는 어느 파일이 거부됐는지 사라져 원래 의도를 파괴하고, (c)는 이동 차단이 그대로 남는다.

<details><summary>원래 제기된 형태(기록 보존)</summary>

- [ ] **[Review][Decision] 등록 부분 실패 후 재제출 → 매물이 하나 더 생긴다** — `SellForm.tsx:330-338`이 `failedCount > 0`이면 폼·모드·`listingId`를 그대로 둔 채 `return`한다. 화면엔 [재시도] 버튼이 있고 유일한 제출 버튼은 [매물 등록]이라, 사용자가 재시도→제출하면 `listings` INSERT가 다시 돌아 **같은 차가 2건 등록**된다. 게다가 살아남은 사진은 옛 `listingId` 경로의 `storagePath`를 들고 새 매물 id로 행 INSERT를 시도해 `0013` 경로 정합성 트리거에 거부되고, 그 실패 분기가 **이전 매물의 살아 있는 오브젝트를 삭제**한다. 안내 문구("매물 수정 화면에서 다시 시도")와 화면에 실제 있는 [재시도] 버튼이 서로 다른 것을 지시한다. 선택지: (a) 성공 직후 edit 모드로 전환해 create 재실행을 봉쇄 (b) 수정 화면으로 리디렉트 (c) 실패 시 사진 항목을 버리고 폼만 초기화. 4개 레이어 전원 지목.
- [ ] **[Review][Decision] 검증 실패 항목을 목록에 남기는 설계의 파생 결함 3종** — 용량초과·포맷거부 항목이 `items`에 남는 것은 의도된 설계지만, 세 계산이 그것을 저장될 사진과 구분하지 않는다. ① **10장 정원 잠식** — `PhotoUploader.tsx:50-57`의 `count`/`room`이 error 항목까지 세어, 거부된 파일 10장이면 유효한 사진을 한 장도 못 넣는다(AC1 "최대 10장"과 어긋남). ② **대표 배지 실종 + DB 불일치** — `PhotoUploader.tsx:183`이 `i===0 && status!=='error'`라 0번이 실패 항목이면 배지가 어디에도 없고 [대표로]도 없는데(`:208`), 저장 시 대표는 `photo-sync.ts:128-133`의 `survivors[0]`(첫 성공 항목)에 붙어 **화면과 DB가 갈린다** — 코드 주석이 피하겠다고 명시한 바로 그 상태. ③ **edit 저장 후 이동 영구 차단** — `photo-sync.ts:61-64`가 재시도 불가 항목도 매번 `failedCount`에 세고 `SellForm.tsx:300-305`가 그러면 `return`하므로, 고칠 수 없는 항목 1개가 남는 한 저장은 되지만 `/sell`로 영영 못 간다(탈출구는 ✕로 직접 지우는 것뿐인데 안내가 없다). 선택지: (a) error 항목을 정원·대표·failedCount 계산에서 모두 제외 (b) 검증 실패 파일은 목록에 남기지 않고 토스트로만 알림 (c) 현행 유지하고 안내 문구만 보강.

</details>

#### 수정 필요 (patch)

- [x] **[Review][Patch] 🔴 INSERT 성공 후 `rowId`를 상태에 되돌려주지 않아 재제출이 저장된 사진을 파괴한다** [`web/src/app/(user)/sell/photo-sync.ts:98-117`] — `.select('id')`로 id를 받아놓고 `savedCount += 1`만 하고 버린다(`rowId` 대입은 전 코드베이스에서 `photo-item.ts:48` 서버 로드 경로 하나뿐 — grep 실측). 그 항목은 `storagePath`는 있고 `rowId`는 없는 채 화면에 남고, 재제출 시 `:91`의 `if (p.rowId)`가 거짓이라 **같은 `storage_path`로 INSERT를 재시도** → `0012`의 `storage_path ... unique` 위반 → 실패 분기 `:109`가 **이미 정상 저장된 Storage 오브젝트를 삭제**한다(이때는 행이 있어 읽기 정책을 통과하므로 삭제가 실제로 성공). 결과: DB 행은 남고 파일만 사라진 역(逆)고아 = 사용자가 저장했던 사진이 조용히 소실. 스토리·커밋·tech-debt 어디에도 없던 미신고 결함.
- [x] **[Review][Patch] 🔴 오브젝트 삭제 실패를 무시하고 행을 지운다 — 이 커밋이 세운 계약을 이 커밋이 어긴다** [`web/src/app/(user)/sell/photo-sync.ts:44-46`] — `deleteListingImageObject`는 실패·매치0건을 `false`로 돌려주도록 **일부러** 설계됐고(`upload.ts:59-70`, "에러 없음을 성공으로 갈음하지 않는다") 계약 주석까지 붙었는데, 유일한 호출부가 반환값을 `await`만 하고 버린다. 오브젝트 삭제가 403/0건으로 실패해도 다음 줄이 행을 지우고, 그 순간 그 객체는 읽기 정책(행 조인 의존) 때문에 소유자에게도 안 보이는 **영구 고아**가 된다 — `conventions.md` §10.1이 "되돌릴 수 없다"고 못 박은 바로 그 상태. Task 0의 #51 규명이 막으려던 결과가 에러 경로에서 그대로 재현된다. 4개 레이어 전원 지목.
- [x] **[Review][Patch] INSERT 실패 시 `sort_order`에 구멍이 생기고 `is_cover`가 0번 아닌 행에 붙는다** [`web/src/app/(user)/sell/photo-sync.ts:86-133`] — `saved`는 루프 **전에** 고정돼 인덱스 `i`가 그대로 `sort_order`가 되는데, 중간 항목의 INSERT가 실패해도 뒤 항목의 `i`는 재계산되지 않는다. `saved[0]`이 실패하면 DB에 `sort_order=0` 행이 없고, 4단계는 루프 **후에** 재계산한 `survivors[0]`(= `sort_order=1`)에 `is_cover=true`를 건다 → 이 스토리의 핵심 불변식 **"대표 = 순서 0번"이 wire에서 깨진다**. `conventions.md` §10.1의 "연속 정수 0..n-1(구멍 금지)"도 위반. 4개 레이어 전원 지목.
- [x] **[Review][Patch] 서버·클라이언트가 각자 다른 `seq`를 써서 React key가 충돌한다** [`web/src/app/(user)/sell/photo-item.ts:36-37`] — `let seq = 0`은 **모듈 인스턴스별**이다. `toPhotoItems`는 서버 컴포넌트(`[id]/edit/page.tsx`)에서, `toLocalPhotoItem`은 브라우저(`PhotoUploader.tsx`)에서 호출돼 카운터가 각각 0부터 센다. 기존 사진 2장(`photo-1`,`photo-2`)에 새 파일을 추가하면 그 key도 `photo-1` → 같은 `<ul>`에 **중복 key**(`:157`). 순서 변경·삭제 시 미리보기·상태가 엉뚱한 칸에 붙고, `SellForm.tsx:149-151`의 `photosDirty`가 key 비교라 **이탈 경고까지 오탐**한다(AC7 false negative). 3개 레이어 지목.
- [x] **[Review][Patch] 조용한 실패 4곳 — `failedCount`에 안 잡혀 사용자에게 "성공"으로 보인다** [`photo-sync.ts:46, 94, 126, 134`] — 행 삭제 실패·`sort_order` 갱신 실패·`is_cover` 초기화 실패·대표 지정 실패가 전부 `console.error`만 하고 `PhotoSyncResult`에 실리지 않는다. 특히 `is_cover` 2문장은 트랜잭션이 아니라서 ①만 성공하고 ②가 실패하면 그 매물은 **대표 0장**이 되는데 사용자는 성공 안내를 받고 `/sell`로 이동한다. 화면과 DB가 조용히 갈라진다.
- [x] **[Review][Patch] `sort_order` UPDATE가 0행이어도 성공으로 처리된다** [`web/src/app/(user)/sell/photo-sync.ts:93`] — 다른 탭이 그 사진을 지웠으면 대상 행이 없어 0행이지만 `error`는 null이라 성공으로 흐른다. 같은 파일의 `listings` UPDATE는 이 함정을 알고 `.select('id')`로 0행을 잡는데(`SellForm.tsx:283-296`) 여기선 그 패턴을 안 쓴다.
- [x] **[Review][Patch] EXIF orientation 미지정 — 아이폰 세로 사진이 눕는다** [`web/src/lib/images/resize.ts:43`] — `createImageBitmap(file)`을 `{ imageOrientation: 'from-image' }` 없이 호출한다. Orientation 6/8 JPEG는 브라우저·버전에 따라 90° 돌아간 채 인코딩되고, 가로/세로가 뒤바뀌어 리사이즈 비율까지 어긋난다. **재인코딩이라 원본 EXIF가 사라져 되돌릴 수 없다.** 판매자가 폰으로 찍은 사진이 주 사용 시나리오라 실사용 빈도가 높다.
- [x] **[Review][Patch] `photo-sync.ts`가 계약으로 승격한 3규칙에 실행되는 검사가 0건** [`web/src/app/(user)/sell/photo-sync.ts`] — 삭제 순서·대표 2문장·순차 INSERT 전부 주석으로만 존재하고 테스트 파일이 없다. 스토리가 근거로 든 Vitest는 전부 순수 함수(order/validate/computeTargetSize)다. CLAUDE.md B9("주석·문서는 계약이 아니다. 지켜야 하는 규칙이면 실행되는 검사로 바꾼다") 미이행 — 위 수정들과 함께 회귀 테스트를 붙인다.
- [x] **[Review][Patch] 문서 정정 2건** [`docs/tech-debt.md`, `docs/conventions.md`] — ① tech-debt #47-4의 "✅ 클라가 보장" 판정이 과하다: 해피패스 1회 관찰이 근거인데 위 `is_cover` 실패 갈래와 `sort_order≠0` 대표 갈래를 포함하지 않는다(B8 — 대장에 틀린 ✅는 "했는지 모르는 것"보다 비싸다). ② `conventions.md` §10.1이 삭제 **순서**만 규정하고 그 필수 귀결("오브젝트 삭제가 실패하면 행을 지우지 말 것")이 문서에도 코드에도 없다.
- [x] **[Review][Patch] 등록 성공 시 objectURL이 회수되지 않는다** [`web/src/app/(user)/sell/SellForm.tsx:344`] — `setPhotos([])`는 언마운트가 아니라 목록만 비우므로, 매 렌더 갱신되는 `itemsRef.current`가 이미 `[]`라 언마운트 훅도 회수할 대상이 없다(`PhotoUploader.tsx:39-48`). 개별 ✕ 삭제 경로만 revoke하고 일괄 초기화 경로가 빠졌다. 연속 등록 시 등록 1건당 최대 10장 blob이 탭 닫을 때까지 누적.
- [x] **[Review][Patch] `beforeunload`에 `returnValue` 미설정** [`web/src/app/(user)/sell/SellForm.tsx:159-161`] — `preventDefault()`만 호출한다. 최신 Chrome/Firefox는 이걸로 뜨지만 Safari 등 일부는 `e.returnValue = ''`를 요구해 그 환경에선 **경고 없이 작성 내용이 사라진다**. 스토리의 AC7 실측은 Playwright(Chromium) 단일 브라우저였다.
- [x] **[Review][Patch] `photo-sync.ts`에 `'use client'`가 없다** [`web/src/app/(user)/sell/photo-sync.ts:1`] — 상단 주석은 "브라우저 전용"이라 선언하지만 지시자가 없다. 현재는 `SellForm.tsx`(client)만 import해 동작하나, 같은 폴더의 서버 컴포넌트가 실수로 import하면 `@/lib/supabase/client`가 서버에서 돈다. **이 스토리가 바로 그 종류의 런타임 500을 겪고 `photo-item.ts`를 분리했는데**(Completion Notes) 재발 방지가 주석에만 남았다.

#### 이월 (defer) — 전부 `docs/tech-debt.md`에 등재

- [x] **[Review][Defer] 수정 도중 매물이 `sold`로 바뀌면 그대로 관통한다** [`web/src/app/(user)/sell/[id]/edit/page.tsx:72`] — deferred, pre-existing. `sold` 차단이 서버 렌더 1회뿐이고 제출 경로는 status를 재확인하지 않으며, `0002`의 `listings_update_own`도 `auth.uid() = seller_id`만 본다. 코드 주석이 "이중 방어"라 적었지만 실제로는 화면 층 단일 방어(B9). Epic 2 유래.
- [x] **[Review][Defer] 서명 URL TTL(3600) 만료 후 미리보기 복구 수단이 없다** [`web/src/app/(user)/sell/[id]/edit/page.tsx`] — deferred. 수정 화면을 1시간 이상 열어두면 기존 사진이 전부 깨진 이미지가 되는데 갱신 로직이 없다. 삭제·순서 조작은 `storagePath` 기반이라 계속 동작하므로 사용자가 "사진이 사라졌다"고 오인해 지울 위험.
- [x] **[Review][Defer] 다른 탭·세션이 10장을 채우면 업로드된 오브젝트가 영구 고아로 남는다** — deferred. 업로드는 전부 성공한 뒤 행 INSERT가 `enforce_listing_images_max_10`에 걸리고, `photo-sync.ts:109`의 정리 시도는 행이 없어 실패한다(코드 주석도 인정만 함). #43·#46·#49의 합성 발현이라 개별 수정보다 정리 잡·서버 강제가 맞다.
- [x] **[Review][Defer] WebP→JPEG 폴백 경로가 테스트·실측 0건** [`web/src/lib/images/resize.ts:62-66`] — deferred. `resize.test.ts`가 스스로 이 분기는 실행되지 않는다고 명시했고 Task 6 실측도 WebP 경로만 측정했다. 해당 브라우저에서 AC4 규격 보장이 미검증.
- [x] **[Review][Defer] 애니메이션 WebP 첫 프레임만 저장 / 투명 PNG 알파 손실이 무고지** [`web/src/lib/images/resize.ts:43-63`] — deferred. 재인코딩이라 원본 복구 불가인데 사용자에게 아무 안내가 없다. 캔버스 배경을 칠하지 않아 JPEG 폴백 시 투명 영역이 검게 나온다.
- [x] **[Review][Defer] `conventions.md` §10.1 "읽는 쪽은 `order by sort_order, id`" 서술이 현재형 약속** — deferred, 문서 미세 드리프트. 실제로 그 정렬을 거는 곳은 `[id]/edit/page.tsx` 하나뿐이고 9.4·9.5가 아직 없다. 강제하는 검사가 없어 다음 소비처가 안 걸어도 아무도 안 깨진다.

#### 기각 (dismiss) 1건

- `Button`이 `<form>` 안에서 기본 `type=submit`이라 [대표로]·[재시도] 클릭이 폼을 제출한다(Blind Hunter) → **오탐.** `web/src/components/ui/Button.tsx`가 `type = 'button'`을 기본값으로 명시하고 있고 주석까지 "폼 안에서 실수로 제출되지 않게"라고 적혀 있다. diff만 본 레이어의 예상된 한계 — 직접 파일을 열어 확인 후 기각.

#### 개발 에이전트 보고 검증 결과 (사용자 요청분)

| 보고한 주장 | 판정 | 근거 |
|---|---|---|
| 대표 = 순서 0번, `is_cover`는 파생 | **부분 사실** | 화면 상태 층은 주장대로(별도 cover 필드 없음, `moveToFront = reorder(items,i,0)`). 그러나 **DB 층에서 갈라지는 경로 3개** 존재(위 patch 3·5). DB에 이 규칙을 강제하는 제약·트리거도 없다 |
| 삭제는 오브젝트 먼저 → 행 나중 | **부분 사실** | 순서 자체는 코드가 지킨다(반대 순서 경로 0건). 그러나 **"삭제 실패 시 행을 지우지 않는다"는 보장이 없어** 계약이 막으려던 영구 고아가 에러 경로에서 발생(위 patch 2) |
| 저장본 1600px·WebP·0.82, 89% 감소 | **사실** | 업로드 진입점이 `photo-sync.ts:69` 하나뿐이고 바로 앞에서 무조건 `resizeImage`를 탄다. 원본이 리사이즈 없이 Storage에 닿는 경로 0건. 상수도 일치. 5MB 검증이 원본 기준인 것도 계약과 일치 |
| 미해결 3건(#53·#43·#49) 신고 | **정확하나 불완전** | 세 건의 서술은 코드와 일치(각각 확인). 그러나 **미신고 결함이 최소 6건** — 특히 patch 1·2·3·5는 이 스토리가 새로 만든 것이고 대장 어디에도 없다 |
| lint·test·build 초록 | **사실(재측정)** | 리뷰 세션에서 직접 실행: `npm run lint` 무경고, Vitest **33건 통과**. 단 스토리 File List는 "27건"으로 적혀 있어 숫자 드리프트 |

## Dev Notes

### 0. 범위 경계 (먼저 읽을 것)

**이 스토리는 web만 만든다.** AC 원문은 "매물 등록/수정 폼"이라고만 하고 플랫폼을 못 박지 않지만, 이 레포의 증분 구조는 **Epic 9~15 = web·api, Epic 16 = Flutter 앱 미러링**이다(`epics-increment-2026-07-12.md` Epic 16: 16-1 토큰·내비 … 16-2 이미지 카드 재설계-앱). 또 앱에는 사진 선택 패키지(`image_picker` 등)가 `app/pubspec.yaml`에 **아예 없다** — 앱 업로더는 새 의존성·권한(카메라/갤러리) 설계가 붙는 별개 크기의 일이다.

⚠️ Epic 16에도 "앱 사진 업로더" 스토리가 없어서 현재 계획대로면 앱 판매자는 사진을 못 올린다. **사용자 결정(2026-07-18): 웹만 이번에 하고, 앱 업로더는 Epic 16에 별도 스토리로 추가한다.** 그 사실은 `docs/tech-debt.md`에 등재해 뒀다 — **이 스토리에서 앱을 건드리지 않는다.**

**이 스토리 밖(하지 말 것):** 목록 카드 렌더(9.4) · 상세 갤러리(9.5) · AI 응답 카드 사진(9.6) · 시드 사진(9.7) · 이미지 모더레이션(Epic 9 Non-goal).

### 1. 지금 코드가 어떤 상태인가 (실제로 읽고 적음)

| 파일 | 현재 상태 | 이 스토리가 하는 것 |
|---|---|---|
| `web/src/app/(user)/sell/SellForm.tsx` | `'use client'`. 등록·수정 **겸용**(`mode: 'create'\|'edit'`). 15필드. 브라우저 Supabase 클라이언트로 **직접 insert/update**. 검증은 `validateAndBuild()`(142-207행) 클라이언트 100%. 사진 필드 **없음** | 사진 섹션 추가 + 등록 insert에 `.select('id').single()` 추가 |
| `web/src/app/(user)/sell/page.tsx` | 서버 컴포넌트. 본인 매물 목록 조회 후 `SellForm` 렌더 | 변경 최소 |
| `web/src/app/(user)/sell/[id]/edit/page.tsx` | 서버 컴포넌트. 본인 매물만 조회, `sold`면 수정 차단. `SellForm mode="edit"` 재사용 | 기존 사진 조회 + **서버측 서명 URL 발급**해서 전달 |
| `web/src/lib/storage/index.ts` | 9.2 산출물. `getSignedUrl(bucket, path)` · `getSignedUrls(bucket, paths)`, TTL 3600, 실패 시 **`null` 반환(throw 아님)**. **서버 전용** | 읽기에 그대로 재사용. **업로드 함수는 없다 — 이번에 만든다** |
| `web/src/components/ui/` | `Button` `FocusTrap` `CardCarousel` `ResponsiveGrid` `Skeleton` `EmptyState` `ErrorState` `Logo` | `Button`·`FocusTrap` 재사용. **새 프리미티브 만들지 말 것** |
| dirty 가드 | `beforeunload`/`isDirty` **grep 0건** — 없음 | 이번에 처음 만듦 |

**업로드는 브라우저(anon key + Storage RLS)에서 한다.** `SellForm`이 이미 브라우저 클라이언트로 insert하고 있고, `storage_path` 위조는 마이그레이션 `0013`의 트리거가 **DB에서** 막는다(소유자를 클라 입력이 아니라 `listings`에서 조회해 대조). `service_role`은 어디에도 두지 않는다.

### 2. 계약 (값의 정본은 `docs/conventions.md` §10 — 여기 사본을 만들지 말 것)

- 버킷 `listing-images`(비공개) · 경로 `{user_id}/{listing_id}/{filename}`(첫 세그먼트=소유자) · 상한 10장/5MB/MIME 3종 · TTL 3600
- `listing_images(id, listing_id, storage_path unique, sort_order, is_cover, credit)` — `supabase/migrations/0012_listing_images.sql`
- **대표는 매물당 최대 1장**(부분 유니크 인덱스 `listing_images_one_cover_per_listing`)
- `ListingCard.image_url`은 **null 또는 빈 문자열 모두** "사진 준비중"으로 취급(§4) — 이 스토리는 값을 쓰기만 하고 렌더는 9.4/9.5 몫
- ⚠️ **UX 목업 HTML(`mockups/forms-2.html:882`)의 "JPG · PNG, 장당 최대 20MB"는 낡았다.** 정본은 5MB · MIME 3종(WebP 포함)이다. **목업 문구를 그대로 베끼지 말 것**

### 3. 이미 실측된 함정 (남의 스토리가 피 흘려 알아낸 것 — 다시 밟지 말 것)

| # | 무엇 | 이 스토리에서 |
|---|---|---|
| **#47-1** | 대표 교체를 `set is_cover=(id=:new) where listing_id=:L` 단일 UPDATE로 하면 **`duplicate key`로 죽는다**(도커 실측) | AC8 — 2문장 강제 |
| **#47-3** | 10장 초과 예외의 SQLSTATE가 일반 `P0001`이라 **한국어 메시지 문자열 매칭으로만** 구별 가능 | 클라가 먼저 막는다(AC9). 메시지 매칭에 의존하는 코드를 새로 만들지 말 것 |
| **#47-4** | `is_cover default false` → **대표 0장 매물이 정상 상태**로 존재 가능 | 사진이 1장 이상이면 항상 대표 1장이 되게 클라가 보장 |
| **#43** | 10장 상한 트리거가 **INSERT만** 봐서 `listing_id`+`storage_path`를 함께 바꾸는 UPDATE로 **우회된다**(실측: B가 11장) | 이 스토리는 그런 UPDATE 경로를 **열지 않는다**. 사진 이동 기능 만들지 말 것 |
| **#49** | 동시 INSERT 경합 시 둘 다 9장을 보고 통과 → 11장 (미측정, 코드상 명백) | AC9 — 순차 INSERT로 경합을 안 만든다 |
| **#51** | 인증 사용자의 Storage **DELETE/LIST가 403/빈결과**(RLS·GRANT 정상인데도). `service_role`로는 성공. **원인 미상** | **Task 0 — 착수 전 재측정.** 이 스토리가 정면으로 부딪히는 항목 |
| **#46** | 매물 삭제 시 Storage 오브젝트가 **영구 잔존**(고아) | 이번 범위 밖. 단 업로드 후 행 INSERT 실패 시 고아가 생기므로 **가능하면 즉시 정리 시도**하고, 안 되면 기록 |

### 4. 라이브러리 결정 — **새 의존성 0개**

`web/package.json`의 dependencies는 `@supabase/ssr`, `@supabase/supabase-js`, `next`, `react`, `react-dom`뿐이다. 이 스토리는 **여기에 아무것도 더하지 않는다.**

- **리사이즈/압축**: `canvas.drawImage()` → `canvas.toBlob(cb, 'image/webp', 0.82)`. 브라우저 네이티브다. `browser-image-compression` 같은 패키지 불필요
- **드래그 순서변경**: HTML5 native drag&drop(`draggable` + `dragstart`/`dragover`/`drop`). `dnd-kit`·`react-beautiful-dnd` 불필요
- **왜 WebP인가**: 버킷 `allowed_mime_types`에 이미 포함돼 있고, 같은 화질에서 JPEG보다 작다. `toBlob`이 WebP를 못 만드는 환경이면 `image/jpeg` 0.85로 폴백
- **왜 1600px인가**: 상세 갤러리(9.5)가 대표 사진을 크게 쓰고, web은 Next.js Image로 파생 변형을 만들지만 **app은 서명 원본을 그대로 받는다**(ADR-IMG-01). 1600px WebP면 보통 200~400KB — 목록 10장 다중 다운로드에서도 NFR7 안쪽

⚠️ **Next 16 함정**: web 작업 전 `node_modules/next/dist/docs/`를 먼저 읽는다(`web/AGENTS.md`). 훈련데이터의 Next와 다르다. Tailwind는 **v4** — `tailwind.config.js`가 아니라 CSS `@theme`.

### 5. 테스트 표준 (project-context 규칙 12)

- **web은 E2E 우선.** 서버 컴포넌트 밖 **순수 유틸만** Vitest — 이 스토리에선 `validate`·`resize` 계산부·`cover` 3종이 그 대상이다. 컴포넌트 렌더 단위테스트를 새로 만들지 말 것(표준 아님)
- Playwright **스펙 파일은 이 레포에 아직 0개다**. 이 스토리도 스펙을 새로 만들 의무는 없지만, **Task 6의 브라우저 관찰은 반드시 실제로 수행**한다(Playwright MCP로 조작·스크린샷). 로컬에 Playwright Chrome은 설치돼 있다
- CI(`tests.yml`)는 web에서 `npm run lint` → `npm test`를 돌린다. **lint도 게이트다**(2026-07-18 추가) — `react-hooks/refs` 같은 룰에 걸리면 빨간불

### Project Structure Notes

신규 파일 (규칙 9: `components/{ai,auth,layout,listings,ui}`, `lib/{api,auth,supabase}` 구조 준수):

```
web/src/lib/images/validate.ts     (신규, 순수)
web/src/lib/images/resize.ts       (신규, 브라우저 API)
web/src/lib/images/order.ts        (신규, 순수 — 순서 = 대표)
web/src/lib/images/*.test.ts       (신규, Vitest)
web/src/lib/storage/upload.ts      (신규 — 업로드 전용. 기존 index.ts(서버 전용 서명)와 파일을 나눈다)
web/src/app/(user)/sell/PhotoUploader.tsx  (신규, 'use client')
```

- **`lib/storage/index.ts`에 업로드 함수를 섞지 않는다** — 그 파일은 서버 전용이고, 업로드는 브라우저에서 돈다. 한 파일에 두면 서버 클라이언트가 브라우저 번들에 딸려간다
- UI 문자열은 전부 한국어. TypeScript strict. `process.env.X!` 비-널 단언 금지(env는 `getSupabaseEnv()` 경유)
- 마이그레이션이 필요해지면(Task 0 결과 RPC 경로) `supabase/migrations/0014_*.sql`, **전진 전용**, RLS 정책은 해당 테이블 마이그레이션에 동거

### References

- Story 원문 · Epic 9 목표: `_bmad-output/planning-artifacts/epics-increment-2026-07-12.md:442-506`
- UX-DR13(업로더) / UX-DR23(이탈 가드): 같은 파일 `:159`, `:173`
- ADR-IMG-01(비공개 버킷·서명 URL·app은 원본): `_bmad-output/planning-artifacts/architecture-increment-2026-07-12.md:173`
- CR3(wire = 단일 `image_url`): 같은 파일 `:361` · I7(`image_count`): `:384`
- FR26/27/28/29 · NFR7: `_bmad-output/planning-artifacts/prds/prd-bmad-encar-demo-2026-07-11/prd.md:57-61, 148`
- 이미지 스토리지 계약 §10 · ListingCard 계약 §4: `docs/conventions.md:42-75, 185-207`
- 스키마·RLS·트리거: `supabase/migrations/0012_listing_images.sql` · `0013_listing_images_path_integrity.sql`
- 열린 부채 #43·#44·#45·#46·#47·#49·#51: `docs/tech-debt.md:335-412`
- UX 문구·상태표: `_bmad-output/planning-artifacts/ux-designs/ux-bmad-encar-demo-2026-07-12/EXPERIENCE.md:63, 67, 79, 80, 97, 107`
- 업로더 시각 스펙: 같은 폴더 `DESIGN.md:131, 138` · 목업 `mockups/forms-2.html:875-909` (⚠️ 20MB 표기는 낡음)
- 반응형 D5: `DESIGN.md:107` · `_bmad-output/project-context.md` 규칙 13
- Epic 8 회고 A1·A5(대장은 하나 · 실측 없이 선언 금지): `_bmad-output/implementation-artifacts/epic-8-retro-2026-07-15.md:183-197`
- 직전 스토리: `9-1-listing-images-스키마-비공개-버킷-storage-rls.md` · `9-2-서명-url-헬퍼-아티팩트-범용.md`

## Dev Agent Record

### Agent Model Used

claude-opus-4-8[1m] (Claude Code, dev-story 워크플로)

### Debug Log References

#### 1. Task 0 실측 — #51 재현 + **원인 규명** (2026-07-18, 원격 `psrnsasxpkpwqdukjdmt`)

계정: 시드 판매자 `seller-seed@test.com`(uid `12dfba00-…`), 매물 `2bc77650-…`(on_sale, 본인 소유). JWT `role=authenticated`로 확인. 스크립트는 scratchpad(레포 밖).

**(a) #51은 재현된다 — 관찰 원문:**

| 호출 | 결과 |
|---|---|
| `POST /object/list/listing-images` (prefix=`{uid}/{listing}`) | `200` · **건수 0** (슬래시 유무 무관) |
| `DELETE /object/listing-images/{uid}/{listing}/upsert-off.png` | `400` body `{"statusCode":"403","error":"Unauthorized","message":"Access denied"}` |
| `DELETE /object/listing-images` (배치 prefixes) | `200` · **매치 0건**(조용히 안 지워짐) |
| 삭제 후 `storage.objects` SQL 직접 조회 | **행 그대로 존재** — 실제로 안 지워졌음을 화면 아닌 DB로 확인 |

**(b) 원인 — 가설을 세우고 실험으로 확인했다.** 
`storage.objects`의 유일한 SELECT 정책 `listing_images_objects_read`(원격에서 `pg_policies`로 직접 조회)는 **`listing_images` 행과 `storage_path = objects.name`으로 조인**해야 참이 된다. Storage API의 DELETE·LIST는 **먼저 대상 객체를 SELECT로 찾는다.** 따라서 **`listing_images` 행이 없는 객체는 소유자에게도 보이지 않고**, 그 결과가 `403 Access denied` / `LIST 0건`이다. RLS 정책도 GRANT도 정상이라는 9.2의 관찰과 모순되지 않는다 — 9.2는 **행 없는 객체**로 시험했다.

**예측 → 검증:** 같은 객체에 `listing_images` 행을 **먼저 넣고** 재시도:

| 호출 | 행 없을 때 | 행 있을 때 |
|---|---|---|
| LIST | `200` 0건 | `200` **1건** |
| SIGN | `404 not_found` | **`200`** |
| DELETE 단건 | `403 Access denied` | **`200 {"message":"Successfully deleted"}`** |

**→ #51은 버그가 아니라 순서 제약이다. `service_role`도 `security definer` RPC도 필요 없다.**
**해소 규칙(이 스토리가 지킬 것): 삭제는 반드시 `Storage 오브젝트 먼저 → 그 다음 `listing_images` 행`. 행을 먼저 지우면 객체가 안 보이게 되어 영구 고아가 된다(#46).**

**(c) 덤으로 나온 실측 — `x-upsert: true`는 RLS에 막힌다.** 같은 경로·같은 토큰으로 미존재 객체 업로드 시 upsert 없음 → `200`, `x-upsert:true` → `400 {"statusCode":"403","message":"new row violates row-level security policy"}`. 같은 뿌리(업서트가 존재확인 SELECT를 거친다)로 설명된다. **업로더는 upsert를 쓰지 않는다** — 파일명이 uuid라 충돌이 없으므로 필요도 없다.

**(d) 정리 못 한 잔여물 (숨기지 않고 적음):** 원인 규명 과정에서 `{uid}/probe-a.png`·`{uid}/x/y.png` 2개(각 70바이트)를 만들었다. 이 경로들은 `listing_images` 경로 계약(3세그먼트 `{소유자}/{매물}/{파일}`)을 만족하지 못해 행을 붙일 수 없고, 따라서 **위 규칙대로는 지울 수 없다.** SQL 직접 삭제도 `storage.protect_delete()`가 막는다(`42501`). **= #46(고아 오브젝트)의 실제 발현 사례 1건.** 사용자 조치 필요 → Completion Notes에 항목으로 올림.

### Completion Notes List

**AC별 확인 방법(전부 실제로 돌려서 관찰함 — 추론 0건):**

| AC | 확인한 것 | 어떻게 |
|---|---|---|
| 1 | 드롭존·`N/10` 카운터·첫 칸 "대표" 배지·[대표로]·순서 변경 | 브라우저 조작 + 스크린샷 3장. [대표로] 클릭 시 초록이 0번으로 오고 나머지 순서 보존 관찰 |
| 2 | 사진 0장 등록 정상 완료 | 사진 없이 제출 → `"매물이 등록되었습니다. 아래 목록에 바로 노출됩니다."`, 오류 0건 |
| 3 | 실패가 폼을 막지 않음 + 대표 자동 승격 | 8.4MB 파일 포함 4장 제출 → **매물 등록 성공** + `"(사진 3장 저장, 1장 실패)"`. 대표 삭제 후 다음 장이 즉시 대표가 됨(화면·DB 양쪽) |
| 4 | 저장본이 실제로 작아짐 | 원본 PNG 1,706,169~1,870,531B(2400×1600) → 저장본 WebP **196,430 / 201,952 / 205,536B · 1600×1067**. 바이트는 `storage.objects.metadata`, 해상도는 브라우저 `naturalWidth`로 실측. **약 89% 감소** |
| 5 | 경로 규약 | DB 실측: `12dfba00…/d427d191…/{uuid}.webp` — `{user_id}/{listing_id}/{filename}` 일치. 등록 insert에 `.select('id').single()` 추가해 id 확보 후 업로드 |
| 6 | 성공한 사진만 행 생성 | 4장 중 3장 성공 → `listing_images` **3행**(실패분 행 없음). 진행/실패 상태는 화면에만 존재 |
| 7 | 이탈 가드 | ① 변경 없음 → 경고 없이 이동 ② 순서만 바꾼 뒤 [취소] → 다이얼로그 노출(문구 일치) ③ dirty 상태 주소 이동 → **브라우저가 페이지를 못 떠남**(서버 로그에 요청 미도달) |
| 8 | 대표 2문장 | 대표 삭제·순서 변경 후에도 `is_cover`가 정확히 **1건**, `sort_order=0` 행에 위치. `duplicate key` 미발생 |
| 9 | 클라 1차 차단 + 순차 INSERT | 코드상 순차 루프. 10장 초과분은 UI가 제외하고 사유 안내 |
| 10 | 삭제가 실제로 지워짐 | 대표 1장 삭제 후 DB 실측: 행 2개(sort_order 0,1) · Storage 오브젝트 2개 · **삭제 대상 잔존 0건**. Task 0에서 정한 순서(오브젝트→행)가 실제 앱 흐름에서 통함을 확인 |

**설계 판단 3건:**
1. **업로드 시점을 제출 시로 통일** — AC5가 신규 등록에 스테이징 경로를 금지하므로 등록은 `listing_id`를 받기 전엔 올릴 곳이 없다. 수정 모드만 즉시 업로드하면 코드가 둘로 갈려 한쪽만 고치는 버그가 난다 → **등록 = "기존 사진 0장인 수정"** 으로 두고 `syncListingPhotos` 하나가 둘 다 처리한다.
2. **검증 실패 항목에는 [재시도]를 주지 않았다** — AC3 문구는 "인라인 오류 + 재시도"지만, 용량초과·포맷거부는 같은 파일로 다시 눌러도 결과가 같다. 재시도는 **업로드 실패(네트워크·서버)** 에만 붙이고 검증 실패는 사유만 보여준다. (실패 항목의 [대표로] 버튼도 제거 — 저장되지 않을 사진을 대표로 지정하면 배지 없는 첫 칸이 생긴다.)
3. **`0014` 마이그레이션을 만들지 않았다** — 스토리 Task 0이 "재현되면 RPC로 감싸라"고 했으나, 재현 후 **원인이 순서 문제**로 밝혀져 RPC·`service_role`이 불필요해졌다(Debug Log 1). 새 마이그레이션 0개.

**실행이 잡아낸 버그 1건 (타입검사·lint·build가 전부 놓침):** `toPhotoItems`를 `'use client'` 파일(`PhotoUploader.tsx`)에 두었더니 수정 페이지(서버 컴포넌트)가 호출하는 순간 **런타임 500**(`Attempted to call toPhotoItems() from the server`). `tsc`·`eslint`·`next build` 셋 다 초록이었고 **페이지를 실제로 열어서야** 드러났다. → 타입·변환을 `'use client'` 없는 `photo-item.ts`로 분리. 경위는 그 파일 상단 주석에 남김.

**⚠️ 사용자 조치 필요 1건:** `listing-images` 버킷에 지울 수 없는 프로브 객체 2개가 남았다 — `12dfba00-…/probe-a.png`, `12dfba00-…/x/y.png`(각 70B, Task 0 원인규명 중 생성). 경로가 3세그먼트 계약을 만족하지 않아 `listing_images` 행을 붙일 수 없고, 행이 없으면 소유자에게도 안 보여 Storage API로 못 지운다. SQL 직접 삭제도 `storage.protect_delete()`가 차단(`42501`). **Supabase 대시보드 → Storage → `listing-images`에서 수동 삭제**하면 된다(대시보드는 `service_role` 경로). `docs/tech-debt.md` #46에 실제 발현 사례로 등재함.

**이 스토리가 안 다룬 것(명시):** #43(트리거 UPDATE 우회)은 **경로를 열지 않았을 뿐 구멍은 그대로**. #49(동시성 경합)는 **순차 INSERT로 회피했을 뿐 여전히 미측정 가설**. #47-2·#47-3은 클라 규약으로 회피, DB는 미변경. 헤더 `<Link>` 이탈은 **막지 못했고** #53으로 신규 등재. 전부 `docs/tech-debt.md`에 판정과 함께 적었다.

### File List

**신규 (7)**
- `web/src/lib/images/validate.ts` — MIME 화이트리스트 + 5MB 상한(순수)
- `web/src/lib/images/resize.ts` — 저장본 규격 인코딩 + `computeTargetSize`(순수부 분리)
- `web/src/lib/images/order.ts` — `reorder`/`moveToFront`/`remove`(순수, 순서 = 대표)
- `web/src/lib/images/validate.test.ts` · `order.test.ts` · `resize.test.ts` — Vitest 33건
- `web/src/app/(user)/sell/photo-sync.test.ts` — **계약 3규칙 회귀 테스트 17건**(코드리뷰 F8). Supabase·Storage 경계를 가짜로 바꿔 **호출 순서·인자**를 검사한다. 검사 상단에 "안 보는 것"(실제 RLS 동작·브라우저 동작)을 명시
- `web/src/lib/storage/bucket.ts` — 버킷 상수(서버·브라우저 공용, 클라이언트 혼입 방지)
- `web/src/lib/storage/upload.ts` — 브라우저 업로드·오브젝트 삭제(삭제 순서 계약 주석)
- `web/src/app/(user)/sell/PhotoUploader.tsx` — 업로더 UI(클라이언트 컴포넌트)
- `web/src/app/(user)/sell/photo-item.ts` — 사진 항목 타입·변환(서버·브라우저 공용)
- `web/src/app/(user)/sell/photo-sync.ts` — 화면 상태 → DB·Storage 반영(등록·수정 공용)

**수정 (4)**
- `web/src/app/(user)/sell/SellForm.tsx` — 업로더 배선, 등록 insert에 `.select('id').single()`, dirty 감지·이탈 가드, [취소]를 버튼으로
- `web/src/app/(user)/sell/[id]/edit/page.tsx` — 기존 사진 조회 + **서버측** 배치 서명 URL 발급
- `docs/conventions.md` — §10.1 신설(저장본 규격·대표 = 순서 0번·대표 2문장·삭제 순서·upsert 금지·순차 INSERT·sort_order 연속)
- `docs/tech-debt.md` — #51 **닫음**(원인 규명 + 해소 규칙), #46에 실제 발현 사례, #43·#47·#49에 9.3 판정, **#53 신규**(Link 이탈 미차단)

### Change Log

| 날짜 | 내용 |
|---|---|
| 2026-07-18 | Task 0 실측 — #51 재현 + 원인 규명(행 없는 객체는 소유자에게도 안 보임). RPC·마이그레이션 불필요 판정 |
| 2026-07-18 | Task 1 — 순수 유틸 3종 + Vitest 27건 추가(변이 3건으로 red 확인 후 green 복귀) |
| 2026-07-18 | Task 2~5 — 업로더 컴포넌트·등록/수정 배선·이탈 가드 구현 |
| 2026-07-18 | 런타임 버그 수정 — `toPhotoItems`를 `'use client'` 밖(`photo-item.ts`)으로 분리(서버 컴포넌트 호출 500) |
| 2026-07-18 | Task 6 — 브라우저 E2E 관찰 + DB·Storage 직접 조회로 AC 10개 전부 확인. 테스트 데이터 정리 |
| 2026-07-18 | Task 7 — `conventions.md` §10.1 신설, `tech-debt.md` #51 닫음·#53 등재·#43/#47/#49 판정 |
| 2026-07-19 | 코드리뷰 수정 14건 적용(dev 세션) — `photo-sync.ts` 중심. **세션 중단으로 검증 단계 미완** |
| 2026-07-19 | 중단분 이어받음 — 누락 2건 완료: `photo-sync.test.ts` 신규 17건 + `conventions.md` §10.1 귀결("①이 실패하면 ②를 하지 않는다") 추가 |
| 2026-07-19 | 수정본 자체 검토에서 결함 1건 추가 발견·수정 — 동일 warning이 사진 수만큼 중복돼 사용자 문구에 반복 노출. `[...new Set()]`로 제거 |
| 2026-07-19 | **red/green 실측 7건** — 고친 것을 하나씩 되돌려 전부 🔴 확인 후 복구 🟢. lint 무경고 · Vitest **51건**(33→+18) · `next build` 성공 |
| 2026-07-19 | 브라우저 E2E 재관찰(sonnet) — 등록·순서변경·삭제·재제출2회·EXIF·이탈가드 6항목 OK. EXIF는 Orientation=6 JPEG로 `naturalWidth/Height`=600×1200 확인(육안 아님, 썸네일이 정사각 크롭이라 스크린샷으로는 판정 불가) |
| 2026-07-19 | 후속 실측 2건 → 대장 등재 — **#60 매물 삭제 시 사진 영구 고아**(cascade 실측: 행 1→0, 가짜 경로로 측정해 실제 고아 미생성) · **#61 동시 더블클릭을 `loading` 가드가 막지 못함**(PATCH 2회 실측, 이번엔 멱등이라 무해) |
| 2026-07-19 | **#60·#61 수정 + 실측** — 매물 삭제가 사진 오브젝트를 먼저 지운 뒤 매물을 지운다(실패 시 삭제 중단) · 중복 제출 빗장을 상태→ref로 이동. 회귀 5건 추가(Vitest 56건), red/green 2건. 브라우저: 삭제 후 `listing_images`·`storage.objects` 모두 0건 / 동기 더블클릭 시 POST·PATCH 각 1회(수정 전 2회) |
