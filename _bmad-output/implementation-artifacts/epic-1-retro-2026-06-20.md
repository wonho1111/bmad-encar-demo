# Epic 1 회고 — 프로젝트 기반 + 인증·계정

- **일자:** 2026-06-20
- **대상:** Epic 1 (FR1~FR4) — 역할 선택 가입·로그인/로그아웃·역할별 접근제어·관리자 계정
- **상태:** 스토리 1.1~1.5 전부 `done` (5/5, 100%)
- **참여(가상 팀):** Amelia(개발·facilitator), Winston(아키텍트), John(PM), Sally(UX), Mary(분석) + Dnjsg(프로젝트 리드)
- **첫 회고** (이전 에픽 없음)

## 전달 요약

| 스토리 | 내용 |
|---|---|
| 1.1 | 모노레포/web 스캐폴딩 + Supabase 배선 + 공유 계약 단일출처(constants·conventions) |
| 1.2 | `0001_profiles`(테이블+가입 트리거+is_admin+RLS) + 역할 선택 가입 |
| 1.3 | 로그인/로그아웃 + 홈 인증 상태 표시 |
| 1.4 | 접근제어 — `proxy.ts` 1차 게이트 + `(admin)` `requireRole` 2차 집행 + profiles RLS 검증 |
| 1.5 | 관리자 계정 시드(`supabase/seed.sql`, `admin@test.com`, 영속) |

- 운영 장애: 0건(미배포). 막혔다 풀린 함정: RLS 무한재귀·`getSession` 거짓통과·트리거 admin강등·Next16 proxy 개명.

## 잘된 점 (반복할 패턴)

1. **범위 경계 문서화 + 인수인계** — 매 스토리 "이건 다음 스토리" 명시, 리뷰 dismiss를 "범위 외"로 체계 이연 → 중복·혼동 거의 없음.
2. **재발명 금지** — `USER_ROLE`·`client.ts`/`server.ts`를 출처 1개로 명시·재사용(`[Source: 경로]`). 이번 세션 `ROLE_LABEL` 통합도 같은 결.
3. **한국어 에러 일관성** — `toKoreanError`/`toKoreanLoginError`로 원본 영문 에러 은닉 + 친절 안내(보안+UX 동시).
4. **다층 E2E + SQL 교차검증** — Playwright 행복/오류 경로 + `execute_sql`로 DB 상태(역할·멱등·정리)까지. 테스트 계정 cascade 삭제로 깨끗하게 마감.

## 어려웠던 점 / 반복된 함정

1. **Supabase env 누락 가드 부재** — 1.1·1.2·1.3 내내 `process.env...!`만 → 불투명 에러. 1.4에서 `getSupabaseEnv()`로 일원화 종결. *교훈: 미룬 가드는 세 스토리를 따라다녔다.*
2. **'거짓 성공' 응답** — Supabase가 에러도 user도 없이 성공한 척(중복가입 `identities:[]` 등) → `!data.user` 가드 패턴 정착.
3. **RLS 무한재귀(1.2)** — 정책이 자기 테이블 재조회. `is_admin()` SECURITY DEFINER로 끊음(이후 관리자 판정의 원천 패턴).
4. **Next.js 16 `middleware.ts`→`proxy.ts` 개명(1.4)** — 문서가 옛 표기라 코드리뷰 'middleware 아님' High **오탐** 유발. → 문서 미정정이 원인.

## 핵심 교훈

- 보안 가드(env·접근제어)는 **미루지 말고 토대에서** 세운다 — 안 그러면 매 스토리를 따라다닌다.
- **문서와 코드의 표기 불일치는 미래의 오탐**을 만든다(이번 proxy 사례) → 즉시 동기화.
- 이중 방어(proxy optimistic 1차 + 서버 레이아웃 역할 2차 + RLS) 패턴이 Epic 6까지 재사용된다.

## 이번 세션 후속 작업(회고 직전 처리)

- 관리자 역할별 랜딩: admin 로그인→홈에서 `redirect('/admin')` (party-mode 합의: 랜딩만 분기, 메인 접근 미차단). 커밋 `1977f31`.
- 공용 상단바 `AppHeader`로 홈·관리자 UI 통일 + `ROLE_LABEL` constants 이동. 커밋 `1977f31`.
- body `suppressHydrationWarning`(확장 프로그램 경고 방어). 커밋 `085aaff`.
- 계획문서 `middleware.ts`→`proxy.ts` 표기 정정. 커밋 `0ca043b`. ✅
- 탭 제목 "중고차 직거래". 커밋 `4a5e302`. ✅

## 액션 아이템

| # | 항목 | 분류 | 담당 | 상태 |
|---|---|---|---|---|
| 1 | 설명을 더 쉽게(잘게·비유·확인지점) — 초보자 1회독 이해 목표 | 프로세스 | Claude | 진행(메모리 기록 `explain-simpler-for-beginner`) |
| 2 | **Vercel↔GitHub 저장소 연결 + push → 실제 배포·검증** | 배포(크리티컬) | Dnjsg | 미완 |
| 3 | open-redirect 방어 — "로그인 후 원래 경로 복귀" 기능 만들 때 안전검증(`/`로 시작만 허용) 함께 | 기술부채(이연) | — | 이연(현재 미사용=위험 없음) |
| 4 | 계획문서 proxy 표기 정정 | 문서 | Claude | ✅ 완료 |
| 5 | 탭 제목 서비스명 | 웹 | Claude | ✅ 완료 |

## Epic 1 준비도(Readiness) 점검

- **스토리 완료:** ✅ 5/5 done
- **테스트·품질:** ✅ lint/build + Playwright E2E + SQL 교차검증 통과(로컬)
- **배포:** ⚠️ **미완** — Vercel↔GitHub 연결·push 안 됨. 스토리는 끝났으나 실제 배포·운영 검증은 안 됨.
- **잔여 기술부채:** open-redirect 방어(이연, 현재 위험 없음)
- **판정:** 스토리 관점 완료. 단 **배포(액션 2)** 가 Epic 1의 실질 마무리로 남음.

## 다음: Epic 2 — 판매자 매물 등록·관리 (FR5~8)

- **핵심:** `0002_listings`(15필드 + `status` + 임베딩 컬럼, 사진 제외)
- **선치 원칙(Winston):** 소유권 RLS(FR6) + 판매완료 비노출 RLS(FR11)를 **같은 `0002` 마이그레이션에 동거**시켜 DB측(RLS+CHECK/트리거)으로 못박기.
- 매물 등록·즉시노출(FR7) → 본인 매물 조회·수정·삭제(FR6) → 구매완료 전환(FR8) → 샘플 매물 시드(2.5)
- **의존성:** 건강함. Epic 1의 `is_admin()`·RLS 동거 원칙·`status` enum 상수·시드 패턴(1.5)·비운 홈 중앙 콘텐츠 자리가 그대로 받쳐줌. **막힌 선행작업 없음.**
- **중대 발견(plan 변경 필요):** 없음 — Epic 2 계획 그대로 유효.

## 다음 단계

1. (권장) 액션 2 처리: Vercel↔GitHub 연결 + push로 Epic 1 실배포 마무리.
2. Epic 2 첫 스토리(2.1 `0002_listings` + 소유권/비노출 RLS) 작성 → 개발.
3. 액션 1(쉬운 설명)은 모든 후속 답변에 상시 적용.
