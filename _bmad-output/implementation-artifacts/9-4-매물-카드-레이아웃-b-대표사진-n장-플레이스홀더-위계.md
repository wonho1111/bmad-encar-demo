---
baseline_commit: 5183ff00c0b57e0c1227afeddad2a2b7300559c0
---

# Story 9.4: 매물 카드 레이아웃 B (대표사진·N장·플레이스홀더·위계)

Status: done

> ✎ **개정 (2026-07-19)**: Story 9.0(공개 버킷 전환) 이후로 다시 썼다. 서명 URL 관련 지침(배치 발급·서버 전용 제약·만료 처리·`next/image` 캐시 위험)이 전부 사라져 **원래보다 짧다**. 이전 판을 보고 있다면 그건 폐기본이다.

## Story

As a 구매자,
I want 목록 카드에서 대표사진·가격·핵심 정보를 한눈에 보길,
so that 빠르게 훑고 관심 매물을 고른다.

**범위: web only** (`ListingCard.tsx` + 소비처 3곳). api·app·DB 마이그레이션은 이 스토리 밖 — [Dev Notes §0](#0-범위-경계--먼저-읽을-것).

## Acceptance Criteria

원문 출처: `_bmad-output/planning-artifacts/epics-increment-2026-07-12.md` Story 9.4 (FR27·FR29·FR32, UX-DR4/6/7, D5).
AC1~AC6이 원문 유래, **AC7~AC9는 `docs/tech-debt.md`가 이 스토리를 예약·트리거로 지목한 이월**(#57·#58·#63·#59·#47-4)이다(CLAUDE.md B5).

---

1. **레이아웃 B 순서** — 위에서 아래로 정확히:
   **① 사진(5:3) → ② 신뢰속성 행 슬롯 → ③ 차량명 → ④ meta(주행·연료·지역) → ⑤ 가격(최상위) → ⑥ 옵션 칩 슬롯**
   - ③ `text-card-title`(16/600) `text-ink-primary`, 형식 `[제조사] 모델 · 연식`
   - ④ `text-meta`(13/500) `text-ink-muted`, **한 줄 가로 유지**
   - ⑤ `text-price`(26/800) `text-price-emphasis` — **카드에서 시각적으로 가장 큰 요소**(차량명보다 작으면 실패)
   - ②·⑥ 슬롯은 **값이 없으면 아무것도 렌더하지 않는다**(빈 높이·빈 테두리 금지). 값은 Epic 10이 채우며 지금은 항상 비어 있다.
   - ⚠️ 현재 `ListingCard.tsx`는 zinc 유틸(`border-zinc-200`·`text-zinc-500`)이다. 8.1 토큰(`bg-surface-raised`·`text-ink-*`·`rounded-card`·`shadow-card`)으로 **교체**한다 — 새 토큰을 만들지 않는다(전부 `globals.css`에 있다).
   (FR32, UX-DR4, DESIGN.md "매물 카드 — 레이아웃 B")

2. **사진 있으면 대표 1장 + "N장" 배지** — `image_url`이 있으면 5:3으로 대표 1장을 렌더하고, 사진 위 **우하단**에 다크 pill "N장"을 붙인다(`image_count` ≥ 1일 때만).
   - 크롭 = 클라 렌더 크롭(`object-fit: cover`, 중앙). 저장본을 다시 만들지 않는다(I14).
   - **`backdrop-filter` 없이도 읽혀야 한다** — 불투명 다크 pill이 기본, blur는 지원될 때만 얹는다(구형 WebView no-op, UX-DR6).
   - **폴백 상태의 흰 글자 대비를 계산해 기록**한다(≥4.5:1). 리뷰가 "폴백 대비 미검증"으로 지적한 항목이다(`review-accessibility.md` [medium]).
   (FR29, UX-DR6, I14)

3. **사진 없거나 로드 실패면 "사진 준비중" 플레이스홀더** — `bg-placeholder-bg` + 카메라 아웃라인 글리프 + "사진 준비중". **빈 영역·깨진 이미지 아이콘 금지**(의도적으로 보이게).
   - 트리거 3가지를 **모두** 처리한다: `image_url`이 **`null`** · **빈 문자열(`""`)** · **`onError`(로드 실패)**.
   - ⚠️ **`onError`가 9.0 이후 더 중요해졌다.** 서명 시절엔 발급 실패가 `null`로 와서 자동으로 플레이스홀더가 됐지만, `getPublicUrl`은 **파일이 없어도 문자열을 돌려준다**(존재를 확인하지 않는다). 이제 깨진 이미지를 막는 유일한 장치가 `onError`다 — 빠뜨리면 사진 없는 매물에서 깨진 아이콘이 그대로 노출된다.
     - ✎ **2026-07-19 코드리뷰 정정: `onError` 하나로는 부족하다.** 하이드레이션 전에 나는 실패(=404 같은 빠른 실패)는 `onError`가 못 잡는다. **2겹이 필요하다** — 정본은 `conventions.md` §10.2. **이 AC 문장을 9.5·9.6에 그대로 복사하지 말 것.**
   - ⚠️ **문구 대비를 계산해 확인한다.** `--ink-muted`(#676D69) on `--placeholder-bg`(#F1EFE9) = **약 4.4:1로 AA 4.5:1 미달 가능성이 높다**(개략 계산, 실측 필요). 미달이면 이 문구만 `text-ink-secondary`(#565F5D, 약 5.9:1)로 올린다. **다크 모드도 같이 잰다.** 토큰 값 자체는 바꾸지 않는다(다른 화면 회귀).
   (FR27, UX-DR7, `conventions.md` §4, `review-accessibility.md`)

4. **찜(♡) 버튼은 사진 밖, 위치·시각만** — 정보 영역 **우상단** 원형 버튼(그림자). **동작·토글·상태는 Epic 10.5의 몫이며 여기서 만들지 않는다.**
   - 동작이 없으므로 `disabled` + `aria-hidden="true"` + `tabIndex={-1}` — 스크린리더·키보드가 죽은 컨트롤을 잡지 않게.
   - 터치 타깃 **≥44×44px**(`review-accessibility.md` [high]).
   - ⚠️ **HTML 중첩 함정**: 현재 카드 전체가 `<Link>` 하나다. `<a>` 안의 `<button>`은 유효하지 않다. 루트를 `<article className="relative">`로 바꾸고 `<Link>`는 **내용 영역만** 덮은 뒤 찜 버튼을 그 밖에 `absolute`로 둔다.
   (FR32, DESIGN.md D3)

5. **대표 1장만 로드** — 대표 외의 이미지를 요청하지 않는다. `loading="lazy"` + `decoding="async"` + `aspect-ratio`로 **레이아웃 시프트 없이** 지연 로드. (NFR7)

6. **D5 반응형 무결성** — `/search`·홈 미리보기의 카드 목록을 `ResponsiveGrid`로 바꾼다(**≥1100px 4열 · 640~1099px 2열 · <640px 1열**). 현재는 `<ul className="flex flex-col gap-2">` 1열 고정이다.
   - **카드 내부 가로 배치(meta 한 줄·신뢰속성 행·옵션 칩)는 어떤 폭에서도 세로로 접히지 않는다.** 공간 부족은 `truncate`로만.
   - 검증은 **실제 브라우저 3개 뷰포트**(1280·800·390px)에서 눈으로 + 스크린샷(AC10).
   (D5, project-context 규칙13, UX-DR21)

---

**여기부터는 대장이 이 스토리를 지목한 이월 항목이다.**

7. **[#57] 착수 전 단독 작업 — WebP 폴백 경로를 먼저 측정한다** ⚠️ **다른 작업보다 먼저**
   `web/src/lib/images/resize.ts`의 WebP→JPEG 폴백 분기는 **한 번도 실행된 적이 없다**. `canvas.toBlob`을 주입 가능하게 바꿔 폴백 분기를 **실제로 태워 보고** 관찰 결과를 Debug Log와 #57에 기록한 뒤 판단한다. **추측으로 고치지 않는다.**

8. **[#58][#63] 업로더 잔여 2건**
   - `#58`: `PhotoUploader`에 **애니메이션 WebP는 정지 이미지가 되고, 투명 PNG는 배경이 채워진다**는 안내를 올리기 전에 보이게 한다(기존 드롭존 보조문 옆).
   - `#63`: `onDrop` 첫 줄에 `if (disabled) return;`. ⚠️ **한 줄이지만 먼저 재현한다** — 재현 → 수정 → 재현 안 됨 확인 순서를 Debug Log에 남긴다. 재현되지 않으면 고치지 말고 그 사실을 #63에 적고 닫는다.

9. **[#59][#47-4] 이미지 조회 규칙을 코드와 계약에 동시에 박는다**
   - **정렬**: 이미지 조회는 반드시 **`order('sort_order').order('id')`**. tie-break가 정의돼 있지 않아(#47-2) 2차 키가 없으면 조회마다 순서가 달라진다. 이 정렬을 **강제하는 테스트 1건**을 남긴다(B9).
   - **대표 판별**: 카드는 **`is_cover`를 읽지 않는다.** `sort_order, id` 정렬의 **첫 행**이 대표다(`conventions.md` §10.1 "`is_cover`는 파생 결과" + 시드·레거시 행은 `is_cover=false`뿐일 수 있다).
   - **결정 기록**: "**대표 0장은 정상 상태이고, 읽는 쪽은 `sort_order` 첫 행을 대표로 본다**"를 §10에 명문화하고 #47-4를 닫는다.

10. **검증 — 실제로 돌려서 관찰한다** (B4)
    - dev 서버 → 브라우저로 `/`·`/search`.
    - **사진 있는 매물**(9.3으로 1건 등록)과 **사진 없는 매물**(시드 100건)이 **같은 화면에 동시에** 보이는 상태에서 확인 — 플레이스홀더와 실사진이 섞여도 그리드가 어긋나지 않아야 한다.
    - **1280 · 800 · 390px** 스크린샷 3장.
    - 관찰한 것을 Completion Notes에 **사실대로**(안 된 것은 안 됐다고).

> **FR11에 대해**: 이 스토리는 FR11 강제 지점을 **추가하지 않는다.** 사진 파일 URL은 FR11 대상이 아니다(`conventions.md` §6.1, 사용자 결정 2026-07-19). 매물의 목록 비노출은 `buyerListingsQuery`의 `status='on_sale'`이 이미 강제한다 — **새 앱 레벨 필터를 만들지 말 것.**

## Tasks / Subtasks

- [x] **Task 0 — 착수 전 이월 3건 (AC: 7, 8)** ⚠️ **카드를 건드리기 전에**
  - [x] `#57` 폴백 분기 주입 가능하게 → 실제 실행 → 기록 → 닫기
  - [x] `#63` 재현 시도 → (재현되면) 한 줄 수정 → 재현 안 됨 확인 → 닫기
  - [x] `#58` 안내 문구 추가 → 닫기
  - [x] `npm test` 통과, 신규 테스트는 일부러 깨서 red 확인 후 되돌림

- [x] **Task 1 — 대표 이미지 유틸 + Vitest (AC: 9)**
  - [x] `web/src/lib/images/coverImages.ts` — 입력: `listing_images` 행 배열(`{listing_id, storage_path, sort_order, id}`), 출력: `Map<listing_id, { coverPath, count }>`. 대표 = 그룹에서 **`sort_order` → `id`** 순 첫 행. `is_cover` 미사용
  - [x] `coverImages.test.ts`: ① `sort_order` 동률이면 `id` 낮은 쪽이 대표(#59) ② `is_cover=true`가 `sort_order=3`에 붙어 있어도 `sort_order=0` 행이 대표(#47-4) ③ 행 0건 매물은 Map에 없음
  - [x] 각 테스트 일부러 깨서 red 확인 후 되돌림

- [x] **Task 2 — 서버측 데이터 채우기 (AC: 2, 3, 5, 9)**
  - [x] `web/src/lib/listings.ts`에 `attachCoverImages(supabase, listings)` — `/search`·홈이 **같은 함수**를 쓴다(로직 이원화 금지)
    - ① 페이지의 `listing.id[]`로 `listing_images` 한 번에 조회(`.in('listing_id', ids)`, `.order('sort_order').order('id')`)
    - ② Task 1 유틸로 대표 경로 + 개수 산출
    - ③ **`getPublicUrl(LISTING_IMAGES_BUCKET, coverPath)`** 로 URL 조립(`@/lib/storage`) — 동기·왕복 없음
    - ④ `image_url`·`image_count`를 각 listing에 병합
  - [x] `search/page.tsx`·`page.tsx`에서 호출
  - [x] `docs/conventions.md` §10에 대표 판별 규칙 명문화(AC9) — **§10.2 신설**(읽는 쪽 규칙)

- [x] **Task 3 — ListingCard 레이아웃 B (AC: 1~5)**
  - [x] 루트 `<article className="relative">`, 내용은 `<Link>`, 찜 버튼은 Link 밖 `absolute`
  - [x] 이미지: `aspect-[5/3]` + `object-cover` + `loading="lazy"` + `decoding="async"` + **`onError` → 플레이스홀더**
  - [x] "N장" 배지(불투명 다크 pill 기본, blur는 `supports-[backdrop-filter]:`로만) · 대비값을 코드 주석에 기록
  - [x] 플레이스홀더 · 문구 대비 계산 후 색 결정 — **계산 결과 AA 통과라 토큰 유지**(아래 Debug Log 2)
  - [x] 정보 영역: 차량명 → meta(한 줄, `truncate`) → 가격 → (슬롯 2개는 값 없으면 렌더 안 함)
  - [x] 계약-외 값 방어(§4): `image_url === ''` → 플레이스홀더 · `image_count` 음수 → 0 하한

- [x] **Task 4 — 목록 그리드 D5 (AC: 6)**
  - [x] `/search`·홈의 `<ul className="flex flex-col gap-2">` → `ResponsiveGrid`(`@/components/ui/ResponsiveGrid`, **이미 존재 — 다시 만들지 말 것**)
  - [x] AI 채팅(`ChatAssistant.tsx`) 카드 목록은 **그대로 둔다**(좁은 대화 컬럼)

- [x] **Task 5 — 실브라우저 검증 (AC: 10)**
  - [x] 사진 있는 매물 1건을 `/sell`로 실제 등록
  - [x] 브라우저(로컬 크로미움)로 `/`·`/search`, 1280·800·390px 스크린샷 3장
  - [x] 사진 있는 카드 + 플레이스홀더 카드가 **한 화면에 섞인 상태** 확인 (96건 중 사진 6 / 플레이스홀더 90)
  - [x] 결과 기록, dev 서버 정리

### Review Findings

_코드리뷰 2026-07-19 · 범위 `110ac40..HEAD`(스토리 9.0+9.4 합본) · 3레이어 병렬(Blind Hunter / Edge Case Hunter / Acceptance Auditor) + 전 항목 실측 검증._

**결정 4건 모두 해소됨 (2026-07-19, 사용자 판단).** 각 항목의 결론과 그에 따라 생긴 patch는 아래에 반영.

- [x] [Review][Decision] **D1 관리자 삭제 순서 + §10.1 자기모순 → (a) 순서를 뒤집는다.** §10.1에 "이 순서는 더 이상 안전을 좌우하지 않는다"(:236)와 "①이 실패하면 ②를 하지 않는다 → 영구 고아"(:240)가 같은 절에 공존하고, `ListingAdminActions.tsx:48-71`은 철회된 쪽을 구현하고 있었다. **근거:** 그 순서 규칙은 *비공개 버킷*(파일 가시성이 DB 행에 매달림) 때문에 존재했고, 9.0의 공개 전환 + 경로 기반 스토리지 정책이 그 전제를 없앴다 — 이제 행이 없어도 소유자·관리자가 객체를 보고 지울 수 있으므로 고아는 회수 가능하다. 반면 현 순서는 매물 DELETE 실패 시 **사진만 영구 소실**시키는데 이건 회수 불가 = 더 나쁜 실패. → 매물 먼저 삭제 후 사진 정리로 전환 + §10.1 모순 제거.
- [x] [Review][Decision] **D2 시드 스크립트 귀속 → 대장에 등재만 하고 9.7은 `backlog` 유지** (추천안 (a)에서 하향 조정). **조사 결과 인계 흔적이 부분적으로 있었다** — 커밋 `9ecb69f` 메시지가 *"9.7 전량 시딩과 시드 2회 재실행 검증(#27)은 여전히 9.7의 일"* 이라고 경계를 명시했고, Discord(2026-07-18 23:53)에도 "소량은 지금, 전량·2회 검증은 9.7" 합의가 있다. 즉 **의도는 기록됐고 장부에만 안 들어왔다.** 시점 근거로도 급하지 않다: 전량 시딩이 필요한 시점은 9.7 자신이 exit-gate(SM-A·CM-A·G3 검증 스토리가 9.7로 지정)이고 제출·데모 마감일자는 어느 문서에도 없다. → 9.7 스토리 파일을 미리 만들지 않고(BMAD 흐름상 create-story 몫), **tech-debt에 "스크립트가 존재하고 5건/10장까지 부분 실행됨 + 잔여 5건은 9.7"** 을 등재. 잔여분은 9.7 생성 시 인수조건으로 심는다(B5).
- [x] [Review][Decision] **D3 AI 카드 "사진 준비중" 거짓 표시 → (a) 지금은 두고 대장에만 등재.** 사용자 판단: 현재 AI 검색 사용자가 없어 실해가 없음. 계획 부재가 아니라 **미착수**임을 확인했다 — `9-6-ai-응답-카드-사진: backlog`가 api에 사진 경로를 붙이는 스토리이고, 9.7 시드가 9.6보다 먼저 실행되는 바람에 생긴 일시적 창(窓)이다.
- [x] [Review][Decision] **D4 시드 라이선스 필터 NC/ND 통과 → (c) 대장 등재만.** **실측 결과 현재 위반 0건** — 업로드된 10장은 `CC BY-SA 4.0` 8 · `CC0` 1 · `CC BY 2.0` 1로 NC·ND가 하나도 들어오지 않았다(원격 DB 조회). 필터 버그는 실사고가 아니라 잠재 결함. 사용자 판단: 과제용이고 배포도 내릴 예정이라 등재만. ⚠️ **함께 발견된 것:** 10장 중 **9장이 저작자 표시 의무** 대상인데 `credit`(저작자·라이선스·원본링크)을 DB에 저장만 하고 화면에 표시하는 코드가 없다(`coverImages.ts:16`이 일부러 안 받음). 이것도 같은 대장 항목에 함께 적는다.

- [x] [Review][Patch] **(D1 결정에서 파생)** 관리자 삭제 순서를 뒤집는다 — `listings` DELETE 성공 후 사진 정리로 변경(정리 실패는 회수 가능한 고아이므로 삭제를 막지 않는다). 아울러 `conventions.md` §10.1의 모순 문장(:240 "①이 실패하면 ②를 하지 않는다") 제거 [web/src/app/(admin)/admin/listings/ListingAdminActions.tsx:48-71 · docs/conventions.md:236-240]
- [x] [Review][Patch] **(D2 결정에서 파생)** `tech-debt.md`에 시드 스크립트 항목 신설 — 존재·부분 실행(매물 5건/사진 10장)·버그 3건·잔여 5건(전량 시딩 · 시드 2회 재실행 검증 #27 · `status <> 'sold'` 마이그 #54 · credit 표시 · 라이선스 필터 수정)이 9.7 몫임을 명시. 9.7 스토리 생성 시 인수조건으로 심을 것(B5) [docs/tech-debt.md]
- [x] [Review][Patch] **(D3 결정에서 파생)** `tech-debt.md`에 등재 — AI 응답 카드가 사진 있는 매물에도 "사진 준비중"을 표시(9.6 착수까지의 일시적 창). 트리거: 9.6 [docs/tech-debt.md · web/src/components/ai/ChatAssistant.tsx:138]
- [x] [Review][Patch] **(D4 결정에서 파생)** `tech-debt.md`에 등재 2건 — ① 시드 라이선스 필터가 NC/ND를 통과시킴(현재 위반 0건, 잠재 결함) ② **CC BY/BY-SA 9장의 저작자 표시 의무 미이행** — `credit` 저장은 하나 렌더링 없음 [docs/tech-debt.md · scripts/seed_listing_photos.py:66,106 · web/src/lib/images/coverImages.ts:16]
- [x] [Review][Patch] 찜(♡) 버튼이 차량명 위에 겹쳐 그려짐 — 정보영역에 우측 예약 폭 없음 (D5 "레이아웃 어긋남 = 절대 금기") [web/src/components/listings/ListingCard.tsx:42,47,80]
- [x] [Review][Patch] 이미지 로드 실패 시 "N장" 배지까지 사라짐(5장 매물이 0장이라 광고) + `failed`가 `url` 변경 시 리셋되지 않아 한 번 실패하면 영구 플레이스홀더 [web/src/components/listings/ListingCardImage.tsx:23,32-63]
- [x] [Review][Patch] 편집화면 썸네일에 `onError` 없음 — `getPublicUrl`이 항상 문자열을 반환해 "미리보기 없음" 폴백이 죽은 코드가 됨(9.0의 교훈이 카드에만 적용됨) [web/src/app/(user)/sell/PhotoUploader.tsx:203]
- [x] [Review][Patch] `attachCoverImages`가 전체 id를 단일 `.in()`에 실음 + `/search`에 `.limit()` 없음 → 카탈로그가 커지면 414로 **전 카드 사진이 한꺼번에** 소실(무음) [web/src/lib/listings.ts:62-71]
- [x] [Review][Patch] 시드: `sort_order`·`is_cover`를 `enumerate` 인덱스로 매김 → 실패 시 0번 구멍 + `is_cover=true` 0건 (§10.1 "구멍 금지" 위반) [scripts/seed_listing_photos.py:216,238-243]
- [x] [Review][Patch] 시드: 업로드 성공 후 행 INSERT 실패 시 객체 정리 없음 → 영구 고아 (`photo-sync.ts:207-213`은 정리함) [scripts/seed_listing_photos.py:238-251]
- [x] [Review][Patch] 시드: "이미 사진 있음" 사전조회가 `limit`/`Range` 없는 PostgREST 읽기 → 1000행 초과 시 조용히 잘려 중복 삽입·`is_cover` 유니크 충돌 [scripts/seed_listing_photos.py:168-172]
- [x] [Review][Patch] `update storage.buckets ... where id=...`는 버킷 행이 없으면 **0행 무음 성공** — "on conflict로 조용히 무효"를 "부재 시 조용히 무효"로 바꾼 것뿐 [supabase/migrations/0014_listing_images_public_bucket.sql:27-32]
- [x] [Review][Patch] 스토리지 RLS가 `public.is_admin()`을 감싸지 않고 직접 호출 → LIST마다 **행당** `profiles` 조회 [supabase/migrations/0014_listing_images_public_bucket.sql:51,68]
- [x] [Review][Patch] `conventions.md` §10이 "api는 URL을 만들지 않는다"를 이 테스트가 지킨다고 적었으나 마커가 `["/sign/","token="]`뿐이라 **공개 URL을 못 잡음**(빈 가드) [api/tests/test_storage_signed_url_contract.py:28]
- [x] [Review][Patch] `encodeWithFallback`의 `toBlob` 콜백에 타임아웃 없음 → 미호출 시 업로드 루프가 영구 대기, 폼이 잠긴 채 에러도 안 남 [web/src/lib/images/resize.ts:44-52]
- [x] [Review][Patch] `<ul>/<li>` → `ResponsiveGrid`(plain `<div>`) 전환으로 리스트 시맨틱 소실 — 스크린리더가 항목 수·목록 탐색을 잃음 [web/src/components/ui/ResponsiveGrid.tsx:24]
- [x] [Review][Patch] "N장" 배지가 `bg-black/80`(반투명)인데 AC2는 **불투명** pill을 요구하고 Task 체크박스는 AC 문구대로 `[x]` 처리됨 [web/src/components/listings/ListingCardImage.tsx:59]
- [x] [Review][Patch] §6 FR11 강제 지점 목록에 이미지 축이 하나도 없음 — 9.0이 `storage.objects`를 지우고 `listing_images` RLS는 애초에 미등재인데 9.4가 그 테이블 조회 경로를 2개 화면에 새로 염 (규칙7의 유일한 실패 모드) [docs/conventions.md §6]
- [x] [Review][Patch] 스토리 9.0 파일이 `Status: ready-for-dev` + 태스크 전부 미체크인데 완료 노트·파일 목록은 채워져 있고 sprint-status는 `review` [_bmad-output/implementation-artifacts/9-0-공개-버킷-전환-서명-url-제거.md:7]
- [x] [Review][Patch] 대장 불일치 — 스토리 9.0은 tech-debt #44를 해소했다고 적었으나 `tech-debt.md:354`의 #44는 **여전히 미해소**(✅ 없음, 해소란은 제안만) [docs/tech-debt.md:354]

- [x] [Review][Defer] **→ 대장 #72** `getPublicUrl` web/dart 미러가 서로 다른 인코딩 기법을 쓰고 드리프트를 잡는 계약 테스트가 없음 [web/src/lib/storage/index.ts:15 · app/lib/core/supabase/storage_helper.dart] — 대장 #72로 이관(deferred-work.md는 2026-07-15 동결 — 열린 일은 tech-debt.md 하나)
- [x] [Review][Defer] **→ 대장 #73** 3중 무음 폴백(조회 실패·`getPublicUrl` 미검증·`onError`)으로 **전면 장애와 "사진 없는 매물"이 화면상 구별 불가** [web/src/lib/listings.ts:73 · ListingCardImage.tsx] — 대장 #73으로 이관
- [x] [Review][Defer] **→ 대장 #74** Story 9.0 AC9의 브라우저 검증 미완(로그인 세션 필요) — 관리자 삭제 후 Storage 객체 소멸이 관찰된 적 없는데 #66은 ✅로 닫힘 — 대장 #74로 이관 — 🔴 사용자가 직접 확인해야 하는 항목
- [x] [Review][Defer] **→ 대장 #75** 저장형 XSS 근거가 오리진을 잘못 지목(앱 도메인 아님 — Supabase 프로젝트 호스트) [supabase/migrations/0014_listing_images_public_bucket.sql 헤더 · docs/conventions.md §10] — 대장 #75로 이관

#### 🔴 patch 적용 중 **새로 발견**된 결함 (실브라우저 검증이 잡았다)

- [x] [Review][Patch] **`onError`가 하이드레이션 전 실패를 못 잡아 AC3이 실제로는 안 지켜지고 있었다** [web/src/components/listings/ListingCardImage.tsx]
  - **어떻게 나왔나:** 위 "배지가 사라진다" 수정(FIX 2)을 **확인하려고** 이미지 호스트를 블랙홀로 돌려 렌더했다. 배지는 살아남았는데(수정 성공) **플레이스홀더 대신 깨진 이미지+alt 텍스트가 떴다** — AC3이 명시적으로 금지한 화면이다.
  - **원인:** 서버가 그린 `<img>`는 HTML 도착 즉시 로드를 시작한다. **하이드레이션 전에** 에러가 나면 React가 이벤트를 재생하지 않아 `onError`가 영영 발화하지 않는다. **파일 없음(404)이 정확히 그렇게 빨리 실패**하므로, 9.4가 "9.0 이후 깨진 이미지를 막는 **유일한** 장치"라고 못박은 방어가 **가장 흔한 실패에는 안 듣고 있었다.**
  - **수정:** ref 콜백에서 `img.complete && img.naturalWidth === 0`을 확인해 이미 끝난 실패를 잡는다(`onError`와 2겹).
  - **검증(B4 red→green):** 같은 조건 재현 → 수정 전 깨진 이미지(red) / 수정 후 "사진 준비중" + "1장" 배지 유지(green). 정상 경로도 재확인해 지연 로드 이미지 오탐 없음.
  - **계약에 반영:** `conventions.md` §10.2에 **"2겹 필수 + 재현 명령"** 을 명문화. 9.5·9.6이 `onError`만 복사하지 않도록 명시(B9).
  - ⚠️ **교훈:** 세 리뷰 레이어 중 아무도 못 잡았다. 코드상으로는 `onError`가 **분명히 있었기** 때문이다 — **"있는 것"과 "듣는 것"의 차이**(B4)는 읽어서는 안 보이고 돌려야 보인다.

**위양성으로 기각(2건, 실측 반증):** ① "관리자가 `listing_images`를 못 읽어 정리에 실패한다" → `0012:115` `listing_images_select_admin`이 무조건 허용이라 sold 포함 정상 조회됨. ② "tech-debt #44가 0014를 근거로 닫혔다" → #44는 닫히지 않음(불일치는 스토리 쪽이며 별도 patch로 등재).

## Dev Notes

### 0. 범위 경계 — 먼저 읽을 것

**안:** `ListingCard.tsx` 렌더 · `/search`·홈의 조회와 그리드 · `lib/listings.ts`·`lib/images/coverImages.ts` · `PhotoUploader` 이월 3건 · `conventions.md` §10 · `tech-debt.md`.

**밖(하지 말 것):**
- **상세 갤러리 → 9.5** · **AI 응답 카드 사진(api 수정) → 9.6** · **시드 사진 → 9.7**
- **찜 동작·신뢰 뱃지 내용·옵션 칩 내용 → Epic 10** (여기선 **슬롯 자리**만)
- **`view_count` → Epic 11** · **Flutter 앱 카드 → 16.2**
- **DB 마이그레이션 없음** — `listing_images`는 0012/0013, 버킷은 0014로 이미 있다.
- **ListingCard 계약 변경 없음** — `image_url`·`image_count` 자리는 8.3이 확보했다. 타입을 안 고치므로 **§4.1 락스텝 4곳 동시 갱신은 불필요**(값만 채운다).

### 1. 지금 코드 상태 (읽고 시작할 것)

| 파일 | 현재 |
|---|---|
| `web/src/components/listings/ListingCard.tsx` (52줄) | `image_url`·`image_count` **자리는 있으나** 렌더 본문이 **이미지를 전혀 안 그린다.** 텍스트 3줄 + zinc 유틸, 8.1 토큰 미사용 |
| `web/src/app/(user)/search/page.tsx` | 서버 컴포넌트. `buyerListingsQuery(...)` — **`listing_images` 조인 없음**, `<ul flex-col>` 1열. **limit·페이지네이션 없음**(시드 100건이 한 번에) |
| `web/src/app/page.tsx` | 홈 미리보기. 같은 쿼리 + `.limit(PREVIEW_COUNT)`, 역시 `<ul>` 1열 |
| `web/src/lib/listings.ts` (32줄) | `buyerListingsQuery` — FR11 단일 출처 |
| `web/src/lib/storage/index.ts` | **`getPublicUrl(bucket, path)` 하나뿐**(9.0). 동기·실패 없음·서버/브라우저 공용 |
| `web/src/components/ui/ResponsiveGrid.tsx` | `grid-cols-1 min-[640px]:grid-cols-2 min-[1100px]:grid-cols-4` — **이미 있다** |
| `web/src/components/ui/Skeleton.tsx` | `CardSkeleton`이 이미 `aspect-[5/3]` + `bg-placeholder-bg` — **카드 이미지 자리 규격의 선례**, 같은 값을 쓸 것 |
| `web/src/app/globals.css` | 색·타이포·radius·shadow 토큰 전부 정의됨. **새 토큰 추가 금지** |
| 관리자 화면 | **사진을 렌더하지 않는다**(grep 실측). 관리자 사진 표시는 Story 15.1 몫 |

### 2. 데이터 흐름 (입력에서 화면까지)

```
[서버 컴포넌트: /search · /]
  ① buyerListingsQuery → listings 행 N개 (status='on_sale'만, FR11)
  ② listing_images.select().in('listing_id', ids).order('sort_order').order('id')
  ③ coverImages(rows) → Map{ listing_id → { coverPath, count } }
  ④ getPublicUrl(bucket, coverPath) → 문자열 조립 (왕복 0회, 만료 없음)
  ⑤ image_url·image_count 병합 → ListingCard로 전달
[브라우저]
  ⑥ <img loading="lazy">가 공개 URL을 직접 로드
  ⑦ null / '' / onError → "사진 준비중" 플레이스홀더
```

**9.0 이전과 달라진 점**: 서명 발급 왕복·만료·"서버에서만 발급 가능" 제약이 전부 없다. 대신 **파일이 없어도 URL이 나오므로** ⑦의 `onError`가 유일한 안전망이다.

### 3. `next/image`를 쓸 것인가 (짧은 결정 — 판단하고 기록만)

**권고: 평범한 `<img loading="lazy" decoding="async">`.**
- 저장본이 이미 작다 — 9.3이 업로드 전 **긴 변 ≤1600px · WebP · q0.82**로 다운스케일한다(실측 196~205KB). 최적화기가 더 줄일 여지가 적다.
- `next.config.ts`에 `images.remotePatterns`가 **없다**(실측) → `next/image`를 쓰려면 설정 추가가 선행이다.
- 공개 URL은 고정이라 브라우저·CDN 캐시가 그대로 먹는다.

`next/image`를 택해도 이제 무리는 없다(URL이 고정이라 최적화 캐시가 맞는다). **어느 쪽이든 이유를 Completion Notes에 한 줄 남긴다.**

> 9.7 시드 사진은 Wikimedia 원본을 올릴 수 있어 9.3의 리사이즈를 안 거친다 → 목록에서 큰 파일이 관찰되면 **여기서 고치지 말고** 사실만 대장에 등재해 9.7이 받게 한다.

### 4. 비용 — 알고 넘어갈 것 (고치지 말 것)

`/search`는 limit이 없어 on_sale 전체(100건+)를 한 번에 가져온다. 이미지 조회가 **쿼리 1회** 늘어난다(서명 왕복은 이제 0회). 브라우저 다운로드는 `loading="lazy"`가 화면에 들어온 것만 받는다. **페이지네이션은 범위 밖** — 관찰된 지연이 실제 문제면 사실만 대장에 등재하고 넘긴다(A3).

### 5. 계약 — 반드시 지킬 값 (정본은 문서, 여기 복사본 아님)

- **ListingCard 필드·정규화**: `docs/conventions.md` §4 (`image_url === ''`도 플레이스홀더, `image_count` 음수는 0 하한)
- **이미지 스토리지**: §10(공개 버킷·`getPublicUrl`) / §10.1(대표 = `sort_order` 0번, `is_cover`는 파생)
- **FR11**: §6 + **§6.1(사진 URL은 대상 아님)**
- **디자인 값**: `DESIGN.md` frontmatter — 값은 이미 `globals.css` 토큰이니 **CSS 변수를 쓰고 hex를 하드코딩하지 않는다**

### 6. 테스트 표준

- **web 표준은 E2E 우선**이지만 — **실측: 이 리포에 Playwright 설정도 `e2e/`도 없다.** "E2E 테스트를 작성한다"는 이 스토리에서 성립하지 않는다. **AC10의 실브라우저 관찰 + 스크린샷**이 검증이다.
- **Vitest는 `src/**/*.test.ts`만**(`.tsx` 제외 — 컴포넌트를 여기서 테스트하지 않는 것이 의도된 표준). 그래서 Task 1 유틸을 컴포넌트 밖으로 뺐다.
- **"만들었다"가 아니라 "잡는다"가 완료다**(B4): 새 테스트는 한 번씩 일부러 깨서 red를 본 뒤 되돌려 green을 확인한다.

### 7. 선행 스토리에서 온 함정

- **9.3 코드리뷰 2차**: "대표 대상을 두 번 계산하면 화면과 DB가 갈린다." → **대표는 한 군데(Task 1 유틸)에서만 정한다.** 카드가 따로 `is_cover`를 보거나 배열 0번을 다시 고르지 않는다.
- **Epic 8 회고 A5**: *"'무서운 발견'일수록 산출물에 박기 전에 재라."* AC2·AC3의 대비값, AC7·AC8의 재현이 전부 이 규칙이다.
- **Next 16 함정**: 훈련데이터의 Next와 다르다. web 작업 전 `web/AGENTS.md`가 지시하는 대로 `node_modules/next/dist/docs/`를 먼저 읽는다. Tailwind는 **v4**(CSS `@theme`).

### Project Structure Notes

- 신규 파일 2개: `web/src/lib/images/coverImages.ts` + `.test.ts`. (`lib/images/`는 9.3이 만든 디렉터리라 자연스럽다. `lib/listings.ts`를 디렉터리로 쪼개지 말 것 — A3)
- 브랜치: `develop`에서 작업. `main` 병합은 사용자 승인 시에만.

### References

- [Source: _bmad-output/planning-artifacts/epics-increment-2026-07-12.md#Story 9.4] — AC 원문, FR27·FR29·FR32, NFR7
- [Source: _bmad-output/planning-artifacts/architecture-increment-2026-07-12.md#ADR-IMG-02] — 공개 버킷 + 고정 URL(ADR-IMG-01 대체), CR3·I7·I8·I14
- [Source: _bmad-output/implementation-artifacts/9-0-공개-버킷-전환-서명-url-제거.md] — 직전 스토리, 전환 근거와 실측
- [Source: docs/conventions.md#4·6·6.1·10·10.1] — 계약·정규화·FR11 범위·이미지 스토리지
- [Source: docs/tech-debt.md#47·57·58·59·63] — 이 스토리가 지목된 항목
- [Source: ux-designs/.../DESIGN.md · .decision-log.md#D3·D5 · review-accessibility.md] — 레이아웃 B·토큰·대비 지적
- [Source: _bmad-output/project-context.md#규칙5·7·9·11·12·13]

## Dev Agent Record

### Agent Model Used

claude-opus-4-8[1m] (Claude Code)

### Debug Log References

#### 1. #57 실측 — `canvas.toBlob`의 미지원 MIME 동작 (2026-07-19, 로컬 HeadlessChrome 151)

스토리가 "추측으로 고치지 말고 먼저 재라"고 지정한 항목. 실제 크로미움에서 캔버스를 만들어 직접 태웠다.

| 요청 MIME | `blob.type` | 앞 4바이트 | 판정 |
|---|---|---|---|
| `image/webp` | `image/webp` | `52 49 46 46` (RIFF) | 지원 |
| `image/jpeg` | `image/jpeg` | — | 지원 |
| `image/tiff` · `image/avif` · `not/a-real-mime` | **`image/png`** | **`89 50 4e 47`** | **null이 아니라 PNG 대체** |

→ **코드리뷰 2차의 의심이 맞았다.** 기존 폴백은 `if (blob) return blob;`이라 **미지원 브라우저에서도 첫 분기가 항상 참**이 되어 JPEG 폴백이 한 번도 실행될 수 없었다. 결과적으로 PNG가 저장되고 `extensionFor`가 `.jpg` 이름을 붙였다(확장자↔내용 불일치).

용량 실측(1600×1067 사진형 그라디언트+노이즈): **WebP 553KB · JPEG 503KB · PNG 4,827KB(약 9배)** — 버킷 5MB 상한에 닿고 AC4 저장본 규격 보장이 깨진다.

**처방:** `encodeWithFallback(toBlob)`으로 분리해 주입 가능하게 하고, 판정을 `blob !== null` → **`blob.type === 요청한 타입`**으로 교체(null을 주는 브라우저와 PNG를 주는 브라우저를 둘 다 잡는다). 둘 다 실패하면 규격 밖 파일을 조용히 저장하지 않고 throw.

**red 확인:** 옛 코드(`if (webp) return webp;`)로 되돌리자 신규 5건 중 2건이 실패(`expected 'image/png' to be 'image/jpeg'`) → 되돌려 11건 green.

#### 2. 대비 실측 — 스토리의 우려가 빗나갔다 (AC2·AC3)

스토리는 플레이스홀더 문구가 *"약 4.4:1로 AA 미달 가능성이 높다(개략 계산, 실측 필요)"* 며 미달이면 `text-ink-secondary`로 올리라고 지시했다. WCAG 상대휘도 공식으로 실제 계산한 결과:

| 대상 | 값 | 판정 |
|---|---|---|
| 라이트: `ink-muted` #676D69 on `placeholder-bg` #F1EFE9 | **4.60:1** | ✅ AA 통과 |
| 다크: `ink-muted` #A6A196 on `placeholder-bg` #2A2924 | **5.66:1** | ✅ AA 통과 |
| "N장" 배지 최악 조건(완전 흰 사진 위, blur 미지원): 흰 글자 on `black/80` 합성 #333333 | **12.63:1** | ✅ 여유 통과 |

→ **미달이 아니므로 색을 올리지 않았다**(토큰 변경 0). 지시를 그대로 따랐다면 근거 없이 색을 바꿀 뻔했다 — Epic 8 회고 A5("무서운 발견일수록 산출물에 박기 전에 재라")가 반대 방향으로도 적용된 사례다.

참고로 가격(`price-emphasis` #C0730F on 흰 배경)은 **3.68:1**로 본문 기준에는 미달이지만 26px/800 = WCAG 큰 텍스트(3:1)라 통과다. 이건 8.1이 정한 토큰이고 이 스토리에서 바꾸지 않았다.

#### 3. #63 재현 → 수정 → 재현 안 됨 (실브라우저)

코드를 읽은 추론이 아니라 실제로 재현시켰다. 로컬 dev 서버 + 크로미움: `/sell`에 사진 2장 → ①썸네일 0에서 `dragstart` → ②"매물 등록" 클릭 → ③드롭존 `button.disabled=true`가 되는 순간을 폴링으로 포착 → ④썸네일 1에 `drop`.

- **수정 전:** `[…46c9a17ecd, …4d2c69d9c6]` → `[…4d2c69d9c6, …46c9a17ecd]` — **제출 중인데 순서가 바뀐다(재현됨)**
- **수정 후:** 같은 절차에서 순서 그대로 — **재현 안 됨**

타이밍 창이 짧아 node↔브라우저 왕복으로는 놓쳤다 — 4단계를 전부 브라우저 안에서 한 번에 돌려야 잡혔다.

#### 4. AC10 실브라우저 관찰 (3 뷰포트, `/search` 96건)

| 항목 | 1280px | 800px | 390px |
|---|---|---|---|
| `grid-template-columns` 열 수 | **4** | **2** | **1** |
| 카드 수 (사진 있음 / 플레이스홀더) | 96 (6 / 90) | 동일 | 동일 |
| "N장" 배지 렌더 | 6 | 6 | 6 |
| meta 실제 렌더 줄 수 | **1줄** | 1줄 | 1줄 |
| 차량명 / 가격 font-size | 16px / **26px** | 동일 | 동일 |
| 찜 버튼 | 44×44 · `disabled` · `aria-hidden="true"` · `tabIndex=-1` | 동일 | 동일 |
| `body.scrollWidth > clientWidth`(가로 스크롤) | **false** | false | false |

같은 행 카드의 `top` 좌표가 행마다 하나의 값으로만 모였다(어긋남 없음). 사진 카드와 플레이스홀더 카드가 **한 화면에 섞인 상태**로 확인했다.

**관찰 중 확인한 것 하나:** `repro63` 매물 2건이 사진을 올렸는데 플레이스홀더로 보여 코드 결함을 의심했으나, DB를 직접 조회한 결과 그 매물들은 **실제로 `listing_images` 0행**이었다(합성 드래그 이벤트가 개입한 테스트의 부작용). 정상 경로로 등록한 매물은 1행이 붙어 사진이 정상 렌더됐다 — **카드가 옳게 그린 것이다.**

### Completion Notes List

- **`next/image`가 아니라 평범한 `<img loading="lazy" decoding="async">`를 선택했다.** 이유 한 줄: 저장본이 이미 작고(9.3이 긴 변 ≤1600px·WebP·q0.82로 줄인다, 실측 196~205KB), `next.config.ts`에 `images.remotePatterns`가 없어 설정 추가가 선행이며, 공개 URL이 고정이라 브라우저·CDN 캐시가 그대로 먹는다. (스토리 §3의 권고와 같은 결론)
- **사진 영역만 클라이언트 컴포넌트로 떼어냈다**(`ListingCardImage.tsx`). `onError` 폴백에 상태가 필요한데 카드 전체를 클라이언트로 만들면 목록 96건이 전부 하이드레이션된다. 나머지 카드는 서버 렌더 그대로다.
- **②·⑥ 슬롯은 표시 로직을 쓰지 않고 주석 자리만 남겼다.** AC1이 "값이 없으면 아무것도 렌더하지 않는다"이고 값은 Epic 10이 채우므로, 오늘 올바른 동작은 "렌더 안 함"이다. 안 쓰일 분기를 미리 만들지 않았다(A2).
- **meta에서 연료를 뺐다.** AC1은 `주행·연료·지역`을 요구하지만 `fuel`은 `ListingCard` 계약(conventions §4)에 없는 필드이고, 계약 변경(§4.1 락스텝 4곳)은 이 스토리 밖으로 명시돼 있다. 대신 `주행 · 지역 · 판매자`를 한 줄로 렌더했다. → **대장 등재 필요**(아래).
- **`/search`·홈의 본문 폭을 넓혔다**(`max-w-3xl`→`max-w-6xl`, 홈은 `max-w-2xl`→`max-w-6xl`). D5 브레이크포인트는 뷰포트 기준이라 본문이 좁으면 1280px에서 4열 규칙이 걸려도 칸이 찌그러진다. 폭을 안 열면 AC6이 화면상 성립하지 않는다.
- **정렬 규칙을 두 층에 박았다.** DB 쿼리(`attachCoverImages`의 `.order('sort_order').order('id')`)와 순수 함수(`coverImages`가 입력 순서를 믿지 않음) 양쪽. 한쪽이 빠져도 화면 결과가 흔들리지 않는다(B9).
- **대장 처리:** `#57`·`#58`·`#63` **닫음**, `#47`의 4번 항목("대표 0장 허용") **닫음**, `#59`는 **9.4 몫만 이행하고 열어 둠**(9.5 상세 갤러리가 남았다). 요약 대시보드 🟢 행 20→17 갱신.
- **새로 등재해야 할 것(다음 스토리 인수조건으로 심을 자리 포함):** `ListingCard` 계약에 `fuel`이 없어 AC1의 meta가 축소 렌더된다 — 필드를 늘리려면 §4.1 락스텝 4곳 동시 갱신이 필요하므로 **Epic 10(카드 값 채우는 에픽)에서 처리**. 그리고 이 리포에는 여전히 Playwright 설정도 `e2e/`도 없어 이번 실브라우저 검증·#63 재현 스크립트가 **상시 회귀로 남지 않는다**(#29와 같은 구멍) — 재현 스크립트는 레포 밖 scratchpad에 있다.
- **검증 실행 결과:** Vitest 82건 통과(8파일) · ESLint 0 · `tsc --noEmit` 0. dev 서버는 검증 후 정리했다.

### File List

**신규**
- `web/src/lib/images/coverImages.ts`
- `web/src/lib/images/coverImages.test.ts`
- `web/src/components/listings/ListingCardImage.tsx`

**수정**
- `web/src/lib/images/resize.ts` (#57 — `encodeWithFallback` 분리·판정 교체)
- `web/src/lib/images/resize.test.ts` (#57 — 폴백 5건 추가)
- `web/src/app/(user)/sell/PhotoUploader.tsx` (#58 안내 문구 · #63 `onDrop` 가드)
- `web/src/lib/listings.ts` (`attachCoverImages` 추가)
- `web/src/app/(user)/search/page.tsx` (대표사진 병합 · `ResponsiveGrid` · 본문 폭)
- `web/src/app/page.tsx` (동일)
- `web/src/components/listings/ListingCard.tsx` (레이아웃 B 전면 개정)
- `docs/conventions.md` (§10.2 신설 — 읽는 쪽 규칙)
- `docs/tech-debt.md` (#47-4·#57·#58·#63 닫음 · #59 부분 이행 · 대시보드)
- `_bmad-output/implementation-artifacts/sprint-status.yaml`
- `_bmad-output/implementation-artifacts/9-4-매물-카드-레이아웃-b-대표사진-n장-플레이스홀더-위계.md`

## Change Log

- 2026-07-19 — Task 0: 이월 3건 해소(#57 실측 후 폴백 판정 교체 · #58 사전 고지 · #63 재현 후 가드). 커밋 `0f24a7b`.
- 2026-07-19 — Task 1~5: 대표사진 유틸·서버 병합·레이아웃 B 카드·D5 그리드·실브라우저 3뷰포트 검증. `conventions.md §10.2` 신설.
