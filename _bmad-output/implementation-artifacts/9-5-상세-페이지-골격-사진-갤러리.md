---
baseline_commit: e88572617b455426d472838cad860c86772d0423
---

# Story 9.5: 상세 페이지 골격 + 사진 갤러리

Status: done

## Story

As a 구매자,
I want 매물 상세에서 사진 갤러리를 넘겨보고 정보가 신뢰→차량→옵션→판매자 순으로 정리되길,
so that 구매 결정을 순서대로 신뢰 있게 내린다.

**범위: web only** (`web/src/app/(user)/listings/[id]/` + 새 갤러리 컴포넌트 + 이미지 조회 유틸 1개). api·app(Flutter)·DB 마이그레이션은 이 스토리 밖 — [Dev Notes §0](#0-범위-경계--먼저-읽을-것).

## Acceptance Criteria

원문 출처: `_bmad-output/planning-artifacts/epics-increment-2026-07-12.md` Story 9.5 (FR11·FR28·FR58, UX-DR12/20/24, D9·D5).
출처별 분류:
- **에픽 원문 유래**: AC1(섹션 순서) · AC2(갤러리) · AC5(15필드 재배치) · AC6(404) · AC7(sticky CTA)
- **대장 이월**(`docs/tech-debt.md`가 이 스토리를 예약·트리거로 지목 — CLAUDE.md B5): **AC3**(#59 정렬)
- **계약이 이 스토리를 콕 집어 요구**: AC4 (`conventions.md` §10.2 "9.5·9.6도 같은 2겹을 둔다")
- **회귀·검증 DoD**: AC9(D5) · AC10(관리자 무회귀) · AC11(실브라우저)
- **제외**: AC8(저작자 표시) — 사용자 결정 2026-07-20. 번호는 참조 어긋남을 막기 위해 비워 둔다.

---

1. **섹션 골격 = 신뢰정보 → 차량정보 → 옵션 → 판매자정보** — 위에서 아래로 정확히 이 순서의 `<section>` 4개로 페이지를 세운다.
   - **① 신뢰정보**: Epic 10.2가 채울 **빈 슬롯**. 지금은 뱃지 값이 없으므로 **아무것도 렌더하지 않는다**(빈 제목·빈 테두리·"준비중" 문구 금지). 슬롯의 자리만 코드 구조로 존재하면 된다.
   - **② 차량정보**: 지금 있는 필드 표를 여기로 옮긴다(AC5).
   - **③ 옵션**: 지금 있는 옵션 칩 목록을 여기로 옮긴다. 카테고리 분류·희소옵션 강조는 **Epic 10.3/10.4의 몫이며 여기서 만들지 않는다.**
   - **④ 판매자정보**: Epic 10.6이 채울 **빈 슬롯**(①과 동일 규칙).
   - ⚠️ 목업 `ux-designs/.../mockups/detail-1.html`은 **옛 순서(옵션→차량정보)**다. 목업이 아니라 이 AC가 정답이다.
   (UX-DR24, D9, `epics-increment-2026-07-12.md:539`)

2. **사진 갤러리** — 사진이 1장 이상이면 갤러리를, 0장이면 5:3 "사진 준비중" 플레이스홀더를 그린다.
   - 구성: **대표 사진 5:3** + **하단 썸네일 스트립** + **"1/N" 카운터(대표 사진 우하단)** + **좌우 화살표**.
   - 썸네일 클릭·화살표 클릭으로 대표 사진이 바뀐다. 키보드로도 조작 가능해야 한다(화살표 버튼은 진짜 `<button>`, `aria-label` 한국어).
   - 첫 화면에 **대표 1장 + 썸네일들만** 로드한다. 큰 사진을 N장 미리 받지 않는다(`loading="lazy"`, `decoding="async"`, `aspect-ratio`로 레이아웃 시프트 0). (NFR7)
   - 크롭은 클라 렌더 크롭(`object-fit: cover`, 중앙). **저장본을 다시 만들지 않는다**(I14) — 9.3이 업로드 시 긴 변 ≤1600px·WebP·q0.82로 이미 다운스케일해 뒀다(실측 196~205KB).
   - 라이트박스(확대보기)는 **범위 밖**. 만들지 않는다.
   (FR28, UX-DR12, `DESIGN.md:141`, `EXPERIENCE.md:78`)

3. **갤러리 조회 정렬은 `order by sort_order, id`** — `listing_images`를 읽는 새 조회는 **반드시 2차 정렬 키 `id`까지** 건다.
   - 이유: `sort_order` 동률을 막는 DB 제약이 없다(`docs/tech-debt.md` #47-2 — 유니크 제약을 일부러 안 걸었다). 2차 키를 빼면 **새로고침할 때마다 사진 순서가 바뀐다.**
   - 대표 = 정렬 첫 행. **`is_cover` 컬럼을 보지 않는다**(파생값). 대표 판별 로직을 **새로 만들지 않는다** — 계산 자리는 `web/src/lib/images/coverImages.ts` 하나다.
   - **이 정렬을 고정하는 단위테스트 1건**을 남긴다(`sort_order` 동률 행을 섞어 넣어 순서가 결정론적임을 단언). 선례: `web/src/lib/images/coverImages.test.ts`.
   - ⇒ 이 AC가 통과하면 `docs/tech-debt.md` **#59를 닫는다**(9.4가 목록 카드 절반만 이행하고 열어 둔 항목).
   (`conventions.md` §10.1·§10.2, tech-debt #59)

4. **이미지 로드 실패 폴백은 반드시 2겹** — 갤러리 대표·썸네일 **모두**에 적용한다.
   - ① `onError` 콜백 + ② **ref 콜백에서 `img.complete && img.naturalWidth === 0` 확인**.
   - ⚠️ **`onError` 하나만 복사하면 같은 자리를 다시 밟는다.** 서버가 그린 `<img>`는 HTML 도착 즉시 로드를 시작하고, 하이드레이션 전에 끝난 실패는 React가 이벤트를 재생하지 않아 `onError`가 **영영 발화하지 않는다**. 404가 정확히 이렇게 빨리 실패한다(9.4 실브라우저 실측으로 발견, 리뷰 3개 레이어가 전부 못 잡음).
   - `getPublicUrl`은 **파일 존재를 확인하지 않는다**(순수 문자열 조립). 깨진 이미지를 막는 유일한 장치가 이 2겹이다.
   - 구현 참고: `web/src/components/listings/ListingCardImage.tsx:25-42, 61-69`.
   (`conventions.md` §10.2)

5. **차량정보 재배치 + 디자인 토큰 적용** — 현재 필드를 새 순서·토큰으로 옮긴다. **필드를 새로 추가하지 않는다.**
   - **"15필드"는 FR5 정의이고 실제와 일치한다**(실측 확인). 내역: 표 13행(제조사·모델·차종·연식·가격·주행거리·색상·연료·변속기·배기량·승차인원·지역·사고이력) + **옵션** + **설명** = 15. 옵션·설명은 표 행이 아니라 별도 `<section>`이라 표만 세면 13으로 보인다. 근거: `supabase/migrations/0002_listings.sql`("FR5 15필드" 블록) · `ListingDetailFields.tsx:14, 57`.
   - `seller_name`은 **FR5 15필드 밖**이다(0007이 넣은 비정규화 표시값, 값이 없으면 행 자체를 숨김). 15에 포함해 세지 말 것.
   - 재배치 시 **15개 전부 남아야 한다.** 옵션은 AC1의 ③ 옵션 섹션으로, 설명은 차량정보 섹션 하단에 둔다. **필드를 새로 만들지도, 빠뜨리지도 않는다.**
   - 토큰: `text-section`(섹션 제목) · `text-body`(값) · `text-meta`/`text-ink-muted`(라벨) · `bg-surface-raised` · `border-hairline` · `rounded-card` · `shadow-card`. 가격은 `text-price-emphasis` + **상세 large 변형(최대 30/800)**.
   - ⚠️ 현재 페이지는 `text-zinc-500`·`bg-red-50`·`bg-green-100` 같은 **원시 Tailwind 팔레트**를 쓴다. 전부 `globals.css`의 8.1 토큰으로 교체한다. **새 토큰을 만들지 않는다**(전부 이미 있다).
   (D9, `DESIGN.md` 타이포/색 토큰)

6. **404 중립 화면** — 미존재·삭제·판매완료(sold) ID로 상세 URL 직접 접근 시:
   - 문구 **그대로**: `매물을 찾을 수 없어요. 삭제됐거나 판매완료된 매물일 수 있어요.` + CTA `매물 목록으로`(→ `/search`).
   - **중립 톤이다. `danger`(빨강) 금지.** 지금 코드의 `bg-zinc-100` 손수 마크업을 `EmptyState`(또는 `ErrorState tone="neutral"`) 프리미티브로 교체한다 — **새 에러 컴포넌트를 만들지 않는다**(`web/src/components/ui/`에 이미 있다).
   - 조회 자체 실패(DB 에러)는 이와 **구분**해 `ErrorState tone="danger"`로 남긴다(현재 `bg-red-50` 손수 마크업을 교체).
   - sold 필터는 **손으로 다시 짜지 않는다** — `buyerListingsQuery`(`web/src/lib/listings.ts:29`)가 FR11 단일 출처다. sold는 `!listing`으로 합류해 자동으로 이 화면이 된다.
   (FR11, UX-DR20, `EXPERIENCE.md:60, 95-96`)

7. **sticky 문의 CTA 자리** — 문의 개시 로직은 **기존 Epic 5 코드를 그대로 재사용**한다. 여기서 만드는 건 배치뿐이다.
   - **모바일(<640px)**: 하단 고정 바(가격 + CTA 상시, top-shadow = `shadow-float`).
   - **데스크톱(≥1024px)**: 2열 배치의 **우 요약 컬럼이 `position: sticky`(top = navbar 높이)** — 긴 섹션을 스크롤해도 문의 CTA가 상시 보인다(D9 "구현 필수").
   - 분기 3가지를 **모두** 유지한다(현재 동작 = 회귀 금지선):
     - 비로그인 → `/login?redirectedFrom=...` 링크(FR58: 열람은 열려 있고 **게이트는 행동에만**)
     - 로그인 + 타인 매물 → `InquiryButton`(`openOrCreateRoom` → `/chat/{roomId}`)
     - **로그인 + 본인 매물 → "문의하기" 대신 "내 매물 관리"** 노출(현재는 버튼을 아예 숨긴다 — 이 AC가 바꾸는 지점이다. 링크 목적지는 기존 판매자 관리 화면).
   (UX-DR23, D9, `EXPERIENCE.md:95`)

8. ~~**사진 저작자 표시(`credit`)**~~ — ❌ **제외 (사용자 결정 2026-07-20). 구현하지 않는다.**
   - 이유: 과제·연습용이고 공개 배포를 유지할 의사가 없다 → CC BY 표시 의무의 발생 조건(공중 배포·송신) 자체가 없어진다. 경위·목업·되살아나는 조건은 `docs/tech-debt.md` #70에 기록.
   - **dev가 할 일: 아무것도 없다.** 갤러리 조회 `select`에 `credit`을 넣지 않아도 되고, 캡션·팝오버·출처 섹션도 만들지 않는다. **"있으면 좋을 것 같아서" 넣지 말 것**(A2·A3).
   - `listing_images.credit` 컬럼과 시드 스크립트의 저장 동작은 **그대로 둔다**(건드리지 않는다). 나중에 켤 때 비용이 낮다.
   - 번호는 비워 둔다 — 아래 AC를 당겨 올리면 Tasks·리뷰의 참조가 어긋난다.

9. **D5 반응형 무결성 + 2열/스택 전환**
   - 데스크톱 **2열**(좌 갤러리 / 우 요약·sticky) → 좁아지면 **스택 1열**. 폭 축소는 **열 수로만 흡수**한다.
   - **컴포넌트 내부 가로 배치(meta 한 줄·썸네일 스트립·"1/N" 카운터·버튼 라벨)를 세로로 접지 않는다.** 공간 부족은 `truncate`·가로 스크롤(썸네일 스트립)로만. **줄바꿈 찌그러짐·2줄로 밀리는 버튼 = 금기.**
   - 검증은 **실제 브라우저 3개 뷰포트**(1280 · 800 · 390px)에서 눈으로 + 스크린샷(AC11).
   (D5, project-context 규칙13)

10. **관리자 상세 무회귀** — `ListingDetailFields.tsx`는 **관리자 매물 상세(`web/src/app/(admin)/admin/listings/[id]/page.tsx:116`)와 공유 컴포넌트**다.
    - 관리자 화면 리스킨은 **Epic 15의 몫이며 이 스토리 밖**이다. 이 스토리의 변경으로 관리자 상세가 **깨지거나 의도치 않게 바뀌면 회귀**다.
    - 권장 방향: 공유 컴포넌트를 in-place로 뒤집는 대신 **구매자 상세 전용 레이아웃을 새로 짜고, 공유 컴포넌트는 관리자가 계속 쓰게 둔다.** 다른 길을 택했다면 이유를 Completion Notes에 남긴다.
    - 어느 길이든 **관리자 매물 상세를 실제로 열어 눈으로 확인**하고 결과를 기록한다("코드상 안 건드림"으로 갈음하지 않는다).

11. **실브라우저 검증(DoD)** — 아래를 실제로 돌려 관찰하고 결과·스크린샷을 Completion Notes에 남긴다. "코드를 그렇게 짰다"로 갈음하지 않는다(CLAUDE.md B4).
    - ① 사진 여러 장 있는 매물: 갤러리 넘기기(화살표·썸네일·키보드), "1/N" 정확
    - ② 사진 0장인 시드 매물: 플레이스홀더, 에러 0건
    - ③ 존재하지 않는 uuid · sold 매물 id로 직접 URL 접근 → 404 중립 화면 문구 일치
    - ④ 비로그인 / 로그인-타인매물 / 로그인-본인매물 3분기
    - ⑤ 1280·800·390px 뷰포트 D5 무결성
    - ⑥ 관리자 매물 상세(AC10)
    - ⑦ **일부러 깨서 red 확인**: 존재하지 않는 `storage_path`를 가진 이미지 행으로 2겹 폴백이 실제로 발화하는지(=플레이스홀더로 떨어지는지) 확인 후 되돌린다.

## Tasks / Subtasks

- [x] **Task 0: 착수 전 실측** (AC 전체)
  - [x] `web/AGENTS.md` 지시대로 `node_modules/next/dist/docs/`를 먼저 읽는다 — **Next 16은 훈련데이터의 Next와 다르다**
  - [x] `docs/conventions.md` §4·§6·§6.1·§10·§10.1·§10.2 재확인(값의 정본)
  - [x] `web/src/app/(user)/listings/[id]/page.tsx`(183줄)·`InquiryButton.tsx`(76줄)·`components/listings/ListingDetailFields.tsx`(109줄) 전체 읽기
  - [x] `web/src/components/ui/` 프리미티브 목록 확인 — **있는 걸 다시 만들지 않는다**

- [x] **Task 1: 갤러리 데이터 조회** (AC2, AC3)
  - [x] `listing_images`에서 해당 매물의 **전 행**을 `select('storage_path, sort_order')` + `.order('sort_order').order('id')`로 조회하는 함수를 `web/src/lib/listings.ts`(또는 `lib/images/`)에 추가 — **`credit`은 넣지 않는다**(AC8 제외)
  - [x] `attachCoverImages`를 갤러리에 재사용하려 하지 말 것 — **대표 1장+개수만 주는 목록용 함수**다
  - [x] `getPublicUrl(bucket, path)`로 URL 조립(동기·실패 없음)
  - [x] 정렬 결정론 단위테스트 1건 추가(`sort_order` 동률 섞어서)

- [x] **Task 2: 갤러리 컴포넌트** (AC2, AC4, AC9)
  - [x] `web/src/components/listings/ListingGallery.tsx` 신규(`'use client'` — 현재 인덱스 상태 필요)
  - [x] 대표 5:3 + 썸네일 스트립 + "1/N" + 좌우 화살표, 키보드 조작
  - [x] **2겹 폴백**(`onError` + ref `complete && naturalWidth===0`) — `ListingCardImage.tsx`에서 패턴만 가져온다
  - [x] 0장이면 "사진 준비중" 플레이스홀더(`bg-placeholder-bg` + 카메라 글리프)

- [x] **Task 3: 페이지 골격 재구성** (AC1, AC5, AC9, AC10)
  - [x] `page.tsx`를 2열(좌 갤러리 / 우 요약 sticky) → 스택으로 재배치
  - [x] 섹션 4개 순서 세우기(신뢰·판매자는 빈 슬롯)
  - [x] 차량정보·옵션을 새 순서·토큰으로 이전, 관리자 공유 컴포넌트 회귀 방지
  - [x] 조회 `select`에 갤러리용 컬럼 추가 여부 확인(별도 쿼리면 불필요)

- [x] **Task 4: 상태 화면 + 문의 CTA** (AC6, AC7)
  - [x] 404 중립 화면을 `EmptyState`로, DB 에러를 `ErrorState tone="danger"`로 교체
  - [x] sticky 문의 바(모바일 하단 고정 / 데스크톱 우 컬럼 sticky)
  - [x] 본인 매물 → "내 매물 관리" 링크 추가(기존엔 숨김만 했다)

- [x] **Task 5: 검증 + 대장 정리** (AC10, AC11)
  - [x] 실브라우저 7항목 실행·스크린샷
  - [x] `docs/tech-debt.md` **#59 닫기**(실제 이행 근거를 한 줄로 함께 적는다). #70은 이미 ⚪ 의도적 보류로 처리됨 — **다시 열지 말 것**
  - [x] 새로 발견한 것은 **대장에 등재**(#72·#73이 상세 페이지에도 상속되는지 판단해 기록)

### Review Findings

코드리뷰 2026-07-20 (새 세션·3레이어 병렬: Blind Hunter / Edge Case Hunter / Acceptance Auditor).
분류 전에 **전부 실제로 확인했다** — 마이그레이션 스키마 조회, 프리미티브 코드 읽기, 테스트·타입·린트 재실행, 사진 전송량 실측(B4).

#### 결정 완료 (사용자, 2026-07-20 — 4건 전부 해소)

- [x] **① AC2 썸네일 전송량 → 현 상태 수용(C).** 선택지를 실측으로 좁힌 뒤 고른 결정이다 — Supabase 이미지 변환은 **실측 `403 FeatureNotEnabled`**(유료 기능)라 쓸 수 없고, 업로드 시 썸네일 생성은 I14와 부딪히며 9.5 범위를 넘고, `next/image`는 설정 추가 + Vercel 과금이 붙는다. 데모 규모(약 1MB)에선 감내 가능. **수용의 조건 2가지를 이행했다**: 완료 노트 #3의 잘못된 "AC2 만족" 주장 정정, 대장 **#83** 등재(트리거 = #68 전량 시딩 → 3~4MB). `ListingGallery.tsx` 헤더 주석도 사실에 맞게 고쳤다.
- [x] **② 사진 1장일 때 화살표·썸네일 미표시 → 현행 유지(A).** dev 판단 수용. 넘길 사진이 없는데 화살표를 두면 눌러도 제자리이고, 썸네일 줄에 사진 하나만 남는다. **코드 변경 없음.**
- [x] **③ 640~1023px 구간 → 대표 사진에 높이 상한(A).** `max-h-[60vh] lg:max-h-none`을 5:3 컨테이너에 걸었다(사진 있음·0장 두 경우 모두). 폭은 그대로 두고 높이만 조여 `object-cover`가 흡수하므로 D5(세로로 접지 않는다)를 지킨다. **검증이 끝난 데스크톱 2열은 `lg:max-h-none`으로 무변경.**
- [x] **④ 가격 임의값 → 토큰 추가(A).** 사실 확인 결과 **dev가 지침대로 한 것이 맞았다** — `DESIGN.md:42`가 *"상세 대표 가격은 large 변형 최대 30까지"*를 이미 규정했고 AC5도 그대로 옮겼다. 구멍은 토큰 층에 있었다(8.1이 26px만 만들고 large 변형을 누락). `globals.css`에 `--text-price-lg: 30px` 추가 후 `text-[30px] leading-[1.2]` → `text-price-lg`로 교체. **새 디자인 결정이 아니라 기존 규정의 토큰화**라 AC5의 "새 토큰 만들지 않는다"와 충돌하지 않는다.

#### (원문) 결정이 필요했던 항목

- [x] **[Review][Decision] AC2 위반 — 썸네일이 원본 사진을 그대로 받아 첫 화면에 큰 사진을 N장 내려받는다** — AC2는 *"첫 화면에 대표 1장 + 썸네일들만 로드한다. 큰 사진을 N장 미리 받지 않는다"*(NFR7). 그런데 썸네일 `<img src={url}>`이 대표와 **동일한 원본 URL**을 쓴다(`ListingGallery.tsx:166-174`). **실측**(사진 3장 매물 `ff73b989`): 서버 HTML의 사진 `<img>` 4개가 가리키는 **고유 원본 3개 = 1,063 KB**(217 / 421 / 423 KB, 전부 1600px). 썸네일 표시 크기는 80×56 CSS px다. 10장 상한이면 3~4 MB로 늘어난다. Completion Note #3은 이 구성이 "AC2를 만족한다"고 적었으나 **AC2가 규정한 것은 DOM에 든 큰 `<img>` 개수가 아니라 내려받는 바이트**다. 고르는 문제라 결정이 필요하다 — ① Supabase 이미지 변환 URL로 썸네일만 축소해 받기(새 의존·설정 결정) / ② 업로드 시 썸네일 규격 생성(**I14 "저장본을 다시 만들지 않는다"와 충돌**) / ③ 현 상태 수용하고 대장에 등재(데모 규모에선 감내 가능).
- [x] **[Review][Decision] 사진이 1장일 때 화살표·썸네일 스트립을 그리지 않는다** — AC2는 네 구성요소를 조건 없이 나열한다. dev가 Completion Note #5에 의도와 이유를 밝혔고 *"되돌리기 쉬운 3줄"*이라 적었다. 판단이 갈리는 자리라 그대로 올린다. (`ListingGallery.tsx:111, 149`)
- [x] **[Review][Decision] 640~1023px 구간이 규정 공백으로 남아 화면을 히어로가 다 먹는다** — 2열 전환은 `lg`(1024px)부터라, 1023px에서 5:3 대표 사진이 약 975×585px가 되어 차량정보 섹션이 전부 첫 화면 아래로 밀린다. 우 컬럼 sticky도 이 구간엔 없다(하단 고정 바가 대신). AC7·AC9가 <640과 ≥1024만 규정하고 그 사이를 비워 둔 결과이며 dev도 Completion Note #7에서 공백을 인지하고 하단 바로 덮었다. 대표 사진에 `max-h`를 줄지, 태블릿 구간을 2열로 당길지는 UX 결정이다. (`page.tsx:209`)
- [x] **[Review][Decision] 상세 가격이 토큰이 아니라 임의값 `text-[30px] leading-[1.2]`** — AC5는 토큰만 쓰되 **새 토큰을 만들지 말라**고 했는데, 30px 토큰이 없다(`globals.css`의 `--text-price`는 26px). 그래서 둘 중 하나를 어겨야 하는 구조다. dev는 "새 토큰 안 만들기"를 택했고 결과적으로 같은 가격이 데스크톱 30px(임의값) / 모바일 26px(토큰) 두 경로로 렌더된다. `--text-price-lg` 토큰을 추가할지(= AC5 문구 완화) 임의값을 남길지 결정이 필요하다. (`page.tsx:230` vs `:245`)

#### 고칠 것 (판단 불필요)

- [x] **[Review][Patch] 404·에러 화면에 `<h1>`이 하나도 없다 — 이전 버전엔 있었다** [`web/src/app/(user)/listings/[id]/page.tsx:146-155, 167-175`] — 교체 전 두 분기 모두 `<h1>매물 상세</h1>`를 렌더했는데(`git show e885726` 확인), `EmptyState`·`ErrorState`는 제목을 `<p>`로만 그린다(프리미티브 확인함). 결과적으로 그 화면들엔 heading 요소가 0개 — 문서 개요와 heading 탐색이 깨진다. ※ 같은 지적에 있던 *"`role="alert"`도 사라졌다"*는 **틀렸다**: `ErrorState`는 `role="alert"`, `EmptyState`는 `role="status"`를 이미 갖고 있다.
- [x] **[Review][Patch] 대장 #68 안에 취소된 credit 작업 체크박스가 살아 있다** [`docs/tech-debt.md:705`] — 같은 커밋이 #70을 ⚪ 의도적 보류로 닫으면서, #68의 *"9.7에 남은 일(인수조건 체크박스로 심을 것)"* 목록에 있는 `- [ ] **저작자 표시(credit) 화면 렌더** — #70 참조`는 그대로 뒀다. B5대로면 9.7 스토리를 만들 때 **취소된 작업이 인수조건으로 심긴다.** 대장이 자기모순 상태다(B8: 대장은 하나, 모순 금지).
- [x] **[Review][Patch] 문의 CTA 3분기의 버튼 폭이 갈린다 (①② `w-full` / ③ `w-fit`)** [`page.tsx:70, 82` vs `InquiryButton.tsx:65`] — `InquiryCta`를 한 자리에 모은 이유가 *"두 군데에 복붙하면 한쪽만 고쳐져 갈린다"*였는데, **분기 사이**에서 갈렸다. `InquiryButton`이 `w-fit`을 내부에 하드코딩하고 className을 받지 않아서, 로그인+타인매물일 때만 320px sticky 카드와 하단 바에서 버튼이 왼쪽에 작게 붙는다.
- [x] **[Review][Patch] 갤러리 상태가 `urls` 변경에 반응하지 않는다 (현재는 도달 불가한 잠재 결함)** [`ListingGallery.tsx:41, 44`] — `index`·`failed`는 `useState`로만 살고 `urls`가 바뀌어도 초기화되지 않으며, 호출부에 `key`가 없다(`page.tsx:211`). 사진 10장 매물에서 index=7일 때 2장짜리로 바뀌면 `urls[7]`이 `undefined`가 되어 `src` 없는 `<img>`와 "8/2" 카운터가 뜬다. **다만 지금 앱에선 재현되지 않는다** — 상세→상세 직접 이동 링크가 없어(진입로는 `/search` 카드와 채팅방뿐, grep 확인) 사이에 다른 라우트를 거치며 컴포넌트가 언마운트된다. 16.2 앱 상세나 "관련 매물" 같은 링크가 생기는 순간 열린다. 예방은 `<ListingGallery key={listing.id} …>` 한 줄.

#### 이월 (대장 `docs/tech-debt.md`에 등재)

- [x] **[Review][Defer] 갤러리 접근성 3종 — 사진 전환이 스크린리더에 고지되지 않음 · 썸네일 자동 스크롤 없음 · `truncate` 값에 `title` 없음** [`ListingGallery.tsx`, `ListingDetailSections.tsx:51`] — 대장 **#79**로 등재. Epic 10/15 리스킨 때 함께.
- [x] **[Review][Defer] 사진 용량 근거 "실측 196~205KB"가 실제와 다르다 (실측 217·421·423 KB)** — 대장 **#80**. `next/image` 미채택·썸네일 규격 미생성 결정이 모두 이 숫자를 근거로 삼고 있어 문서 정정이 필요하다.
- [x] **[Review][Defer] 정렬 비교 로직이 `galleryImages`·`coverImages` 두 벌** — 대장 **#81**. AC3의 *"계산 자리는 하나"* 원칙과 어긋나나 등가성 테스트로 묶여 있어 지금 당장의 위험은 없다.
- [x] **[Review][Defer] `InquiryCta`가 항상 두 벌 마운트되어 리사이즈 시 상태가 갈린다** — 대장 **#82**. `hidden lg:block`/`lg:hidden`은 CSS라 두 인스턴스가 각자 `busy`·`error`를 갖는다.
- [x] **[Review][Defer] 조회 실패와 "사진 0장"이 화면상 구별 불가** — **이미 #73에 등재·판단 기록 완료**(dev가 Task 5에서 상속 판단을 남겼다). 새 항목을 만들지 않는다.

#### 기각 (오탐 — 확인 근거)

Blind Hunter가 프로젝트를 모른 채 스키마·프리미티브를 추측해 생긴 것들이다.

- `year·price·mileage·displacement·seats`가 null이면 상세가 500난다 → **전부 `not null`**(`0002_listings.sql:42-51`)
- `accident_free`가 null이면 없는 사고이력을 있다고 표시한다 → **`boolean not null default true`**(`:55`)
- `sort_order`가 null이면 비교자가 NaN을 반환해 정렬이 무너진다 → **`int not null default 0`**(`0012_listing_images.sql:22`)
- `storage_path` 중복 시 React key 충돌 → **`text not null unique`**(`:21`)
- 에러·404 화면이 스크린리더에 조용하다 → `ErrorState` `role="alert"` / `EmptyState` `role="status"` 확인
- `InquiryButton`이 두 번 마운트돼 채팅방 생성·분석이 두 번 실행된다 → **마운트 시 도는 effect·fetch가 없다**(클릭 핸들러에서만 실행)
- `bg-black`·`text-xs` 같은 원시 유틸 → 9.4 `ListingCardImage.tsx:105`의 동일 배지 선례가 수용된 상태라 이 스토리가 만든 드리프트가 아니다(A3)
- 404 문구가 DOM 2노드로 갈렸다 → 화면에 읽히는 문장은 AC6과 글자 그대로 같다(dev가 `innerText`로 실측, Completion Note #8)

#### 독립 재검증 (dev 주장 대조)

| dev 주장 | 재실행 결과 |
|---|---|
| 단위테스트 9파일 89건 통과 | ✅ 일치 |
| `tsc --noEmit` 통과 | ✅ exit 0 |
| `eslint` 통과 | ✅ exit 0 |
| AC10 `ListingDetailFields.tsx` 0줄 변경 | ✅ diff에 부재 |
| AC8 `credit` 미구현 | ✅ 설명 주석 2곳 외 등장 없음 |
| AC5 15필드 보존·원시 팔레트 제거 | ✅ 13행+옵션+설명=15, 변경 파일에 `text-zinc-*`·`bg-red-*` 0건 |

#### 실브라우저 재검증 (2026-07-20, 사용자 요청으로 리뷰어가 직접 수행)

⚠️ **먼저 정정:** 이 리뷰는 처음에 *"Playwright 브라우저가 설치돼 있지 않아 확인 불가"*라고 적었는데 **틀렸다.** MCP 도구가 `chrome-for-testing` 채널을 찾다 실패했을 뿐, 번들 chromium은 있었다. 에러 메시지를 환경 제약으로 읽고 검증을 건너뛸 뻔한 자리다.

| 확인 | 결과 |
|---|---|
| **③ 태블릿 상한이 실제로 듣는가** | ❌→✅ **처음 넣은 60vh는 효과가 0이었다.** 대표사진은 585→461px로 줄었지만 차량정보 섹션이 **하단 고정 바에 정확히 가려져** 바 위로 보이는 높이가 **0px**였다(바 상단 707px = 섹션 상단 707px). 숫자상으론 "화면 안"이라 통과처럼 보였고 **스크린샷을 봐서 잡았다.** 실측으로 재선정: 60vh→0px · 55vh→39px · **50vh→77px(채택)** · 45vh→116px |
| **무회귀 4개 뷰포트** | 1280×900 = 760×456(5:3 유지, 상한 미적용) 2열 유지 · 1023×768 = 384px 상한 작동 · 820×1180 = 463px(5:3 유지) · 390×844 = 205px(5:3 유지). **전부 가로 스크롤 0.** 상한이 의도한 구간에서만 걸린다 |
| **문의 CTA 3분기 폭** | 320px 카드 안에서 **셋 다 278px 동일** — 비로그인 `로그인하고 문의하기` / 타인매물 `문의하기`(수정 대상) / 본인매물 `내 매물 관리`. 로그인해서 실제로 확인 |
| **404·sold 중립 화면** | 진짜 sold 매물(현대 그랜저 IG)로 직접 접근 → 문구 일치, `text-danger` 0개, 차량 정보 유출 0건, `<h1>` 복구 확인 |
| **사진 전송량(AC2)** | `curl` 실측 1,063 KB — 위 #83 |

**여전히 이 리뷰가 확인하지 않은 것:** dev의 AC11 ①②④⑥⑦(갤러리 키보드 조작·0장 플레이스홀더·관리자 상세·2겹 폴백 red/green). dev의 기록을 신뢰했고 재현하지 않았다. 특히 ⑦은 DB를 일부러 깨야 해서 재현 비용이 있다. dev가 *"⑦이 안 보는 것"*으로 **2겹 중 ①(`onError`)의 발화는 관찰되지 않았다**고 스스로 한계를 적어 둔 점은 정직한 기술이다.

## Dev Notes

### 0. 범위 경계 — 먼저 읽을 것

**안(이 스토리):** 구매자 매물 상세 `web/src/app/(user)/listings/[id]/` · 새 갤러리 컴포넌트 · `listing_images` 전체 행 조회 유틸 1개.

**밖(하지 말 것):**
- 신뢰 뱃지 **내용**·옵션 카테고리 분류·희소옵션 강조·찜 동작·판매자 정보 내용 → **Epic 10** (여긴 빈 슬롯만)
- AI 응답 카드 사진 → **9.6** · 시드 사진 전량 → **9.7**
- Flutter 앱 상세/스와이프 → **16.2**
- 관리자 화면 리스킨 → **Epic 15**
- `view_count` → **Epic 11** · DB 마이그레이션 → 이 스토리는 스키마를 건드리지 않는다
- 라이트박스(확대보기)·이미지 모더레이션 → Epic 9 Non-goal

### 1. 이미지 계약 — 지금 유효한 것만 (9.0이 뒤집었다)

⚠️ **9.1·9.2 스토리 문서의 "비공개 버킷 + 서명 URL(TTL 3600)" 설계는 폐기됐다.** Story 9.0(마이그 `0014`, ADR-IMG-02)이 **공개 버킷 + 고정 URL**로 대체했다. `getSignedUrl`·`getSignedUrls`·`SIGNED_URL_TTL`은 **코드에서 삭제됐다** — 찾지 말 것.

지금 유효한 계약(`docs/conventions.md` §10):
- 버킷 `listing-images`, **`public=true`**. 공개 URL = `{SUPABASE_URL}/storage/v1/object/public/listing-images/{storage_path}`, **만료 없음**
- 경로 `{user_id}/{listing_id}/{filename}` — 첫 세그먼트가 소유자
- `getPublicUrl(bucket, path)` — **동기·순수 문자열 조립·실패하지 않음·파일 존재 확인 안 함**(`web/src/lib/storage/index.ts:15`)
- 상한: 매물당 10장 · 장당 5MB · MIME `image/jpeg`·`png`·`webp`
- 테이블: `listing_images(id, listing_id, storage_path unique, sort_order, is_cover, credit)` — `supabase/migrations/0012_listing_images.sql`

### 2. FR11과 사진 URL의 관계 (헷갈리기 쉬운 자리)

- **FR11이 막는 것**: sold 매물이 목록·검색·**상세**·AI 응답에 **나타나는 것**. → 이 스토리에선 `buyerListingsQuery`가 `status='on_sale'`만 통과시키므로 sold는 자동으로 404 화면(AC6)이 된다.
- **FR11이 막지 않는 것**: 판매중일 때 사진 주소를 저장해 둔 사람이 나중에 그 주소를 다시 여는 것. **명시 수용된 결정**이다(`conventions.md` §6.1, tech-debt #65 ⚪). **이걸 "고치려고" 서명 URL을 되살리지 말 것.**
- ⚠️ `conventions.md` §6이 이 스토리를 콕 집어 경고한다: *"9.5(상세 갤러리)·9.6(AI 카드)이 같은 테이블을 열 때가 정확히 규칙7이 경고한 자리다."* → **새 조회 경로를 열면 FR11 강제 지점 목록에 등록**해야 한다. 실무적으로는 **갤러리 조회를 `buyerListingsQuery`로 매물을 먼저 좁힌 뒤 그 `listing_id`로만** 하면 자동 충족된다(9.4의 `attachCoverImages`가 쓴 방식과 동일).

### 3. 재사용 목록 — 다시 만들지 말 것

| 있는 것 | 경로 | 쓰임 |
|---|---|---|
| `getPublicUrl(bucket, path)` | `web/src/lib/storage/index.ts:15` | 모든 사진 URL 조립 |
| `coverImages(rows)` | `web/src/lib/images/coverImages.ts:38` | 대표 판별(계산 자리는 여기 하나) |
| `buyerListingsQuery(supabase, columns)` | `web/src/lib/listings.ts:29` | FR11 단일 출처, 이미 상세가 쓰는 중 |
| 2겹 폴백 패턴 | `web/src/components/listings/ListingCardImage.tsx:25-42, 61-69` | 갤러리에 복사 |
| `EmptyState` / `ErrorState` | `web/src/components/ui/` | 404 / DB 에러 화면 |
| `Button` / `buttonClasses()` | `web/src/components/ui/Button.tsx` | CTA (`<Link>`엔 `buttonClasses()`) |
| `FocusTrap` | `web/src/components/ui/FocusTrap.tsx` | (라이트박스는 범위 밖이라 지금은 불필요) |
| `Skeleton` / `CardSkeleton` | `web/src/components/ui/Skeleton.tsx` | 갤러리 스켈레톤, `aspect-[5/3]`+`bg-placeholder-bg` 선례 |
| `openOrCreateRoom(...)` | `web/src/lib/chat.ts:58` | 문의 개시(그대로 재사용) |
| 색·타이포·radius·shadow 토큰 | `web/src/app/globals.css` | **새 토큰 만들지 않는다** |

**`next/image`**: 9.4가 **쓰지 않기로** 결정했다(저장본이 이미 작고, `next.config.ts`에 `images.remotePatterns`가 없고, 공개 URL 고정이라 브라우저/CDN 캐시가 먹는다). 갤러리도 같은 판단이 서면 평범한 `<img>`를 쓰고, 다르게 판단했다면 **이유를 Completion Notes에 남긴다**(`remotePatterns` 추가는 설정 변경이므로 조용히 하지 말 것).

### 4. 현재 코드 상태 (실측 — 이걸 바꾼다)

`web/src/app/(user)/listings/[id]/page.tsx` 183줄, 서버 컴포넌트, `export const dynamic = 'force-dynamic'`(CM3: sold 즉시 비노출 — **유지할 것**).
- `page.tsx:77-82` `buyerListingsQuery(...).eq('id', id).maybeSingle<ListingDetail>()`
- `page.tsx:94-112` DB 에러 화면(손수 `bg-red-50` 마크업) → AC6에서 `ErrorState`로 교체
- `page.tsx:115-133` "찾을 수 없음" 화면(손수 `bg-zinc-100`) → AC6에서 `EmptyState`로 교체
- `page.tsx:145-154` 제목·"판매중" 배지·부제(연식·가격)
- `page.tsx:159-167` 문의 CTA 3분기(비로그인 링크 / `InquiryButton` / 본인이면 **아무것도 안 그림**)
- `page.tsx:172` `<ListingDetailFields listing={listing} />` ← **관리자와 공유**(AC10)
- **사진 관련 코드는 어디에도 없다**(`page.tsx:6` 주석이 "사진 없음"이라 적어 뒀다)
- `notFound()`·`not-found.tsx`는 **이 리포 전체에 0건**이다 — 관례형 404 대신 "조건부 렌더 + 커스텀 안내 UI"가 이 리포의 일관된 패턴이다. **관례를 바꾸지 말 것**(AC6은 이 패턴 위에서 프리미티브만 교체하는 것).

`ListingDetailFields.tsx:60-74` 13필드 + `seller_name` 조건부. 옵션·설명은 별도 `<section>`(`:78-106`).

### 5. 함정 모음

- **`onError` 하나로는 부족하다** — AC4. 9.4에서 리뷰 3개 레이어가 전부 놓치고 실브라우저가 잡았다.
- **2차 정렬 키 `id`를 빼면 순서가 조회마다 바뀐다** — AC3. DB 유니크 제약이 없다(#47-2, 의도적).
- **`credit`은 조회하지 않는다** — AC8이 제외됐다. 컬럼은 DB에 그대로 있으니 `select *`를 쓰면 딸려온다(쓰지 말 것, 필요한 컬럼만 명시).
- **3중 무음 폴백**(tech-debt #73): 조회 실패→`withoutImages()`, `getPublicUrl` 존재 미확인, `onError`→플레이스홀더. **전면 장애와 "사진 없는 매물"이 화면상 구별되지 않는다.** 상세도 같은 경로를 쓰면 그대로 상속한다 — 해소는 이 스토리 범위 밖이지만, **상속 여부를 판단해 대장(#73)에 한 줄 남긴다.**
- **`<a>` 안에 `<button>`은 유효하지 않다** — 9.4가 카드에서 밟은 함정. 상세에서도 링크 안에 버튼을 넣지 말 것.
- **관리자 상세 공유 컴포넌트** — AC10. "코드상 안 건드렸다"가 아니라 **열어 보고** 확인한다.
- **`fuel`(연료)** — 카드 meta에서 빠져 있다(tech-debt #67, Epic 10 예약). 상세 표엔 이미 있으므로 **여기서 손대지 않는다.**

### 6. 테스트·검증 표준

- **web = Playwright E2E 우선**(Next.js 공식: async 서버 컴포넌트는 단위테스트 대신 E2E). 다만 **이 리포엔 Playwright 설정도 `e2e/`도 없다**(9.4 기록) → 이번에도 검증은 **브라우저 MCP로 직접 관찰 + 스크린샷**이 된다.
- 서버 컴포넌트 밖 **순수 유틸은 Vitest 단위테스트**로 보강 → AC3의 정렬 테스트가 여기 해당(선례 `coverImages.test.ts`).
- ⚠️ **CI가 web 테스트를 돌리지 않는다**(워크플로는 `migration-gate.yml` 하나, `paths:`가 마이그·스크립트) — 로컬에서 직접 돌린다(tech-debt #29).
- **"만들었다"가 아니라 "잡는다"가 완료다** — AC11 ⑦(일부러 깨서 red 확인 후 green 복귀).

### 7. `credit` 표시 — ❌ 하지 않기로 결정됨 (AC8 제외)

**dev는 이 절을 읽고 "그래도 넣는 게 낫지 않나" 판단하지 말 것.** 결정은 끝났다. 아래는 나중에 되살릴 때를 위한 근거 보관이다.

실제 DB 값(실측, `listing_images.credit` jsonb):
```json
{"author":"Benespit","license":"CC BY-SA 4.0",
 "source":"https://commons.wikimedia.org/wiki/File:0_Kia_Carnival_(KA4).jpg",
 "title":"File:0 Kia Carnival (KA4).jpg"}
```

| 안 | 화면 | 장점 | 단점 |
|---|---|---|---|
| **A** | 갤러리 바로 아래 12px 회색 한 줄 (`사진 © Benespit · CC BY-SA 4.0 · Wikimedia Commons`) | 늘 보여 의무 이행이 확실. 구현 최단 | 시연 화면에 텍스트 한 줄이 항상 붙음 |
| **B** | 사진 위 작은 ⓘ 버튼 → 클릭 시 팝오버 | 평소 화면이 가장 깔끔 | 클릭해야 보임(의무 이행 해석 여지). 팝오버 상태·바깥클릭 닫기 코드가 조금 더 |
| **C** | 갤러리는 그대로, **페이지 최하단 "사진 출처" 섹션**에 N장 모아서 | 갤러리 디자인 무손상. 관행적(위키·언론사 방식) | 사진과 출처가 멀어짐 |

세 안 모두 **코드 부담은 비슷하다**(select에 컬럼 1개 + 렌더 몇 줄). 차이는 시연 화면의 인상뿐.
목업: 세션 스크래치패드 `credit-mockup.html` / `credit-mockup.png`.

**결정 (사용자, 2026-07-20): 하지 않는다.** 과제·연습용이고 공개 배포를 유지할 의사가 없다. CC BY 계열의 표시 의무는 **공중에 배포·송신할 때** 발생하므로, 배포를 내리면 의무의 발생 조건 자체가 없어진다 — **"코드로 막아야 할 버그"가 아니라 "공개 배포를 유지하는 한 지켜야 하는 것"**이었다.

⚠️ 되살아나는 조건: `bmad-encar-demo.vercel.app`(Production)이 공개로 떠 있는 동안은 의무가 실재한다. **공개 배포를 계속 유지하는 쪽으로 방향이 바뀌면** 위 3안 중 하나를 그때 고르면 된다. 경위 정본은 `docs/tech-debt.md` #70.

### Project Structure Notes

- 라우트 그룹 `app/(user)` 유지. 새 컴포넌트는 `components/listings/`, 새 유틸은 `lib/images/` 또는 `lib/listings.ts`.
- import 별칭 `@/*` → `./src/*`. TypeScript **strict**. UI 문자열은 **한국어**.
- Tailwind **v4** — `tailwind.config.js`가 아니라 `globals.css`의 `@theme`. React 19 / Next 16 App Router — 서버 컴포넌트가 기본, `'use client'`는 필요한 곳에만(갤러리는 상태가 있으므로 클라이언트, **페이지 전체를 클라이언트로 만들지 말 것**).
- 브랜치: `develop`에서 작업. `main` 병합은 사용자 승인 시에만.

### References

- `_bmad-output/planning-artifacts/epics-increment-2026-07-12.md:529-543` (Story 9.5 원문), `:158`(UX-DR12), `:170`(UX-DR20), `:173`(UX-DR23), `:174`(UX-DR24)
- `_bmad-output/planning-artifacts/ux-designs/ux-bmad-encar-demo-2026-07-12/.decision-log.md:127-135` (D9), `:88-94` (D5)
- `.../DESIGN.md:141`(갤러리), `:107`(D5), `:14-54`(토큰), `:159`(섹션 순서·목업 경고)
- `.../EXPERIENCE.md:60`(404 문구), `:78`(갤러리), `:95-96`(상세 상태 패턴), `:32`(공개 접근)
- `docs/conventions.md` §4·§4.1·§6·§6.1·§9·§10·§10.1·§10.2
- `docs/tech-debt.md` #59(정렬·9.5 예약, **닫을 것**) · #70(credit·⚪ 의도적 보류로 종결, **손대지 말 것**) · #47-2 · #65 · #72 · #73 · #29
- `_bmad-output/implementation-artifacts/9-4-매물-카드-레이아웃-b-*.md`(2겹 폴백 경위·토큰 대비 실측·`next/image` 미채택 근거)
- `_bmad-output/implementation-artifacts/9-0-공개-버킷-전환-서명-url-제거.md`(ADR-IMG-02)
- `_bmad-output/project-context.md` 규칙 5·6·7·9·11·12·13 · `web/AGENTS.md`
- `_bmad-output/planning-artifacts/nav-ia-rules.md:43-46`(R5 출처 복귀 — 상세 뒤로가기 폴백 `/search`)

## Dev Agent Record

### Agent Model Used

claude-opus-4-8[1m] (dev-story 워크플로)

### Debug Log References

- dev 서버: `npx next dev -p 3100` (검증 후 종료). 로그 = 세션 스크래치패드 `dev.log`
- 스크린샷 7장 = 세션 스크래치패드 `story-9-5/95-01..07-*.png` (**리포에 커밋하지 않는다** — 이 리포는 검증 스크린샷을 한 번도 커밋한 적이 없다. 실측으로 확인한 관례라 바꾸지 않았다. Discord로 전달)
- red/green 확인용 DB 조작: `listing_images.storage_path`를 없는 파일로 바꿨다가 원복 (아래 AC11 ⑦)

### Completion Notes List

#### 무엇을 만들었나 (파일 4개 신규 · 3개 수정)

- **`lib/images/galleryImages.ts`(신규)** — 매물 1건의 사진을 `sort_order` → `id` 순으로 늘어놓는 **순수 함수**. `coverImages`(매물 여러 건의 대표 1장)와 같은 규칙, 다른 모양이다. `galleryImages(rows)[0] === coverImages(rows)의 대표`가 되도록 맞췄고 그것을 테스트로 단언한다 — **대표 판별이 두 갈래로 갈리지 않게**(9.3 코드리뷰의 교훈).
- **`lib/listings.ts`(수정)** — `fetchListingGalleryUrls(supabase, listingId)` 추가. 쿼리에도 `.order('sort_order').order('id')`를 걸어 **정렬을 두 층에 박았다**(B9).
- **`components/listings/ListingGallery.tsx`(신규, `'use client'`)** — 대표 5:3 + 썸네일 스트립 + "1/N" + 좌우 화살표 + 키보드.
- **`app/(user)/listings/[id]/ListingDetailSections.tsx`(신규)** — 구매자 전용 차량정보·옵션 섹션(AC10 대응, 아래 참조).
- **`app/(user)/listings/[id]/page.tsx`(재작성)** — 2열/스택 골격 + 섹션 4개 + 404/에러 프리미티브 + sticky CTA.

#### 판단이 갈렸던 자리 (리뷰가 볼 곳)

1. **AC10 — 공유 컴포넌트를 고치지 않고 새로 만들었다.** `ListingDetailFields.tsx`는 관리자 상세와 공유한다. 제자리에서 뒤집으면 **범위 밖인 관리자 화면이 함께 바뀐다**(리스킨은 Epic 15). 그래서 구매자 전용 레이아웃을 라우트 폴더에 새로 두고 **공유 컴포넌트는 손대지 않았다**(0줄 변경). 대가로 두 화면의 마크업이 잠시 갈라지는데, 이건 스토리가 권장한 방향이고 Epic 15가 합류 여부를 판단한다.
2. **`next/image`를 쓰지 않았다** — 9.4와 **같은 판단**이다(저장본이 이미 196~205KB로 작고, `next.config.ts`에 `images.remotePatterns`가 없어 설정 변경이 선행이며, 공개 URL이 고정이라 브라우저·CDN 캐시가 그대로 먹는다). 설정을 조용히 바꾸지 않기 위해 명시한다.
3. **썸네일에 별도 규격을 만들지 않았다** — 같은 URL을 `loading="lazy"`로 받는다. 저장본을 다시 만들지 않는다는 I14를 지킨다. 부수 효과로 사진을 넘길 때 대표 자리가 **캐시에서 즉시** 뜬다(넘길 때마다 새로 받지 않는다).
   - **⚠️ 정정 (2026-07-20 코드리뷰):** 원래 이 항목은 *"AC2를 같이 만족한다"*고 적었으나 **사실이 아니다.** AC2는 *"큰 사진을 N장 미리 받지 않는다"*를 요구하는데, 썸네일이 대표와 같은 원본 URL을 쓰므로 첫 화면에서 원본이 장수만큼 내려온다. **실측: 사진 3장 매물 = 1,063 KB**(217/421/423 KB, 전부 1600px). AC2가 규정한 것은 DOM에 든 큰 `<img>` 개수가 아니라 **내려받는 바이트**였다.
   - **사용자 결정 2026-07-20 — 현 상태 수용.** Supabase 이미지 변환은 이 프로젝트에서 막혀 있고(실측 `403 FeatureNotEnabled` = 유료 기능), 업로드 시 썸네일 생성은 I14와 부딪히며 9.5 범위를 넘고, `next/image`는 설정 추가와 배포 과금이 붙는다. 데모 규모(약 1MB)에선 감내 가능하다는 판단. **경위·트리거 정본은 대장 #83**(트리거 = #68 전량 시딩 시 3~4MB가 되는 지점).
   - **덤으로 근거 숫자도 틀렸다** — 이 문서 여러 곳의 "실측 196~205KB"는 실제(217~423KB)와 다르다. 같은 리포의 `resize.ts:40`이 이미 *"1600×1067 WebP 553KB"*라 적어 두어 **리포 안에서 두 숫자가 충돌**하고 있었다. 대장 **#80**으로 등재.
4. **화살표는 끝에서 감싼다(wrap), 비활성으로 두지 않았다** — 사진 갤러리의 통상 거동이고, 흐린 비활성 버튼을 사진 위에 얹지 않아도 된다.
5. **사진이 1장이면 화살표·썸네일 스트립을 그리지 않는다** — 고를 것이 없는 컨트롤이라 의미 없는 잉크가 된다. **"1/N" 카운터는 남긴다**(정보라서). AC2의 구성 목록을 문자 그대로 다 그리는 대신 이렇게 읽었다 — 다르게 판단하면 되돌리기 쉬운 3줄이다.
6. **sticky 오프셋 = `top-6`(헤더 높이 아님)** — AC7은 "top = navbar 높이"라 적었지만, 이 앱의 `AppHeader`는 **sticky가 아니라 함께 스크롤된다**(실측). 헤더 높이만큼 띄우면 헤더가 사라진 뒤 빈 틈만 남는다. 그래서 본문 여백(`p-6`)과 같은 값으로 맞췄다.
7. **하단 고정 바 경계 = `lg`(1024px), AC7의 "모바일 <640px"보다 넓다** — AC7은 <640과 ≥1024만 규정하고 **그 사이(640~1023)를 비워 뒀다.** 둘 중 하나가 그 구간을 덮어야 CTA가 사라지지 않는다. 우 컬럼 sticky는 ≥1024부터이므로, 하단 바가 그 아래 전부를 맡는다.
8. **404 문구는 2개 노드로 나뉜다** — `EmptyState`가 title/description 2단이라 `매물을 찾을 수 없어요.` + `삭제됐거나 판매완료된 매물일 수 있어요.`로 넣었다. **화면에 읽히는 문장은 AC6과 글자 그대로 같다**(실측 `innerText`로 확인, 아래 ③). DOM 노드 하나로 붙이려면 프리미티브를 고쳐야 하는데 그건 이 스토리 밖이다.
9. **`credit`은 어디에도 넣지 않았다** — 조회 `select`·렌더 모두. 결정(#70 ⚪)대로다.

#### AC11 실브라우저 검증 — 7항목 전부 실행·관찰 (2026-07-20, localhost:3100)

| # | 무엇을 | 결과 |
|---|---|---|
| ① | 사진 3장 매물(기아 카니발 KA4) 갤러리 | 화살표 클릭 1/3→2/3, 키보드 `→` 2/3→3/3→**1/3(wrap)**, 썸네일 클릭 → 2/3 + 해당 썸네일에 `aria-current`. 대표 `naturalWidth=1600`. 콘솔 에러 **0** |
| ② | 사진 0장 매물(현대 repro63 아반떼) | 5:3 "사진 준비중" 플레이스홀더, 깨진 아이콘 없음, 콘솔 에러 **0** |
| ③ | 미존재 uuid `00000000-…` · sold 매물 `510d8b23-…` | 둘 다 동일 화면. `innerText` = `매물을 찾을 수 없어요.` / `삭제됐거나 판매완료된 매물일 수 있어요.` / `매물 목록으로` — **AC6 문구 일치**. `.text-danger` **0개**(중립 톤 확인) |
| ④ | CTA 3분기 | 비로그인 → `로그인하고 문의하기` (`/login?redirectedFrom=%2Flistings%2F…`) · 로그인+타인매물 → `문의하기` 버튼 · 로그인+본인매물(seller-seed) → **`내 매물 관리`**(`/sell`) |
| ⑤ | 1280 · 800 · 390px | 1280=2열(우 sticky 요약), 800·390=스택 1열 + 하단 고정 바. 390px에서 `document.scrollWidth === clientWidth === 390` — **가로 스크롤 0**. 제목+배지 한 줄, 하단 바 가격+CTA 한 줄(줄바꿈·2줄 밀림 없음) |
| ⑥ | 관리자 매물 상세 | admin@test.com으로 열어 눈으로 확인 — **기존 그대로**(기본 정보 → 옵션 → 설명, 옛 스타일). 콘솔 에러 **0**. `ListingDetailFields.tsx`는 0줄 변경 |
| ⑦ | **일부러 깨서 red 확인** | `sort_order=0` 행의 `storage_path`를 없는 파일로 UPDATE → 대표 자리 **+ 1번 썸네일** 둘 다 플레이스홀더(`svg` 2개, 깨진 아이콘 0), 나머지 썸네일 2장은 정상, **"1/3" 카운터는 유지**. 원복 후 → 플레이스홀더 **0개**, `<img>` 4장 전부 `naturalWidth=1600` **green 복귀** |

**⑦이 증명하는 것 — 발화한 것은 2겹 중 ②(ref 콜백)다.** Next 16 문서(`01-getting-started/05-server-and-client-components.md`)가 명시하듯 **클라이언트 컴포넌트도 서버에서 HTML로 프리렌더**된다. 그래서 `<img>`는 첫 HTML에 있고 404는 **하이드레이션 전에** 끝난다 — React가 그 에러 이벤트를 재생하지 않으므로 `onError`는 발화할 수 없다. 그런데도 플레이스홀더가 떴다 = `img.complete && naturalWidth===0`을 보는 ref 콜백이 잡았다는 뜻이다. (9.4에서 리뷰 3개 레이어가 전부 놓쳤던 바로 그 자리다.)

**⑦이 안 보는 것:** 하이드레이션 **이후** 실패(①`onError` 경로)는 이 방법으로 재현되지 않는다 — 그건 사용자가 보는 동안 파일이 사라지는 경우다. 두 겹 중 ①은 코드에 있으나 **이번 실측에서 발화가 관찰되지는 않았다.**

#### 단위테스트

`galleryImages.test.ts` **6건 신규**. red 확인(모듈 없음 → 실패) → green 6/6. 전체 스위트 **9파일 89건 통과**(회귀 0). `tsc --noEmit` 통과 · `eslint` 통과 · `next build` 통과.

#### 대장(`docs/tech-debt.md`) 정리

- **#59 닫음** — 마지막 소비처(상세 갤러리)가 정렬을 걸고 **검사로 고정**됐다. 요약표 카운트 17→16.
- **#73 상속 판단 기록** — 상세도 목록과 **똑같은 3중 무음 폴백**을 상속한다(조회 실패 → 빈 배열). 상세에선 사진 영역 **전체**가 통째로 "준비중"이 되므로 목록보다 아프다. 다만 해소엔 관측 수단이 필요하고 상세만 다르게 처리하면 규칙이 갈리므로 **여기서 안 고쳤다** — 트리거(#68 전량 시딩) 그대로.
- **#72 상속 판단 기록** — 같은 `getPublicUrl`을 쓰지만 web↔dart **새 비교면은 생기지 않는다**(앱 상세는 16.2). 변경 없음.
- **#70(credit)은 손대지 않았다** — ⚪ 의도적 보류 그대로.

#### 계약 문서(`docs/conventions.md`) 갱신

- **§6 FR11 강제 지점 — 이미지 축에 소비처 목록을 만들고 `fetchListingGalleryUrls`를 등재했다.** §6이 *"9.5·9.6이 같은 테이블을 열 때가 정확히 규칙7이 경고한 자리"* 라고 이 스토리를 콕 집어 경고했던 자리다. 다음 사람이 §6만 읽어도 **어떤 경로가 이 테이블을 여는지** 알 수 있게 목록을 남겼다.
- **§10.2 2겹 폴백 — 9.5 이행 사실과 red/green 실측 결과를 붙였다.**

### File List

**신규**
- `web/src/lib/images/galleryImages.ts`
- `web/src/lib/images/galleryImages.test.ts`
- `web/src/components/listings/ListingGallery.tsx`
- `web/src/app/(user)/listings/[id]/ListingDetailSections.tsx`

**수정**
- `web/src/app/(user)/listings/[id]/page.tsx`
- `web/src/lib/listings.ts`
- `docs/conventions.md`
- `docs/tech-debt.md`
- `_bmad-output/implementation-artifacts/sprint-status.yaml`
- `_bmad-output/implementation-artifacts/9-5-상세-페이지-골격-사진-갤러리.md`

**변경 없음(의도적)**
- `web/src/components/listings/ListingDetailFields.tsx` — 관리자와 공유하므로 건드리지 않았다(AC10)
- `web/src/app/(user)/listings/[id]/InquiryButton.tsx` — 문의 개시 로직은 Epic 5 코드 그대로 재사용(AC7)

### Change Log

- 2026-07-20 — Story 9.5 구현: 상세 페이지 골격(신뢰·차량정보·옵션·판매자 4섹션) + 사진 갤러리 + 2겹 로드 실패 폴백 + 404/에러 프리미티브 교체 + sticky 문의 CTA 3분기. 대장 #59 종결, #72·#73 상속 판단 등재. 실브라우저 7항목 검증 완료.
